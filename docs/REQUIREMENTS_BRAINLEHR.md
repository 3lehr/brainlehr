# Brainlehr — kanonischer Root-Lastenkatalog

Stand: 2026-08-25T21:00 (P23-P26: versionierter, lazy geladener Projektkontext)

Normative Quelle: die vollständige Operator-Matrix der 53 `BDW-*`-
Entscheidungen. Decoder und Evidenz:
`/Users/lehrmacbook/.codex/visualizations/2026/08/17/01a010f0-d6a0-73c0-a545-bea4fc2f1316/brainlehr-entscheidungsdialog.html`
und `docs/RESEARCH_ZIELBILD_2026-08-17.md`.

## Geltung

Dies ist der **eine normative Gesamtplan** für Brainlehr. Pläne, `STAND.md`,
`SPRINTS.md`, `AI_HANDOFF.md`, Research und Teilkataloge sind Evidenz oder
Umsetzung, aber keine konkurrierende normative Quelle. Die vorhandenen
Teilkataloge bleiben untergeordnet; ihre lokalen IDs sind Umsetzungsgates.

Katalogstatus und Produkt-Teststatus sind getrennt: `DECIDED` bedeutet, dass
die Entscheidung feststeht, nicht dass sie gebaut oder abgenommen ist.
`NOT RUN` bedeutet, dass das jeweilige Produktgate noch nicht vollständig
ausgeführt wurde. `OPEN`, `CONFLICT`, `DEFERRED` und `PILOT` bleiben
sichtbar, bis dieselbe BDW-ID aktualisiert wird.

**Die Spalte `Latte`** (Norm `b3249558`, 2026-08-20) nennt das **benannte,
abrufbare Vergleichsobjekt** — woran gemessen wird, nicht ob gemessen wurde.
Das ist eine andere Frage als die des Produktgates: Am 2026-08-19 war
`BDW-E07` mit sieben grünen Fällen belegt, während der Bestand vollständig
unverschlüsselt war — gemessen wurde das Modul, nicht der Gegenstand.

Drei zulässige Werte, ein vierter existiert nicht:
`ein benanntes Objekt` (eine Datei, eine Vorgängerfassung, ein reales
Vorbild — nie „modern", nie „gut lesbar") · `n/a` **mit Grund**, wenn kein
Mensch das Ergebnis ansieht · **leer**, wenn niemand entschieden hat. Leer ist
kein Versehen, sondern die einzige ehrliche Angabe, solange die Entscheidung
aussteht; `melder/gatestand.py` zählt solche Zeilen **nicht** als belegt, auch
wenn ihr Gate auf `PASS` steht. Kein Vorgabewert — ein stiller Vorgabewert
vernichtet genau die Unterscheidung, für die das Feld existiert.

**Agil und ohne Dogma** heißt: Dieser Katalog ist versionierbar und darf durch
neue belegte Betreiberentscheidungen geändert werden. Es heißt niemals, Beleg-,
Sicherheits-, Konflikt- oder Testgates abzuschaffen. Änderungen aktualisieren
dieselben `BDW-*`-IDs; eine parallele Requirement-Skala ist unzulässig.

## Statusübersicht

- Offene IDs: **keine**
- Offene Konflikte: **keine**
- Explizit aufgelöste Ursprungskonflikte:
  - `BDW-C01`: fehlende ADR-025/026 werden nicht fingiert; dieser Root ist der
    gewählte neue Zweckbeschluss aus der Research.
  - `BDW-C02`: Brainlehr ist ein governierter Kern mit optionalem
    Enterprise-Profil, keine ungetrennte Vollplattform.
- Pilot: `BDW-C03`, `BDW-E04`, `BDW-E05`
- Deferred: `BDW-E17` — Auswahl `later` bedeutet im gemeinsamen
  Wizard-Decoder ausdrücklich **Später entscheiden**. Zielbild A bleibt davon
  unberührt local-first; zusätzliche Betriebsprofile werden später festgelegt.
- Bereits bindend und unverändert übernommen: `BDW-R01`–`BDW-R05`
- Produkt-Teststatus: Die ursprünglichen 53 Gates begannen mit **NOT RUN**.
  Bestehende Implementierung ist Evidenz, aber keine pauschale Abnahme dieses
  Root-Katalogs; jede spätere Zeile führt ihr eigenes sichtbares Produktgate.

## Genau ein Interpretationsreview

Der Betreiber war bei einzelnen Formulierungen unsicher, hat aber alle 53
Fragen beantwortet. Daher wird keine konkrete Auswahl pauschal auf `OPEN`
gesetzt. Stattdessen findet nach dem ersten daraus abgeleiteten
Implementierungsplan genau ein gemeinsames Review statt: Optionstext,
normativer Satz und `AC1` werden nebeneinander gelesen. Ein Missverständnis
ändert dieselbe BDW-ID mit Begründung; es erzeugt keine neue ID-Skala.

## Belegte Entscheidungen und Produktgrenze

| ID | Auswahl | Normtyp | Status | Vollständige normative Entscheidung | Scope / Owner | Akzeptanzkriterium | Latte | Produktgate | Quelle |
|---|---|---|---|---|---|---| --- |---|---|
| BDW-R01 | `keep` | MUSS | DECIDED | Die Zweischicht bleibt: Brainlehr trägt Wissens-Governance; Openlehr macht Menschen fachlich handlungsfähig. | Core / Architektur | `BDW-R01-AC1`: Eine Boundary-Prüfung ordnet Governance Brainlehr und Fachlogik Openlehr zu. | n/a -- Zuordnungspruefung, Ergebnis ist eine Liste fuer Maschinen | PASS: `pytest tests/test_schichtgrenze_governance.py` grün (4 Fälle; Mutationsprobe gefahren: `UMSATZSTEUER = 0.19` in `kern/` → rot mit Datei und Zeile) | Auswahl „Beibehalten“; ADR-007; RQ-001/002 |
| BDW-R02 | `keep` | MUSS | DECIDED | Atelier bleibt die getrennte gemeinsame Werkbank; gemeinsamer Rahmen und unabtretbare Sicherheit liegen dort, Fachlogik in den Domänen. | Atelier-Grenze / Architektur | `BDW-R02-AC1`: Eine Schichtprüfung weist keine Domänenfachlogik dem Atelier-Kern zu. | n/a -- Schichtpruefung, kein Erzeugnis | PASS: `pytest tests/test_app_schichtregel.py` grün | Auswahl „Beibehalten“; ADR-008/014; RQ-001/002 |
| BDW-R03 | `keep` | MUSS | DECIDED | V1 bleibt nativ und die Beschreibung plattformblind; eine Weboberfläche ist später möglich, aber nicht terminiert. | Produkt / Architektur | `BDW-R03-AC1`: Ein zweiter Renderer kann dieselbe plattformblinde Beschreibung nutzen, ohne V1-Fachlogik zu duplizieren. | die V1-Darstellung desselben Dokuments, Seite fuer Seite (`spikes/pdf_a3_erechnung/rechnung.pdf`) | PASS: `pytest tests/test_domaene_dienst_oberflaeche.py` grün | Auswahl „Beibehalten“; ADR-024; RQ-001/002 |
| BDW-R04 | `keep` | MUSS | DECIDED | Der Vertrauensregler steuert nur die Rückfragepflicht; Belegpflicht und harte Stopp-Punkte bleiben unverändert. | Core / Safety | `BDW-R04-AC1`: Für jede Reglerstufe bleiben Beleggate und harte Stopps identisch wirksam. | n/a -- Reglerstufen, Ergebnis ist ein Schaltzustand | PASS: `pytest tests/test_vertrauensregler_grenzen.py` grün (5 Fälle, Mutationsprobe gefahren: `IMMER_FRAGEN` gekürzt → 2 Tests rot) | Auswahl „Beibehalten“; PLAN_VERTRAUENSREGLER; RQ-001/002 |
| BDW-R05 | `keep` | MUSS | DECIDED | Der lokale, modell- und MCP-neutrale 0.1-Kern bleibt Produktbasis; Quellen, Geltung, Konflikt und Retrievalwirkung sind Kern, eine stabile API ist noch nicht versprochen. | Core / Produkt | `BDW-R05-AC1`: Derselbe Kern besteht einen lokalen MCP-Lauf mit austauschbarem Modellpfad und ausgewiesener Quelle/Geltung. | n/a -- Modellpfad austauschbar, Ergebnis ist eine Quotenzahl | PASS: `pytest tests/test_kern_modellneutral.py` — 4 von 4 grün (vorher 3 + 1 xfail). `knowledge_search` liefert je Knoten `source`, `norm_rang`, `gilt_ab`, `gilt_bis`, `norm_entscheidung`, `freigabe`; bei Lehren nur `freigabe` — die übrigen Spalten existieren in `lessons_learned` nicht und werden weder erfunden noch leer getarnt | Auswahl „Beibehalten“; README; RQ-001/002 |
| BDW-C01 | `new-decision` | MUSS | DECIDED | Aus der Research wird ein neuer Root-Zweckbeschluss abgeleitet; fehlende ADR-025/026 werden nicht als angenommen fingiert. Dieser Katalog ist dieser Beschluss. | Gesamtprodukt / Product Owner | `BDW-C01-AC1`: Jede normative Produktanforderung ist über eine BDW-ID in diesem Root auffindbar; fehlende ADRs werden nur als Genealogie benannt. | n/a -- Auffindbarkeit ueber IDs, kein Erzeugnis | PASS: `pytest tests/test_requirements_brainlehr.py` grün | Auswahl „Neuen Root-Zweckbeschluss aus Research ableiten“; RQ-001/002/016 |
| BDW-C02 | `governed-core` | Profil | DEFERRED | Brainlehr ist ein governierter Kern mit optionalem Enterprise-Profil. | Gesamtprodukt / Architektur | `BDW-C02-AC1`: Core-Tests laufen ohne Enterprise-Connectoren; das Enterprise-Profil ergänzt, ersetzt aber nicht den Core. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | Auswahl „Governierter Kern mit optionalem Enterprise-Profil“; RQ-012/015 |
| BDW-C03 | `pilot` | Pilot | DEFERRED | Das Rollenmodell wird mit dem ersten realen Mehrbenutzer-Piloten aktiviert. | Enterprise / IAM Owner | `BDW-C03-AC1`: Ein Mehrbenutzer-Pilot startet erst nach Rollen-, Subjekt- und Negativrechtetest. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | Auswahl „Mit erstem realen Mehrbenutzer-Piloten“; RQ-016 |
| BDW-P01 | `regulated` | Profil | DEFERRED | Primäre Zielgruppe der ersten Mehrbenutzer-Fassung sind regulierte Großunternehmen. | Enterprise / Product Owner | `BDW-P01-AC1`: Pilotanforderungen benennen Authority, Datenklassen, Betriebsort, Genehmigungen und SLO. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | Auswahl „Regulierte Großunternehmen“; RQ-013/015 |
| BDW-P02 | `root-index` | MUSS | DECIDED | Es gibt einen Root-Katalog mit referenzierten untergeordneten Abschnitten; unverbundene konkurrierende Kataloge sind unzulässig. | Governance / Product Owner | `BDW-P02-AC1`: Ein automatischer Test findet genau diesen Root und alle Teilkataloge verweisen auf ihn. | n/a -- Auffindbarkeit des Roots, kein Erzeugnis | PASS: `pytest tests/test_requirements_brainlehr.py` grün | Auswahl „Ein Root-Katalog mit referenzierten Abschnitten“; cd571222; RQ-016 |
| BDW-P03 | `profiles` | Profil | DEFERRED | Lokale und Enterprise-Fassung nutzen einen Kern mit klaren Profilen, keine getrennten Codebasen. | Architektur / Tech Lead | `BDW-P03-AC1`: Profiltests belegen einen gemeinsamen Core und verhindern divergierende Implementierungen derselben Regel. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | Auswahl „Ein Kern mit klaren Profilen“; RQ-012/015 |
| BDW-P04 | `eval-suite` | MUSS | DECIDED | Retrieval wird mit Treffer-, Falschmelde-, Abstention- und Aktionsgates abgenommen. | Retrieval / Quality Owner | `BDW-P04-AC1`: Ein versionierter Prüfkorpus misst alle vier Gatearten und weist Schwellen aus. | n/a -- Messkorpus, Ergebnis sind Zahlen und Schwellen | TEILWEISE: `python3 messungen/vier_gatearten.py` — 3 der 4 Gatearten über den echten Weg gemessen: Treffer 0,20 (7/35, Schwelle offen), Falschmeldung 0,0 (0/10, Schwelle 0,20 bestanden), Abstention 1,0 (10/10, Schwelle 0,80 bestanden). **Aktion nicht gemessen** — an der Suchfunktion allein nicht beobachtbar, dafür braucht es Agentenläufe mit und ohne Treffer bei gleichem Prompt. | Auswahl „Treffer-, Falschmelde-, Abstention- und Aktionsgates“; RQ-003–006 |
| BDW-P05 | `A` | Profil | DECIDED | Nächste Produktgrenze ist Zielbild A: ein governierter Local-first-Memory-Kern. | Gesamtprodukt / Product Owner | `BDW-P05-AC1`: Der Kern reproduziert in mindestens 95 % des festgelegten Prüfkorpus Aussage, Quelle, Status und Gültigkeit innerhalb eines Abrufs. | der Wortlaut im Quelldokument, aus dem der Knoten stammt (Feld `source`) -- nicht der Knoten selbst | FAIL: `python3 messungen/zielbild_a_vollstaendigkeit.py` — **3/35 = 8,6 %** gegen Schwelle 95 % (vorher 0/35). Einzeln: Aussage 22,9 %, Quelle 17,1 %, Status 17,1 %, Geltung 8,6 %. **Die Schwelle ist mit der heutigen Auslieferung unerreichbar:** 15 der 35 Ziele sind Lehren, und `lessons_learned` führt die geprüften Spalten nicht unter diesen Namen. Es führt aber `session`, `actor`, `model`, `pruefstelle` (Herkunft), `status` (Status, gefüllt) und `first_seen`/`last_seen` (Zeitraum). Ob das als Quelle/Status/Geltung zählt, ist eine ZUORDNUNGSFRAGE, kein Schema-Umbau — nachgesehen 2026-08-19, nachdem hier zunächst die stärkere Behauptung stand. Offen bleibt ein Datenproblem: `session` und `actor` sind in alten Zeilen NULL. Bei den Knoten trägt inzwischen jeder gefundene die Felder (Quelle und Status 6/6 der gefundenen); was dort fehlt, ist der Abruf, nicht die Auslieferung **DIAGNOSE 2026-08-21 (`runs/zielbild_a_diagnose_2026-08-21.json`): DAS AC IST MIT DIESEM BESTAND UNERREICHBAR, unabhaengig vom Abruf.** Vier Toepfe ueber die 35 Faelle: Ziel nicht unter den ersten fuenf **29** · gefunden, Feld leer **3** · gefuellt aber nicht ausgeliefert **0** · alles da **3**. Bei PERFEKTEM Abruf laege die Quote bei **8/35 = 22,9 %** -- der Abruf koennte fehlerfrei werden und aenderte nichts. Grund ist nicht Nachlaessigkeit, sondern Bedeutung: `gilt_ab`/`gilt_bis` sind Eigenschaften einer NORM, und nur 176 von 5247 Knoten tragen `norm_rang`. Fuer die uebrigen 96,6 % und fuer JEDE Lehre gibt es keine Gueltigkeit, die man eintragen koennte, ohne sie zu erfinden -- was der Serverkode an zwei Stellen ausdruecklich verweigert. **Zwei Fehler im Messwerkzeug, beide faellig, beide ohne Wirkung auf die Obergrenze:** es liest ausschliesslich KNOTEN-Feldnamen, obwohl 15 der 35 Ziele Lehren sind (fuer die sind Quelle und Status strukturell falsch-negativ), und alle vier Bestandteile haengen an `aussage` -- ist das Ziel nicht in den ersten fuenf, sind alle vier False, die drei anderen Quoten sind also keine eigenstaendigen Messwerte. Mit korrigierter Lesart bleibt die Obergrenze bei exakt 8/35, weil `geltung` bei 27 von 35 leer ist und allein bindet. **WAS DIESE ZEILE BRAUCHT, bevor die Schwelle eine sinnvolle Frage stellt:** eine Definition von Gueltigkeit JE SORTE -- Norm: `gilt_ab`/`gilt_bis` · Lehre: `first_seen`/`last_seen`/`status` · Faktum: offen. Das ist dieselbe unbeantwortete Frage 3 aus CLAUDE.md ('Was ist ein Treffer wert? -- offen'). Fuenf Messpunkte, `aussage`: 7 -> 8 -> 8 -> 6 -> 6; die 6 ist der stabile Wert bei Bestandsstand 5247/1187. | Auswahl „A · governierter Local-first-Memory-Kern“; RQ-014/015 |
| BDW-P06 | `trigger-or-nothing` | MUSS | DECIDED | Eine Norm ohne Auslöser gilt als nicht eingezogen. Für jede Regel, die brainlehr durchsetzen soll, wird die Stelle benannt, an der sie gebrochen würde, und dort ein Mechanismus verdrahtet; eine bloße Anzeige im Abruf zählt nicht als Durchsetzung. | Governance / Selbstaufsicht | `BDW-P06-AC1`: Ein nachgestellter Verstoß löst einen Mechanismus aus, der ihn benennt — belegt in beide Richtungen (Verstoß schlägt an, erlaubter Fall geht durch). `BDW-P06-AC2`: Jeder gebaute Mechanismus ist an einem Auslöser eingetragen; `melder/ausloeserlos.py` meldet ihn sonst. | n/a -- Waechter schlaegt an oder nicht | TEILWEISE: `melder/rueckfrageschleife.py --selftest` grün, beide Richtungen über den echten Hook-Weg belegt — Verdrahtung in `~/.claude/settings.json` offen (Klientsperre, nur der Betreiber kann eintragen) | Rundruf 2026-08-18 an sieben Sitzungen, Knoten `58da4895`; vier unabhängige Bestätigungen (atelier, openlehr, videoki, fahrtenbuch); `L-4df8f5`, `L-8b2fb0` |
| BDW-P07 | `id-not-name` | MUSS | DECIDED | Ein Name ist ein Attribut mit Geltungszeitraum, nie ein Schlüssel. Gegenstände — Anwendungen, Dienste, Domänen, Bündelkennungen, Verzeichnisse — tragen eine bedeutungslose ID; jeder Name hängt mit `gilt_ab`/`gilt_bis`/`beleg` daran, mehrere Namensarten gleichzeitig. | Kern / Identität | `BDW-P07-AC1`: Eine Anfrage mit einem NICHT MEHR gültigen Namen liefert den Gegenstand samt heutigem Namen und Geltungszeitraum. `BDW-P07-AC2`: Ein nie vergebener Name liefert nichts (kein Erfinden). `BDW-P07-AC3`: Ein Name ohne Beleg wird abgewiesen. | die Antwort auf denselben Abruf mit dem HEUTIGEN Namen | PASS: `kern/gegenstand.py --selftest` grün (3-Namen-Kette `BrainlehrApp`→`Atelier`→`LehrAtelier`, alter Name auflösbar, Negativfall leer, Beleg erzwungen) | ADR-028; Betreiberentscheidung 2026-08-18 „und wenn wir für solche sachen immer eine feste id anlegen?"; `L-0e0ab6` |
| BDW-P08 | `supersede-not-expire` | MUSS | DECIDED | Wissen verfällt nicht durch Zeit, es wird durch einen Nachfolger mit Grund abgelöst. Das Abgelöste bleibt vollständig lesbar und auffindbar und wird nur gekennzeichnet; der Übergang selbst — der GRUND — ist ein eigener Eintrag, weil er Wissen trägt, das in keinem der beiden Zustände steht. | Kern / Wissensbestand | `BDW-P08-AC1`: Nach einer Ablösung sind Titel, Zusammenfassung und Volltext des Abgelösten unverändert vorhanden. `BDW-P08-AC2`: Der Leser des ALTEN Eintrags erfährt, dass es einen neueren gibt, samt Grund. `BDW-P08-AC3`: Über mehrere Stufen bleibt der Grund JEDES Übergangs einzeln erhalten, nicht nur der letzte. `BDW-P08-AC4`: Eine Ablösung ohne Grund wird abgewiesen. | n/a -- Unveraendertheit ist ein Byte-Vergleich | PASS: `kern/abloesung.py --selftest` grün (Inhalt erhalten, Marke gesetzt, Kette über zwei Stufen mit je eigenem Grund, Grund erzwungen, zwei Negativfälle) | Betreiberentscheidung 2026-08-18 „wir sollten die Ablösung dokumentieren und das abgeloeste nicht komplett wegschmeissen … Kann daraus auch wieder Neues wissen oder leeren entstehen?!“; Vergleich mit agentmemory (Ebbinghaus-Verfall) ausdrücklich VERWORFEN; Gegenbeleg im eigenen Haus: `knowledge_zurueckziehen` leert content und summary |

## Enterprise- und Sicherheitsprofil

BSI-Punkte sind Sicherheitsbaseline/Guidance, keine Compliancebehauptung.

| ID | Auswahl | Normtyp | Status | Vollständige normative Entscheidung | Scope / Owner | Akzeptanzkriterium | Latte | Produktgate | Quelle |
|---|---|---|---|---|---|---|---|---|---|
| BDW-E01 | `external` | MUSS | DEFERRED | Ein externer zentraler IdP ist Identitätsauthority. | Enterprise IAM / Security | `BDW-E01-AC1`: Jede produktive Subjekt-ID ist zum IdP rückführbar und Deprovisioning sperrt den Zugriff. | — vertagt (`BDW-C03`) | DEFERRED: Unternehmensanmeldung (IdP/SSO/SCIM) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Externer zentraler IdP“; RQ-007; BSI BER.2.1/2.5 |
| BDW-E02 | `base` | MUSS | DECIDED | RBAC ist die Basisschicht der Autorisierung. | Enterprise IAM / Security | `BDW-E02-AC1`: Default-Deny-Rollentests belegen Least Privilege. | n/a -- Rechtepruefung, Ergebnis ist erlaubt/verweigert | PASS: `pytest tests/test_enigma_zweckprojektion_unbekannte_rolle_default_deny.py` grün | „RBAC als Basisschicht“; RQ-008; BSI BER.4.1/4.2/4.4 |
| BDW-E03 | `intersection` | MUSS | DEFERRED | Wirksames Recht ist die Schnittmenge aus Rolle, Objekt und Zweck; Tenant- und Werkzeuggrenzen verengen zusätzlich. | Enterprise Policy / Security | `BDW-E03-AC1`: Eine Negativmatrix für Rolle × Objekt × Zweck × Tenant verweigert jede fehlende Freigabe. | — vertagt (`BDW-C03`) | DEFERRED: Mandanten — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Rolle ∩ Objekt ∩ Zweck“; RQ-008 |
| BDW-E04 | `first-pilot` | Pilot | DEFERRED | SSO über den zentralen IdP ist ab dem ersten Mehrbenutzer-Piloten Pflicht. | Enterprise IAM / Security | `BDW-E04-AC1`: Der Pilot besteht Login, Sessionbindung, Logout und IdP-Sperre E2E. | — vertagt (`BDW-C03`) | DEFERRED: Unternehmensanmeldung (IdP/SSO/SCIM) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Ab erstem Mehrbenutzer-Pilot“; RQ-007 |
| BDW-E05 | `pilot` | Pilot | DEFERRED | SCIM 2.0 ist ab dem ersten Enterprise-Piloten Pflicht. | Enterprise IAM / Security | `BDW-E05-AC1`: Create, Update, Disable und Delete werden gegen den Pilot-IdP E2E geprüft. | — vertagt (`BDW-C03`) | DEFERRED: Unternehmensanmeldung (IdP/SSO/SCIM) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Ab erstem Enterprise-Pilot“; RQ-007; RFC 7644 |
| BDW-E06 | `tested` | MUSS | DEFERRED | Mandantentrennung wird technisch erzwungen und negativ getestet. | Enterprise Data / Security | `BDW-E06-AC1`: Cross-Tenant-Lesen, -Suche, -Relation, -Export, -Backup und -Admin scheitern. | — vertagt (`BDW-C03`) | DEFERRED: Mandanten — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Technisch erzwungen und negativ getestet“; RQ-008 |
| BDW-E07 | `sensitive` | MUSS | DECIDED | Alle sensiblen Daten und ihre Ableitungen werden ruhend verschlüsselt. | Data / Security | `BDW-E07-AC1`: Daten, Index und Backup eines sensiblen Testfalls sind ohne autorisierten Schlüssel nicht lesbar. | n/a -- Unlesbarkeit ist ein Byte-Vergleich in Datei, Index und Sicherung | PASS: `pytest tests/test_adr031_sensible_knoten_ohne_index.py` (12 gruen), darin `test_schritt2_alle_drei_teile_des_ac_e07_an_einem_fall` -- Daten, Index und Backup eines sensiblen Testfalls in EINEM Lauf, statt aus drei Teilbelegen zusammengesetzt, die nie gemeinsam galten. Ueber den PRODUKTIVEN Weg gemessen (`knowledge_add(..., sensibel=True)` / `knowledge_read`), nicht am Modul allein. Der Schluessel liegt in einer eigenen Datei (`kern/schluesselablage.py`, `BRAINLEHR_SCHLUESSEL`) -- laege er im Bestand, waere jede Sicherung eine Kopie von Schloss und Schluessel. Gegenprobe im selben Lauf: ein Knoten OHNE `sensibel=True` steht weiterhin im Klartext da (Absicht, ADR-031). Nach Schluesselvernichtung bleibt der Knoten sichtbar und sagt, DASS geloescht wurde (ADR-029). **Grenze, ausdruecklich:** kein Bestandsknoten ist heute als sensibel markiert (`select count(*) ... where sensibel=1` = 0); belegt ist der Weg, nicht seine Nutzung. | „Alle sensiblen Daten und Ableitungen“; RQ-009 |
| BDW-E08 | `operator` | Profil | DEFERRED | Der Betreiber entscheidet die Transportverschlüsselung je Betriebsprofil; diese Wahl darf Sicherheitsgates nicht abschaffen. Remote-Pfade benötigen authentisierte Vertraulichkeit und Integrität, Abweichungen nur als dokumentierte isolierte/offline Ausnahme. | Betrieb / Security | `BDW-E08-AC1`: Jedes Profil weist verschlüsselte Remote-Pfade oder eine explizite, getestete lokale/offline Ausnahme aus. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Betreiber entscheidet“; RQ-009; BSI ASST.4.2 |
| BDW-E09 | `customer` | MUSS | DECIDED | Schlüssel sind kundenseitig kontrollierbar. | Key Management / Security | `BDW-E09-AC1`: Rotation, Widerruf und Restore mit kundenseitig kontrolliertem Schlüssel bestehen. | n/a -- Rotation/Widerruf/Restore, kein Erzeugnis | PASS: `pytest tests/test_kundenschluessel.py` grün — Rotation (alter Schlüssel entschlüsselt den neuen Chiffretext nicht mehr), Widerruf (unlesbar ohne Überschreiben) und Restore aus gesichertem Schlüssel je einzeln belegt; Mutationsprobe gefahren (Widerruf als No-op → rot). | „Kundenseitig kontrollierbar“; RQ-009 |
| BDW-E10 | `tamper` | MUSS | DECIDED | Audit ist manipulationsgeschützt und versioniert. | Audit / Security | `BDW-E10-AC1`: Mutation wird erkannt; Korrektur bleibt als neues Ereignis sichtbar. | n/a -- Mutationserkennung, Ergebnis ist erkannt/nicht erkannt | PASS: `pytest tests/test_auditkette.py` grün | „Manipulationsgeschützt und versioniert“; RQ-006/010 |
| BDW-E11 | `stream-export` | MUSS | DEFERRED | SIEM erhält Standardexport und Streaming über ein versioniertes ersetzbares Format. | Audit/SIEM / Operations | `BDW-E11-AC1`: Batch und Stream liefern dasselbe minimierte Ereignisschema ohne Inhaltsleck. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Standardexport plus Streaming“; RQ-010 |
| BDW-E12 | `class-policy` | MUSS | DECIDED | Aufbewahrung folgt einer zentralen Policy je Datenklasse. | Data Lifecycle / Data Owner | `BDW-E12-AC1`: Jede persistierte Datenklasse besitzt Zweck, Frist und Ablaufverhalten. | n/a -- Datenklassen tragen Felder, kein Erzeugnis | PASS: `python3 kern/aufbewahrung.py --selftest` grün — jede Datenklasse trägt Zweck, Frist und Ablaufverhalten; `unbefristet` ist ein eigener, ausdrücklicher Weg statt eines fehlenden Werts; eine Klasse ohne Regel wirft statt einen Vorgabewert zu erfinden. | „Zentrale Policy je Datenklasse“; RQ-010 |
| BDW-E13 | `verified` | MUSS | DECIDED | Löschung erfolgt automatisch, protokolliert und prüfbar über Primärdaten, Indizes, Caches und Kopien. | Data Lifecycle / Data Owner | `BDW-E13-AC1`: Ein Fristlauf entfernt alle Testableitungen und erzeugt einen minimierten Nachweis. | die Sollform des Nachweises in `ADR-029` (Kennung, Zeitpunkt, Ablaufverhalten, kein Inhalt) | PASS: `pytest tests/test_adr031_sensible_knoten_ohne_index.py` (16 gruen), vier Faelle fuer diese Zeile ueber den ECHTEN Bestand: Fristlauf vernichtet den Schluessel und der Inhalt ist weg, waehrend der Knoten samt Titel bleibt und sagt, DASS geloescht wurde (ADR-029) · Legal Hold haelt an und wird laut gezaehlt statt still uebersprungen · ein sensibler Knoten OHNE `datenklasse:`-Etikett faellt sichtbar auf `ohne_regel`, statt still einer nie entschiedenen Frist zugeschlagen zu werden (BDW-E12) · und der Lauf VERWEIGERT sich, solange ein sensibler Knoten im Volltextindex steht -- sonst bescheinigte der Nachweis eine Loeschung, die nicht stattfindet. Der Nachweis bleibt inhaltsfrei (im Test gegen Titel, Zusammenfassung und Inhalt geprueft). `fristlauf()` selbst ist unveraendert; `_BestandsSpeicher` gibt ihm den Bestand in der Form, die es ohnehin verlangt. | „Automatisch, protokolliert und prüfbar“; RQ-010 |
| BDW-E14 | `policy` | MUSS | DECIDED | Legal Hold ist eine explizite Policy mit Freigabe und Audit. | Data Lifecycle / Legal+Security | `BDW-E14-AC1`: Autorisierter Hold blockiert Löschung sichtbar; Aufhebung setzt den Fristlauf fort. | n/a -- Hold blockiert oder nicht | PASS: `pytest tests/test_kundenschluessel.py` grün (11 Fälle) — Legal Hold als Sperre auf der Schlüsselvernichtung (ADR-029): `widerrufen()` wirft `Rechtssperre` statt still durchzulaufen, Inhalt bleibt lesbar; Aufheben löscht NICHT von selbst; Sperre wirkt auch vor dem Anlegen; Sperre ohne Grund wird abgewiesen. Mutationsprobe gefahren: Sperrprüfung entfernt → 2 Tests rot. | „Explizite Policy mit Freigabe und Audit“; RQ-010 |
| BDW-E15 | `managed` | MUSS | DECIDED | Backups sind automatisch, getrennt und offline-fähig. | BCM / Operations | `BDW-E15-AC1`: Eine versionierte verschlüsselte Kopie bleibt nach simuliertem Primärausfall verfügbar. | n/a -- Verfuegbarkeit einer Kopie, kein Erzeugnis | TEILWEISE: gemessen 2026-08-19 je Merkmal, nach dem Umbau. **automatisch: JA** -- LaunchAgent `de.brainlehr.tagessicherung` (taeglich 04:30, `RunAtLoad=false`, damit der Lauf aus dem Zeitplan kommt und nicht aus dem Laden). NICHT behauptet, sondern gefahren: `launchctl kickstart` -> Log `gesichert: sicherungen/brainlehr.db.bak-20260819T172925 (0.13 GB); 1 alte entfernt`. **getrennt: Verzeichnis JA** -- alle zwoelf Schreibstellen rufen `sicherungen.sicherungspfad()`, `kandidaten()` liest beide Orte (rot gegen 858c82c4: fand 0 statt 1). **Datentraeger NEIN** -- gemessen: nur zwei Traeger gemountet, der Startdatentraeger steht bei 100 %. Ein zweites Medium existiert nicht, `BRAINLEHR_SICHERUNGSORT` nimmt es auf, sobald eines da ist. **offline NEIN.** **verschluesselt NEIN** -- die Kopie ist Klartext; das ist dieselbe Luecke wie `BDW-E07` und der Grund, warum diese Zeile nicht auf PASS geht. `kern/sicherungen.py --selftest` gruen, 10 Faelle. | „Automatisch, getrennt, offline-fähig“; RQ-011 |
| BDW-E16 | `regular` | MUSS | DECIDED | Restore wird regelmäßig isoliert getestet, einschließlich RPO/RTO, Integrität, Rechte und Suchfähigkeit. | BCM / Operations | `BDW-E16-AC1`: Ein protokollierter Restore erreicht Profil-RPO/RTO und besteht Rechte- und Recalltest. | n/a -- Restore erreicht Zeitziele, kein Erzeugnis | PASS: `python3 -m pytest tests/test_restore_isoliert.py` — Sicherung isoliert zurückgespielt und dort lesbar; eine beschädigte Sicherung fällt auf (`InvalidTag`, keine stille Falschentschlüsselung); ADR-029 als eigener Fall belegt: Schlüssel vernichtet → Inhalt auch in der Wiederherstellung unlesbar, Tatsache weiter abfragbar | „Regelmäßiger isolierter Restore-Test“; RQ-011 |
| BDW-E17 | `later` | Deferred | DEFERRED | Unterstützte zusätzliche Betriebsprofile werden später entschieden; Zielbild A bleibt local-first, ohne Cloud/Hybrid jetzt normativ festzulegen. | Deployment / Product+Operations | `BDW-E17-AC1`: Vor Aktivierung eines weiteren Profils werden Betriebsgrenzen, Datenflüsse, Residenz und SLO in derselben ID entschieden und getestet. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | Globale Wizard-Auswahl „Später entscheiden“; RQ-011/015 |
| BDW-E18 | `risk` | Profil | DECIDED | Genehmigungen folgen einer risikobasierten Matrix; Vier-Augen gilt selektiv für entsprechend kritische Vorgänge. | Governance / Security | `BDW-E18-AC1`: Regelrang, Export, Connector, Providerwechsel, Ausnahme und Hold sind klassifiziert und korrekt gegatet. | n/a -- Einstufung und Gate, Ergebnis ist hoch/niedrig | PASS: `python3 -m pytest tests/test_risikoeinstufung.py` gruen (27). Die zu grobe Einstufung von `hold` ist am 2026-08-19 aufgeloest: `hold_setzen` NIEDRIG (schuetzt, folgenlos wenn zu Unrecht), `hold_aufheben` HOCH (gibt zur Vernichtung frei, unumkehrbar). `test_e18_die_riskante_haelfte_von_hold_ist_gegatet` ist der einzige Test, der die Trennung erzwingt; rot gegen den festen Bezugspunkt 6832afa0 (KeyError), gruen danach. Gegenprobe in beide Richtungen im selben Test. | „Risikobasierte Matrix; Vier-Augen selektiv“; RQ-011 |
| BDW-E19 | `tenant-region` | MUSS | DEFERRED | Zulässige Datenregionen werden je Mandant technisch begrenzt. | Residency / Data Owner | `BDW-E19-AC1`: Daten, Index, Backup, Telemetrie und Modellfluss verlassen keine erlaubte Region. | — vertagt (`BDW-C03`) | DEFERRED: Mandanten — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Zulässige Regionen je Mandant“; RQ-009/011 |
| BDW-E20 | `default-deny` | MUSS | DEFERRED | DLP/Privacy an Ausgängen ist klassifiziert, minimiert und default-deny. | DLP / Security | `BDW-E20-AC1`: Recall, Fehler, Telemetrie, Connector und Export leaken keine gesperrten Testinhalte. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Klassifiziert, minimiert, default-deny“; RQ-011 |
| BDW-E21 | `profile` | Profil | DEFERRED | SLI/SLO werden je Betriebsprofil festgelegt. | Observability / Operations | `BDW-E21-AC1`: Jedes aktive Profil misst Verfügbarkeit, Recall-Nutzen, Fehlklassifikation, Latenz, Restore und Policy-Denials. | — vertagt (`BDW-C03`) | DEFERRED: zwei Fassungen — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „SLI/SLO je Betriebsprofil“; RQ-011 |
| BDW-P09 | `standalone` | MUSS | DECIDED | Vor der Installation waehlt der Nutzer das Betriebsprofil: `standalone` oder `multiuser`. `standalone` ist der Auslieferungszustand. Die Spalte `mandant` traegt den Vorgabewert `lokal` (Betreiberwort 2026-08-21); die Profilnamen lauten `einzelplatz` und `unternehmen`. Der Wechsel standalone -> multiuser MUSS spaeter moeglich sein; daraus folgt, dass die Mandanten-Achse VORHER im Schema liegt. | Gesamtprodukt / Product Owner | `BDW-P09-AC1`: Ein frischer und ein gewachsener Bestand tragen dieselbe Achse. `BDW-P09-AC2`: Wechsel und Rueckweg je einmal gefahren, mit Bestandszaehlung davor und danach. | die Betriebsdatenbank selbst (5240 Knoten, 1178 Lehren) -- gegen ihre Kopie gefahren, nicht gegen eine Vorrichtung | PASS: `python3 -m pytest tests/test_achsen_b1.py tests/test_betriebsprofil.py` -- 12 + 6 gruen, von mir nachgefahren. AC1: frischer UND gewachsener Bestand tragen dieselbe Achse, je Achse ein Negativtest (NULL scheitert bei mandant/kreis; sprache bleibt NULL statt geraten; eine Geltung fuer Kreis A aendert nichts fuer Kreis B). AC2: Wechsel und Rueckweg gegen eine Kopie der Betriebsdatenbank gefahren, Zaehlung an drei Punkten identisch 5240/1178. Commits `f64e7a12`, `56602630` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P10 | `sprache` | SOLL | DECIDED | Jeder Eintrag traegt die Sprache, in der er geschrieben wurde. KEIN Uebersetzungssystem: Uebersetzung ist ein Problem der Ausgabe, nicht des Speichers. Der Abruf ist bereits mehrsprachig (gemessen: deutsche Frage auf englischen Text 0,819). | Oberflaeche / Barrierefreiheit | `BDW-P10-AC1`: Ein ausgelieferter Eintrag nennt seine Sprache, sodass eine Oberflaeche WCAG 3.1.2 erfuellen kann. | die beiden Grosskorpora als Erwartung: /nasa-llis (1638) muss ueberwiegend en sein, /germanquad (2713) de | PASS: `python3 kern/spracherkennung.py`, Ergebnis `runs/sprachachse_2026-08-21.json`. 3573 de, 1609 en, 58 ohne Zuweisung; 0,05 % Falschzuweisung, 0,71 % Enthaltung, 20er-Stichprobe einzeln gelesen. **BEFUND:** die im Plan als gemessen gefuehrte Heuristik (36 Stoppwoerter, 758/770) EXISTIERTE ALS CODE NICHT -- neu gebaut, reine Stdlib. Commit `f64e7a12` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P11 | `assistent` | MUSS | DECIDED | Der Erststart fuehrt durch die Einrichtung -- im Chat, nicht in einem zweiten Programm. Gefragt werden Profil, Sprache und Einbettungsdienst; Kataloge werden zum Import vorgeschlagen. | Gesamtprodukt / Product Owner | `BDW-P11-AC1`: Ein frisch angelegter Bestand ist nach dem Durchlauf benutzbar. `BDW-P11-AC2`: Ein eingelesener Katalog steht als `nachschlagewerk` und senkt die Trefferquote nicht. | Gemessen gegen dieselbe Nulllinie wie vorher | TEILWEISE: `python3 -m pytest tests/test_einrichtung.py` gruen (19), rot gegen den festen Bezugspunkt b55ecf16 (ModuleNotFoundError: kern/einrichtung.py existierte dort nicht). **AC1 PASS, beide Ausgangszustaende gefahren** -- leer: nach dem Durchlauf Knoten geschrieben und ueber knowledge_fts wiedergefunden; gewachsen: am ECHTEN Bestand (5240 Knoten, 1180 Lehren) springt nichts an, ein Durchlauf mit vollen Antworten laesst knowledge_config-Hash und beide Zaehlungen unveraendert. **AC2 HALB** -- Gattung ja (951 Controls als `nachschlagewerk`, 0 Katalogknoten in irgendeinem Ergebnis), Trefferquote NEIN: 14/35 -> 13/35 (LESSON 7->6, Fall L-606b63). Ursache gemessen, nicht vermutet: derselbe Katalog als `arbeitsbestand` (Positivkontrolle) liefert EXAKT dieselbe Zahl -- die Gattung wirkt am Filter, nicht am Index; knowledge_fts nimmt die Zeilen gattungsblind auf und bm25 ist korpusrelativ. Nulllinie zweimal auf identischen Kopien gefahren, in allen vier Gruppen gleich (kein Rauschen). `runs/einrichtung_2026-08-21.json` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P12 | `herkunftstreu` | MUSS | DECIDED | Fremdimporte erfinden KEINE Herkunft. Sie tragen ihren Weg ein; die Aussage bleibt unbelegt, nur ihr Weg ist bekannt. Gemessen: keiner von acht Wettbewerbern erzwingt ein Herkunftsfeld. | Wissensbestand / Data Owner | `BDW-P12-AC1`: Ein importierter Eintrag traegt eine Herkunft, die den Importweg nennt und keine Quelle behauptet. | Vier Agenten suchten unabhaengig, vier Fehlanzeigen | PASS: `python3 -m pytest tests/test_einrichtung.py` gruen, rot gegen b55ecf16. Herkunft hat genau eine Form -- `importiert aus <Weg> am <Zeitpunkt>` (kern/fremdimport.py::importherkunft), durchgesetzt in `eintragen()`, dem EINEN Schreibweg fuer Kataloge wie Fremdsysteme. Negativtest am SCHREIBWEG, nicht nur am Pruefer: ein Import mit behaupteter Quelle ('laut BGBl I S. 123') wird abgewiesen, danach 0 Knoten im Bestand; dazu vier abgelehnte Belegformen. Gebaut: holographic (memory_store.db, Tabelle facts) und Markdown-Ordner. `runs/einrichtung_2026-08-21.json` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P13 | `vorlauf` | SOLL | DECIDED | brainlehr kennt die Verfallsrate je Themengebiet und den Wissensstichtag des arbeitenden Modells und warnt, wenn beides auseinanderfaellt. Quellen werden zur Laufzeit GESUCHT, nicht hinterlegt -- ein eingebackener Wissensstand ist ab dem naechsten Tag falsch. | Retrieval / Quality | `BDW-P13-AC1`: Vorlaufmessung liegt vor (2026-08-20: 5 Monate am GModG-Fall, gegen 14 Tage Rueckstand im heutigen Verhalten). `BDW-P13-AC2`: Eine gefundene Quelle wird als Eintrag mit Herkunft und Geltung erfasst; zwei unabhaengige Belege, sonst gilt sie als nicht gefunden. | der eigene Bestand: 5240 Knoten in 29 Aesten, Widerrufe aus knowledge_fassungen, zurueckgezogen und den Relationstypen | TEILWEISE: `python3 kern/verfallsrate.py`, Ergebnis `runs/verfallsrate_2026-08-21.json`. AC1 offen -- die Vorlaufmessung am GModG-Fall (5 Monate) ist die des Plans, nicht neu erhoben. Gebaut sind zwei der drei Quellen: Widerrufsquote je Ast gegen den echten Bestand (apps 0,82 / methodik 0,51 / brainlehr 0,33 / germanquad 0,00 / nasa-llis 0,00) und die Schaetzung als GESETZTER Wert ohne Vorgabe (Ablage nach dem Lauf leer, 0 gesetzt -- das ist das Ergebnis). **BEFUND:** die Quelle misst Bearbeitungsintensitaet, nicht fachlichen Verfall; 4351 von 5240 Knoten (83 %) sind Grossimporte mit Rate 0, und ob ein Ast stabil ist oder nur nie geprueft wurde, kann sie nicht unterscheiden. AC2 offen (E2/E3 nicht gebaut). Commits `abde354f`, `f6933dbd` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P14 | `englisch` | MUSS | DECIDED | Schnittstelle, Feldnamen, Docstrings und Dokumentation werden englisch. Betreiberentscheidung gegen den Einwand des Assistenten: Ziel ist internationale Verbreitung, und deutscher Code ist dafuer eine Barriere. | Gesamtprodukt / Product Owner | `BDW-P14-AC1`: README und CONTRIBUTING englisch. `BDW-P14-AC2`: Kein deutscher Parametername in der MCP-Schnittstelle (heute 10 von 94). | die englischsprachigen READMEs vergleichbarer Speicher-Projekte -- ein fremder Entwickler landet auf einer englischen Tuer | AC1 PASS, AC2 offen: README.md und CONTRIBUTING.md englisch, deutsche Fassungen als README.de.md / CONTRIBUTING.de.md erhalten und gegenseitig verlinkt; alle relativen Links geprueft, beide Dateien ganz gelesen. **BEFUND:** README.md war bereits englisch (fruehere Sitzung, Zweig nicht gemergt) -- deutsch waren nur zwei Absaetze am Ende. AC2 (kein deutscher Parametername in der Schnittstelle, heute 10 von 94) ausdruecklich NICHT angefasst: er faellt mit einem Schemaumbau zusammen, und zweimal angefasst entsteht ein Zwischenstand mit `mandant` neben `tenant`. Commit `df9299cf` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-P15 | `ablage` | MUSS | DECIDED | brainlehr ist auch Dokumentenablage. Drei Schichten, und nur die mittlere geht in den Volltextindex: die Datei (mit Pruefsumme, nicht im Index), der Knoten (Verweis, Pruefsumme, Herkunft, Geltung, kurze Zusammenfassung -- im Index) und der Auszug (eine belegte Stelle mit Fundstelle -- im Index). Der ORT der Datei ist eine Einstellung JE DOMAENE (`ablage.<domaene>` = `domaene` oder `brainlehr`, Vorgabe `domaene`), Bauform wie `mitstart.<domaene>` aus ADR-023. Die Einstellung entscheidet NICHT, ob eine Pruefsumme gefuehrt wird -- die wird immer gefuehrt. | Gesamtprodukt / Data Owner | `BDW-P15-AC1`: Ein Dokument wird in BEIDEN Stellungen abgelegt und in beiden ueber seine Zusammenfassung gefunden; ein Dokumentknoten ohne `quell_hash` wird abgewiesen. `BDW-P15-AC2`: Die Trefferquote des Arbeitsbestands bleibt gleich, gegen dieselbe Nulllinie und denselben Fragensatz -- das loest ADR-026 ein. **KONSIL 2026-08-21, 2 zu 1: KEIN zweiter Vektorraum.** Nachgestellt: bei zwei Raeumen laesst ein `gattung`-Wechsel ohne Vektorumzug den Knoten in KEINEM Raum stehen, ohne Fehlermeldung, und `melder/vektorstand.py` meldet dagegen 4354 Falschmeldungen. A scheitert erst, wenn jemand einen einzelnen Knoten vermisst; C scheitert in einer Messung. **Und `sensibel` ist fuer Dokumente Dritter das falsche Werkzeug** -- gemessen Rang 854/1130/2571 statt 1-3, weil es den Kanal entzieht, der Namen findet. Geschuetzt wird ueber `mandant`/`kreis`/`freigabe` (Abfragefilter, umkehrbar), nicht ueber Verschluesselung. | die Nulllinie des Abrufs vor der Ablage (2026-08-21: 14/35 ueber `kern/abrufguete.py`) | PASS: `python3 -m pytest tests/test_dokumentenablage.py` -- 18 gruen, von mir nachgefahren. AC1: ein Dokument in BEIDEN Stellungen abgelegt und in beiden ueber ein Suchwort gefunden, das NUR in der Zusammenfassung steht; ein Dokumentknoten ohne `quell_hash` wird vom Trigger abgewiesen (BI und BU). Gegenprobe beide Richtungen: unveraendert schweigt der Waechter, nach einer Zeile Aenderung meldet er zwei Befunde mit Soll- und Ist-Hash, nach der Ruecknahme schweigt er wieder. Echter Lauf mit einem 9,96-MB-Energieausweis aus buckeberg, sha256 des Originals vorher und nachher identisch. `quell_hash` 49 -> 52, die drei neuen sind Dokumentknoten. **AC2 offen**, siehe BDW-P05: die Trefferquote wurde nach dem Bau gemessen (14/35 unveraendert), aber die Nulllinie stammt aus demselben Lauf. Commit `81e10b9f` | Betreiberentscheidung 2026-08-21, `docs/adr/ADR-032-dokumentenablage-der-ort-ist-eine-einstellung.md` |
| BDW-P16 | `gegenstand` | MUSS | DECIDED | Wer oder was gemeint ist, ist eine EIGENE Achse, kein Name im Text. Personen, Objekte und Vertragspartner werden als Gegenstand gefuehrt und an Eintraegen gebunden; der Name gilt ueber die Zeit und kann wechseln (ADR-028: ein Name ist nie ein Schluessel). Gemessen 2026-08-21: `gegenstaende` traegt 2 Zeilen, `gegenstand_namen` 7 -- beide Gegenstaende sind Software. Keine Person, kein Objekt. Bindend VOR jeder Dokumentenablage und vor jedem Werkzeug mit Beteiligten, weil sich einem Altbestand kein Gegenstand rueckwirkend zuschreiben laesst. | Wissensbestand / Data Owner | `BDW-P16-AC1`: Ein Eintrag nennt seinen Gegenstand als Kennung, nicht als Zeichenkette; eine Umbenennung des Gegenstands aendert die Auffindbarkeit NICHT. `BDW-P16-AC2`: Negativtest -- zwei Personen mit demselben Namen bleiben unterscheidbar, und ein Eintrag ohne gebundenen Gegenstand wird als solcher ausgewiesen statt geraten. **ERSTANWENDUNG entschieden 2026-08-21 (Betreiber): die PLANKENNUNGEN.** Gemessen: 57 Plandateien, `S12` dreimal echt vergeben (`PLAN_S12_ZWEITER_ANLAUF`, `PLAN_DESTILLE`, `SPRINTS.md`), `S1` in mehreren. Der Betreiber verlangte einen Schluessel, der auch Datum und Projekt traegt -- das sind NAMEN, und ADR-028 verbietet Namen als Schluessel. Aufloesung ohne Umnummerierung: jeder Sprint bekommt einen stabilen, bedeutungslosen Schluessel; `S12`, die Ablagedatei, das Datum und das Projekt werden seine NAMEN mit `art_des_namens` und Geltung. Ein alter Verweis auf `S12` bleibt aufloesbar, weil dasteht, wann und wo dieser Name galt -- drei gleichnamige Sprints sind dann drei Gegenstaende, kein Widerspruch. Als Erstanwendung gewaehlt, weil Plaene im Repo liegen und kein Bestand Dritter daran haengt. | die vorhandene Bauform aus ADR-028 (`gegenstand_namen` mit gilt_ab/gilt_bis), angewandt auf 2 statt auf Personen | PASS: `python3 -m pytest tests/test_gegenstand_plankennungen.py` -- 18 gruen, von mir nachgefahren. Bestand 2 -> 58 Gegenstaende, 7 -> 231 Namen; der Altbestand loest unveraendert auf (Atelier -> LehrAtelier). AC1: die Kennung ist stabil und bedeutungslos, `S12`/Datei/Datum/Projekt sind NAMEN mit Geltung. AC2: zwei Gegenstaende mit demselben Rufnamen zu verschiedenen Zeiten bleiben unterscheidbar, und die Aufloesung ohne Zeitpunkt waehlt NIEMALS stillschweigend einen aus. Erstanwendung gemessen: 209 Roh-Vorkommen, 56 echte Vergaben, 153 blosse Erwaehnungen; von 21 kollidierenden Kennungen sind 22 Faelle Zweitbenennungen und 34 wirklich verschiedene Sachen. Commit `5403a71b` | Betreibergespraech 2026-08-21, `docs/PLAN_NAECHSTE_STUFE_2026-08-21.md` Paragraf 3.1 |
| BDW-P17 | `faelligkeit` | MUSS | DECIDED | Was wann von wem zu tun ist, ist EINE Achse -- Kalender, Aufbewahrungsfrist, Wiedervorlage, Zahlungsziel und Wartungstermin sind ihre Anwendungen, nicht fuenf Werkzeuge. Gemessen 2026-08-21: `gilt_ab` in der Zukunft steht bei 0 von 5240, `gilt_bis` bei 2 -- der Speicher hat keine Zukunft. Strang F hat den Vorgang mit Zustand gebaut (offen/erledigt/abgelehnt/ueberholt), ihm fehlt nur das Datum. Bindend zuerst, weil sich einem Altbestand keine Faelligkeit rueckwirkend zuschreiben laesst. | Gesamtprodukt / Product Owner | `BDW-P17-AC1`: Ein Vorgang traegt Datum, Zustaendigen und Zustand; die faelligen erscheinen beim Sitzungsstart nach Faelligkeit sortiert, nicht nach Alter. `BDW-P17-AC2`: Negativtest -- ein Vorgang ohne Datum bekommt KEINES geraten, und ein abgeschlossener verschwindet aus der Liste. | die heutige Startliste aus `melder/forderung_vorgang.py --offene` (nach Alter sortiert, ohne Datum) | PASS: `python3 -m pytest tests/test_forderung_faelligkeit.py` -- 7 gruen, mit den Nachbartests 30, von mir nachgefahren. AC1: ein Vorgang traegt Datum und Zustaendigen, die Startliste sortiert nach FAELLIGKEIT statt nach Alter. AC2: ein Vorgang ohne Datum bekommt KEINES hineingeraten, ein ungueltiges Datum scheitert am Trigger. Gegenprobe beide Richtungen samt gemischter Sortierung (mit Frist und ohne). Beide Ausgangszustaende. **GRENZE:** `forderung_zustaendig` ist freier Namenstext statt Gegenstandsverweis -- ein Name als Schluessel, den ADR-028 verbietet. Uebergangsstand; seit `81e10b9f` stehen Personen auf der Achse und es ist nachziehbar | Betreibergespraech 2026-08-21, `docs/PLAN_NAECHSTE_STUFE_2026-08-21.md` Paragraf 3.2 |
| BDW-P18 | `namensfrage` | SOLL | DECIDED | Eine Frage nach einer PERSON wird als Namensfrage erkannt und exakt gesucht, statt den ganzen Satz zu verrechnen. Gemessen 2026-08-21 am echten Bestand: der blosse Name findet 3 von 3 Zielen auf den Raengen 1-3, die natuerlich gestellte Frage (`zeige mir alles was mit Frau Doeldissen zu tun hat`) nur 1 von 3 -- die beiden fehlenden stehen gar nicht mehr in der Kandidatenliste. Auf Rang 2 der schlechten Fassung steht eine ANDERE Person, getroffen auf das Wort `Frau`. Der Verlust sitzt in der Frageform, nicht im Index und nicht am Deckel. | Retrieval / Quality | `BDW-P18-AC1`: Dieselbe Frage in natuerlicher Form liefert dieselben Ziele wie der blosse Name -- 3 von 3, gemessen gegen denselben Fall. `BDW-P18-AC2`: VOLLSTAENDIGKEIT ist das Mass, nicht Rang: `alles was mit ihr zu tun hat` heisst alle Vorkommen, nicht die aehnlichsten. Negativtest: eine Sachfrage wird NICHT als Namensfrage behandelt. | der gemessene Lauf vom 2026-08-21: bloss `Doeldissen` = 3/3 auf Rang 1-3 | PASS: `python3 -m pytest tests/test_namensfrage_p18.py` -- 5 gruen, und die Zahlen vom Koordinator selbst ueber den Produktivweg nachgefahren. AC1: die natuerlich gestellte Frage liefert 3/3 statt 1/3, der blosse Name unveraendert 3/3, die Sachfrage 0/3 als richtiger Negativfall. AC2 VOLLSTAENDIGKEIT: ueber ALLE Textspalten und ALLE Zeilen geprueft, auch `zurueckgezogen=1` und `sensibel=1` -- keine weiteren Vorkommen. Der Personenbezug steht dabei im `content`-Feld, nicht in Titel oder Zusammenfassung; ohne content-Suche waere die Vollstaendigkeitspruefung selbst unvollstaendig gewesen. Trefferquote des Arbeitsbestands unveraendert 14/35, keine kalibrierte Zahl geaendert. Commit `6ffb96a9` | Betreibergespraech 2026-08-21, `docs/PLAN_NAECHSTE_STUFE_2026-08-21.md` Paragraf 4.1 |
| BDW-P19 | `oberflaechensprache` | SOLL | DECIDED | Nutzersichtbare Texte folgen der Sprache des NUTZERS, nicht der des Hauses. Betreiberwort 2026-08-21: "wenn wir einen englischsprachigen user haben, sollten diese dinge auch auf englisch angezeigt werden". Abzugrenzen von `BDW-P10`: dort ist `sprache` die Sprache des EINTRAGS (gemessen 3573 de / 1609 en), hier die der Anzeige -- zwei verschiedene Groessen. Heute fuehrt die englische Tuer aus `BDW-P14` in ein deutsches Haus: jede Hakenmeldung, jeder Meldertext, jeder Triggerfehler ist deutsch. | Oberflaeche / Barrierefreiheit | `BDW-P19-AC1`: Ein englischsprachiger Nutzer bekommt Sitzungsstart, Melderausgaben und Werkzeugbeschreibungen englisch; die Erhebung des Umfangs liegt vor (`runs/sprachstand_oberflaeche_2026-08-21.json`). `BDW-P19-AC3`: Die Erklaerungen des Hermes-Plugins sind in beiden Sprachen GESCHRIEBEN, nicht maschinell uebersetzt -- Betreiberwort 2026-08-21: "deswegen brauchen wir auch gute mehrsprachige erklaerungen dazu!" (`2f73ee48`). Ein uebersetzter Satz erklaert die deutsche Denkweise auf Englisch; ein geschriebener erklaert die Sache. `BDW-P19-AC2`: Negativtest -- ein Text, auf den ein Waechter per regulaerem Ausdruck prueft, wird durch die Uebersetzung NICHT blind (L-8fce9c), und ein geaenderter Triggertext erreicht auch die gewachsene Datenbank (L-55075a). | die englische Tuer aus BDW-P14 (README/CONTRIBUTING) -- ein fremder Entwickler landet englisch und liest danach deutsch | TEILWEISE: Erhebung liegt vor (`runs/sprachstand_oberflaeche_2026-08-21.json`): 707 nutzersichtbare Textstellen, 60543 Zeichen, davon erreichen 690 wirklich jemanden; keine Uebersetzungsschicht im Repo (Nullbefund ueber zwei Suchwege). **Bauform entschieden in `ADR-033`: jetzt KEINE Schicht.** Ein deutscher Satz laesst sich in einem Jahr genauso uebersetzen wie heute -- er altert nicht und verliert keine Zuordnung. Damit faellt der einzige Grund weg, es vorzuziehen, und es gibt heute keinen englischsprachigen Nutzer. Stattdessen zwei Regeln: jeder NEU geschriebene nutzersichtbare Text entsteht zweisprachig (zuerst die Hermes-Plugin-Erklaerungen, die es noch gar nicht gibt), der Altbestand bleibt deutsch, bis ein solcher Nutzer erscheint -- DAS ist der Ausloeser, nicht ein Datum. **Befund, der die Lage dreht:** der groesste Einzelposten sind die 113 MCP-Werkzeugbeschreibungen mit 21402 Zeichen, und die sind bereits ueberwiegend englisch (48 zu 11) -- die Flaeche, die ein fremdes Modell zuerst sieht, ist es schon. | Betreibergespraech 2026-08-21, `docs/adr/ADR-033-mehrsprachigkeit-neue-texte-zweisprachig-alte-nicht.md` |
| BDW-P20 | `grundbestand` | MUSS | DECIDED | Das Paket liefert KEINEN Wissensbestand mit -- weder fremden noch eigenen. Fremde Kataloge (BSI, NASA LLIS, WCAG) werden beim Einrichten aus der URSPRUNGSQUELLE geholt, damit jeder Nutzer beim Rechteinhaber bezieht statt fremdes Gut weiterverteilt zu bekommen; ohne belegte Quelle wird nicht geraten, sondern `art: keine` eingetragen. Der EIGENE Bestand (gemessen 2026-08-21: 251 Knoten, 769 Lehren mit `freigabe='offen'`, 2,18 MB) wandert nicht mit. Betreiberwort 2026-08-21: "lass mein eigenes zeugs weg, das sollen sich die leute selbst erarbeiten!" -- und das ist keine Sparsamkeit, sondern der Zweck: eine Lehre ohne den eigenen Beleg, aus dem sie stammt, ist in einem Belegsystem eine Behauptung. Ein frischer Bestand ist leer und richtig, nicht leer und unfertig. | Auslieferung / Product Owner | `BDW-P20-AC1`: Ein gebautes Rad enthaelt keine Datenbank und keinen Katalogtext. `BDW-P20-AC2`: `kataloge()` loest keinen Netzzugriff aus; nur `katalog_holen()` tut das, und nur auf Aufruf. | Wheel-Inhalt gegen Dateiliste, Netzzugriff gegen abgeklemmten Socket | PASS (bounded): built wheel and sdist contain no DB, catalog payload, runs or other forbidden source; the sole packaged `docs/CLIENT_BOOTSTRAP_POLICY.json` is an executable policy, not catalog text. Socket-denied `kataloge()` remains local; `katalog_holen('wcag')` returns explicit no-source without guessing. `uv run --with hatchling --with pytest python -m pytest -q tests/test_paketbau.py tests/test_einrichtung.py::test_kataloge_macht_keinen_netzzugriff tests/test_einrichtung.py::test_katalog_holen_ohne_quelle_raet_nicht_sondern_meldet` — 6 passed. | Betreiberentscheidung 2026-08-21; loest die Auslieferungsfrage aus C2 (`docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` §C2) |
| BDW-P21 | `auslieferungsform` | MUSS | DECIDED | Der Kern bleibt **AGPL-3.0-or-later**, der Hermes-Adapter ist **MIT** -- zwei getrennte Pakete, zwei getrennte Lizenzen. Betreiberwort 2026-08-21: "na fuers plugin die MIT lizenz, fuer brainlehr wie gehabt". Der Adapter laeuft deshalb als eigener Prozess und spricht MCP, statt brainlehr zu importieren -- ein Import haette die AGPL ueber die Prozessgrenze getragen und MIT zur Behauptung gemacht (`42c32f7d`). Das eigenstaendige Repository heisst **hermes-brainlehr**, der Anbietername in Hermes bleibt **brainlehr**: der Repo-Name ordnet ein, der Anzeigename ist an `MemoryProvider.name` gebunden und nicht frei (`598d39b3`). Die MIT-Datei schreibt ihre Reichweite aus -- sie deckt dieses Verzeichnis, nicht brainlehr, und "Installing this adapter does not install brainlehr" (`85c84fda`). | Auslieferung / Product Owner | `BDW-P21-AC1`: Der Adapter enthaelt keinen Import aus dem brainlehr-Kern. `BDW-P21-AC2`: Die Lizenzdatei des Adapters nennt ausdruecklich, was sie NICHT deckt. | Zwei Prozesse gegen einen Import, Lizenztext gegen Reichweite | PASS (bounded): separate Hermes checkout `501a8c3` runs `python3 -m pytest -q tests/test_provider.py -m 'not braucht_brainlehr'` — 50 passed, 2 deselected. The suite AST-checks no Brainlehr-core imports and rejects host-specific absolute paths; its MIT LICENSE/NOTICE states scope and non-coverage. | Betreiberentscheidungen 2026-08-21, Commits 42c32f7d, 598d39b3, 85c84fda |
| BDW-P22 | `bauvermeidung` | SOLL | DECIDED | Bevor gebaut wird, sieht brainlehr im eigenen Haus nach und schlaegt vor, es NICHT zu bauen. Betreiberentscheidung 2026-08-21, Knoten `cb2193a8`, woertlich: "user sagt ich haette gerne einen kalender, brainlehr schaut autonom in git, und schlaegt vor nicht selbst bauen unter xyz liegt schon genau das was du willst" (`d2674ac5`). Die Suche geht nach TAETIGKEIT, nicht nach Dateiname -- wer nach dem Namen sucht, findet nur, was er schon kennt. | Gesamtprodukt / Product Owner | `BDW-P22-AC1`: Zu einem Vorhaben, das im Verbund bereits existiert, kommt ein Fundstellen-Vorschlag statt eines Bauplans. `BDW-P22-AC2`: Negativkontrolle -- zu einem Vorhaben, das es NICHT gibt, wird nichts vorgeschlagen; ein Werkzeug, das immer etwas findet, findet nichts. | Fund gegen Fehlanzeige, beide Richtungen | NOT RUN | Betreiberentscheidung 2026-08-21, Knoten `cb2193a8`, Commit `d2674ac5` |
| BDW-E22 | `kreis` | MUSS | DEFERRED | Normen und Lehren sind an Personenkreise bindbar. Der Kreis steht VON ANFANG AN fest -- nachtraegliches Sperren verraet ueber die Trefferzahl, dass es etwas gibt. Erfordert die Trennung von allgemeinem Teil und Begruendung beim ERFASSEN, nicht beim Lesen. | Enterprise Data / Security | `BDW-E22-AC1`: Der fremde Kreis sieht den engen Teil NICHT -- Negativtest; ein Positivtest belegt keine Trennung. `BDW-E22-AC2`: Ein neu angelegter enger Eintrag aendert die Trefferzahl des fremden Kreises nicht. | die vorhandene Sperr-Bauform: ein gesperrter Knoten taucht weder auf noch in children_count (knowledge_mcp_server.py) | TEILWEISE: `python3 -m pytest tests/test_trennung_b3.py` -- 19 gruen, von mir nachgefahren. AC1 PASS (Negativtest: der fremde Kreis sieht den engen Eintrag nicht). AC2 PASS und das ist der eigentliche Inhalt: ein neu angelegter enger Eintrag aendert SIEBEN Zahlen des fremden Kreises nicht -- Trefferzahl, children_count, Kantenliste, knowledge_stats, lesson_query, knowledge_sitzung, knowledge_modell, verglichen ueber das ganze Dict. Bauform von `_NICHT_GESPERRT_SQL` abgeschrieben statt neu erfunden. **OFFEN:** die Trennung von allgemeinem Teil und Begruendung BEIM ERFASSEN -- ein Eintrag ist heute ganz oder gar nicht eng. Commit `c977d844` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-E23 | `je-kreis` | MUSS | DEFERRED | Geltung ist zweiseitig, sobald Kreise existieren: dieselbe Regel kann fuer verschiedene Empfaenger verschieden lang gelten. Eigene Tabelle (Eintrag x Kreis), die Spalte bleibt Vorgabe. | Enterprise Data / Security | `BDW-E23-AC1`: Eine Regel traegt fuer zwei Kreise verschiedene Geltung, und beide werden korrekt ausgeliefert. | der Pruefsatz aus L-6af5ac: 'gilt bis X' -- vollstaendig, oder fehlt ein 'fuer wen'? | PASS: `tests/test_trennung_b3.py`, Tabelle `geltung_je_kreis` (Eintrag x Kreis), die Spalten gilt_ab/gilt_bis bleiben Vorgabe fuer alle ohne eigenen Eintrag. Negativtest in BEIDE Richtungen: eine Geltung fuer Kreis A aendert nichts an dem, was Kreis B sieht, und wer keinen Eintrag hat, bekommt die Spaltenvorgabe. Bei mehreren passenden Kreisen gewinnt der alphabetisch erste -- deterministisch, mit Ausbaupfad; heute traegt kein Ausweis mehr als einen Kreis. Commits `f64e7a12`, `c977d844` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-E24 | `zweiter-faktor` | SOLL | DEFERRED | Der Bestandsschluessel entsteht aus zwei Teilen; der zweite liegt nicht auf demselben Geraet. Ein TOTP-Code genuegt NICHT -- er authentifiziert, er verschluesselt nicht. | Enterprise Security | `BDW-E24-AC1`: Ohne den zweiten Teil ist der Bestand auch mit Vollzugriff auf die Platte unlesbar. `BDW-E24-AC2`: Der Rueckweg bei Verlust ist ausdruecklich entschieden -- er ist zugleich eine Hintertuer. | Sechs Wege im Plan, keiner entschieden | FUTURE (Betreiberentscheidung 2026-08-21: "als future markieren, brauchen wir noch nicht"). Vier der sechs Wege scheiden am genannten Fall (Beschlagnahme) ohnehin aus -- Hardware-Token, Handy und Wechselmedium werden mitgenommen. Uebrig bleiben getippte Passphrase und sperrbarer Netzabruf, und letzterer widerspricht `BDW-P05` (local-first). Faktisch also EINE Frage. Die Vorfrage ist beantwortet und der Plan an dieser Stelle veraltet: er sagt "Startfrequenz noch nicht erhoben", gemessen am `access_log` ueber 17 Tage sind es **Median 9 Sitzungen je Tag, Maximum 15** -- eine Passphrase je Sitzung waere neunmal taeglich, je Tag einmal. Vor einer spaeteren Entscheidung bleiben drei Punkte offen: Rueckweg bei Verlust (er ist zugleich eine Hintertuer), je Start oder je Entschluesselung, und ob er auch die Knotenschluessel aus ADR-031 betrifft. **Wieder faellig**, sobald der Bestand auf einem Geraet liegt, das der Betreiber nicht selbst verwahrt -- der genannte Fall (Beschlagnahme) setzt genau das voraus, und solange alles auf seiner Platte liegt, schuetzt ein zweiter Faktor gegen nichts, was der erste nicht schon abdeckt. | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |
| BDW-E25 | `muster` | SOLL | DEFERRED | Ungewoehnliche Zugriffsmuster werden erkannt und gemeldet. Nicht die MENGE ist das Signal -- gemessen war die auffaelligste Sitzung die harmloseste (4 859 Zugriffe auf 201 Knoten). Es zaehlen Zugriffe je Knoten und Abdeckung. | Audit / Security | `BDW-E25-AC1`: Ein hergestellter Lauf ueber 500 verschiedene Knoten schlaegt an. `BDW-E25-AC2`: Die 4-Prozent-Sitzungen des echten Bestands schlagen NICHT an. | die drei nachgerechneten echten Sitzungen des Bestands (24,2 / 7,9 / 5,1 Zugriffe je Knoten) -- die auffaelligste ist die harmloseste | PASS: `python3 melder/zugriffsmuster.py --selftest` und echter Lauf, Ergebnis `runs/zugriffsmuster_2026-08-21.json`. AC1 PASS: hergestellter Lauf ueber 500 verschiedene Knoten je einmal gelesen schlaegt an (Faktor 1,0, Abdeckung 96 %) -- ohne diese Positivkontrolle waere der Melder von einem kaputten nicht zu unterscheiden, denn im echten Bestand gibt es keinen Positivfall. AC2 PASS: 0 Treffer ueber 97 Sitzungen, naechster Kandidat (Faktor 4,1 bei 4,1 % Abdeckung) auf beiden Achsen unauffaellig, einzeln gelesen. **BEFUND beim Bau:** zaehlt man ALLE Aktionen, meldet der Melder eine reine Massenmigration (754 Schreibzeilen, 380 Knoten, NULL Lesevorgaenge, Faktor 2,04) -- er zaehlt deshalb nur read/browse/search mit gesetztem node_path, Rot-Probe dazu festgehalten. An SessionStart verdrahtet. Commits `7a41e528`, `e0faf586` | Betreiberentscheidungen 2026-08-20, `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md` |

| BDW-P23 | `projektkontext` | MUSS | DECIDED | Jedes neue oder bestehende, mit brainlehr geplante Projekt besitzt genau einen klientneutralen Projektkontext. Ein idempotenter Ensure-Lauf unterscheidet `missing`, `partial`, `stale` und `current`, uebernimmt vorhandenes projektgebundenes Wissen statt es zu duplizieren und initialisiert nur Fehlendes. Das getrackte Manifest enthaelt nur stabile Konfiguration; die automatisch erzeugte Kapsel mit Git-Stand, Codeverteilung und belegten Einstiegspunkten liegt in brainlehr, damit ein Commit sein eigenes Manifest nicht sofort veraltet macht. Rohcode wird nicht kopiert. | Gesamtprodukt / Architektur | `BDW-P23-AC1`: Derselbe Ensure-Lauf wird je Ausgangszustand zweimal ausgefuehrt; der zweite Lauf veraendert weder Projektdatei noch Wissenswurzel. Nach einem neuen Commit bleibt das Manifest bytegleich und nur die Kapsel wird als veraltet erkannt. | n/a -- Zustands- und Bytevergleich, kein menschliches Erzeugnis | PASS: `python3 -m pytest -q tests/test_project_context.py` (11 passed) | Betreiberauftrag 2026-08-25, Knoten `ac76be2e` |
| BDW-P24 | `lazy-context-ladder` | MUSS | DECIDED | Der Klient MUSS Projektkontext stufenweise laden: zuerst projektgebundene Kurzfassungen und gedeckelte Codefundstellen; direkte typisierte Relationen DARF er nur fuer ausgewaehlte relevante Treffer laden; Volltext DARF er nur fuer ausdruecklich ausgewaehlte IDs laden. Er MUSS bei jeder Stufe die erlaubte naechste Wahl erhalten und DARF nie rekursiv den ganzen Ast laden. Rohcode und komplette Symboltabellen werden nicht als Wissen kopiert; nur nachgepruefte, wiederverwendbare Aussagen werden mit Commit und Fundstelle kuratiert. | Retrieval / Token Economy | `BDW-P24-AC1`: Ohne Auswahl liefert der Abruf nur Kurzfassungen, gedeckelte Codefundstellen und eine maschinenlesbare Wahl (`selection_required`, erlaubte IDs/Stufen, Obergrenze); Relation und Volltext werden ohne ausgewaehlte IDs abgewiesen, mit Auswahl auf hoechstens drei IDs begrenzt und Relationsbelege auf 30. | n/a -- Ergebnisform und Obergrenzen, kein menschliches Erzeugnis | PASS: `python3 -m pytest -q tests/test_project_context.py` (11 passed) | Betreiberauftrag 2026-08-25, Knoten `ac76be2e`; Knoten `455c4dd0`, `197425fe` |
| BDW-P25 | `project-tool-registry` | MUSS | DECIDED | Weil jedes Repository anderen Code besitzt, verweist sein Projektkontext auf Faehigkeiten statt globale Dateinamen anzunehmen. Das Register unterscheidet `available` und `planned`: verfuegbare Werkzeuge tragen einen aufrufbaren Befehl und eine Quelle, geplante nur einen Planbezug und werden niemals als aufrufbar ausgegeben. Native Einstiegspunkte aus `pyproject.toml` und `package.json` liegen in der erzeugten Kapsel, damit das getrackte Manifest stabil bleibt; explizite Registrierungen duerfen sie mit `covers`, `edge_types` und Artefaktbezug uebersteuern. Ein Aufgabenabruf liefert nur passende Werkzeugverweise. | Gesamtprodukt / Architektur | `BDW-P25-AC1`: Python- und npm-Einstiegspunkte werden aus ihren nativen Manifesten in die Kapsel uebernommen; ein explizit geplantes Werkzeug bleibt `callable=false`; eine fachfremde Aufgabe erhaelt keinen Werkzeugdump; eine explizite Kennung erzeugt keinen doppelten Verweis. | die nativen Register `pyproject.toml [project.scripts]` und `package.json scripts` | PASS: `python3 -m pytest -q tests/test_project_context.py` (11 passed) | Betreiberauftrag 2026-08-25, Knoten `ac76be2e` |
| BDW-P26 | `versioned-code-impact` | MUSS | DECIDED | Nach jedem verifizierten Code-Commit hinterlegt der Codeschreiber einen versionierten append-only Aenderungsbeleg aus Basis-Commit, Ziel-Commit, geaenderten Dateien, Tests und semantischer Wirkung. Eine Korrektur erzeugt einen neuen Beleg mit `supersedes`, statt den alten zu ueberschreiben. Aus belegten gerichteten Kanten wird die vollstaendige transitive Verbraucherkette bis zu ihrem Ende verfolgt; jeder erreichbare direkte und indirekte Verbraucher wird mit Distanz und Kantenbeleg pruefpflichtig, aber nie automatisch inhaltlich umgeschrieben. Zyklen enden am bereits besuchten Knoten. Die statische Python-Analyse vereinigt Basis- und Zielgraph, damit geloeschte oder umbenannte Provider ihre Verbraucher nicht verlieren; mehrdeutige oder bekannte nicht-statische Importformen werden als begrenzte Abdeckung statt geraten gemeldet. Jeder Beleg traegt Schema- und Analyzerkennung; jede Kante traegt `from`, `to`, `edge_type`, `source_ref`, `observed_commit`, `observed_at` und Evidenz. Import-/Call-/Schema-/Trace-Kanten, historische Ko-Aenderung, explizite Input-Output-Zeit-Vertraege und Vektoraehnlichkeit bleiben unterscheidbare Evidenzarten; insbesondere ist ein Import kein bewiesener Datenfluss und ein Vektortreffer keine Abhaengigkeit. Die vollstaendige Kette wird berechnet, aber stufenweise nach Distanz geladen. Testnamen und Summary sind vom Aufrufer gelieferte Belege, keine vom Server ausgefuehrte Verifikation. | Code Knowledge / Architektur | `BDW-P26-AC1`: Eine dreistufige Python-Importkette macht direkten und indirekten Verbraucher auf Distanz 1 und 2 sichtbar; ein Zyklus terminiert, ein geloeschter Provider behaelt seine Basisverbraucher, ein relativer Import wird belegt und ein mehrdeutiger Modulname erzeugt keine erfundene Kante. Zwei Commits und eine Korrektur erzeugen getrennte Receipts. | der Nullbefund `L-503687`: 35 von 35 heuristisch behaupteten Datenfluessen waren falsch | PASS: `python3 -m pytest -q tests/test_project_context.py` (11 passed) | Betreiberauftrag 2026-08-25; Lehre `L-503687`; `kern/codekanten.py`; `hub/scripts/mycel.py` |
| BDW-P27 | `public-context-export` | MUSS | DECIDED | Verifizierte oeffentliche Selbstbeschreibung, Architektur und Workflow werden ausschliesslich aus einer getrackten repo-relativen Pfad-Allowlist, getrackten repo-relativen Quellen und dem benannten Projekt-Scope aus der SQLite-DB in ein deterministisches Git-Artefakt exportiert. Das Ziel liegt ebenfalls im Repository. Das Artefakt enthaelt nur sichere Inhaltsfelder plus oeffentliche Provenienz (Allowlist, Exporter, `source_git_commit`), niemals Datenbankdatei, Sitzungen, Betreiberweisungen oder lokale Quellen. Fehlende, nicht freigegebene oder gegen ihre Quellartefakte veraltete Knoten brechen den Export ab und hinterlassen kein Artefakt. | Auslieferung / Architektur | `BDW-P27-AC1`: Derselbe DB-Stand und Quellcommit erzeugen bytegleiches JSON; eine externe oder ungetrackte Allowlist/Quelle, ein fehlender oder veralteter Knoten laesst das alte Ziel unveraendert. | n/a -- Bytevergleich und Negativkontrolle, kein menschliches Erzeugnis | PASS: `tests/test_public_context_export.py` (4 passed); live exporter wrote three allowlisted nodes and a second run returned `current`. | Betreiberauftrag 2026-08-25; bestehend `pflege/export_offen.py` |
| BDW-P28 | `public-context-no-private-data` | MUSS-NICHT | DECIDED | Der oeffentliche Kontext-Export darf weder rohe Datenbankfelder noch `source`, `actor`, `session`, `model`, `client`, Zugriffsprotokolle, Betreiberentscheidungen, lokale Pfade, E-Mail-/Schluesselmaterial oder freie nicht-allowgelistete Knoten ausgeben. Eine Freigabe ausserhalb der Allowlist erweitert den Export nicht. Die Musterpruefung erkennt nur bekannte Pfad-, E-Mail- und Schluesselformen und ist KEINE vollstaendige PII-/Secret-Erkennung; die Freigabe des allowgelisteten Inhalts bleibt eine menschliche Entscheidung. | Auslieferung / Privacy | `BDW-P28-AC1`: Ein privater Knoten, ein lokaler Pfad und ein Geheimnis-Muster werden abgewiesen; nur die drei allowgelisteten offenen Knoten erscheinen. | n/a -- Negativtest, kein menschliches Erzeugnis | PASS: `tests/test_public_context_export.py` (4 passed); generated artifact contains only the three allowlisted public nodes. | Betreiberauftrag 2026-08-25; bestehend `pflege/export_offen.py` |
| BDW-P29 | `project-context-hardening` | MUSS | DECIDED | Projektkontext benennt die Grenzen seiner deterministischen Evidenz: Kapsel, Receipt und Export tragen eigene Schema-/Versionskennung; „vollständig“ gilt ausschliesslich für den benannten statischen Analyzer, nie für Laufzeitdatenfluss oder Buildsysteme. Weitere Analyzer, Graph-Caches und Provenienzontologien entstehen erst nach gemessener Abdeckung, Fehlerquote, Laufzeit oder Tokenwirkung. | Gesamtprodukt / Architektur | `BDW-P29-AC1`: Eine Kapsel und ein Receipt tragen Schema/Analyzer; ein Nicht-Python-Commit erzeugt `coverage_gap`, eine bekannte nicht-statische Form eine begrenzte Analyzer-Abdeckung; die Tests belegen die Benennung. | n/a -- deterministische Vertragsprüfung, kein menschliches Erzeugnis | PASS: `python3 -m pytest -q tests/test_project_context.py tests/test_public_context_export.py tests/test_requirements_brainlehr.py` (17 passed). | Zweites Projektkontext-Konsil 2026-08-25, Knoten `a09757aa`; L-39b018; L-503687 |
| BDW-P30 | `separate-code-retrieval` | MUSS | DECIDED | Projektcode DARF neben dem allgemeinen BGE-M3-Wissenskanal einen eigenen lokalen Code-Retrieval-Kanal erhalten; BGE-M3 bleibt der allgemeine Prosa-/Wissenskanal. CodeRank ist ausschließlich ein getrennter, komplementärer Code-Spezialist mit eigenem Vektorraum, eigener Modell-/Kontext-/Chunk-/Quellcommit-Provenienz und projektgebundenem Index; Vektoren oder Kosinuswerte werden nie gemischt oder konkatenziert. Die Antwort darf nur geordnete Trefferlisten rank-only fusionieren. Diese Prüfung ist **deferred next tranche**: H0 = kein Fusionsnutzen; H1 = CodeRank liefert einzigartige relevante Treffer und eine ausschließlich rangbasierte Fusion schlägt auf versiegelten Code-Queries sowohl BGE-only als auch CodeRank-only. Prosa-Routing und -Ranking bleiben BGE-identisch. Gewichtete RRF-/Router-Parameter werden endlich vorab festgelegt und ausschließlich auf einem Dev-Split getuned; genau ein unveränderter, versiegelter Test-Split entscheidet. Der realistische Full-Repo-Lauf trennt Train/Dev/Test, misst Zeit, Latenz und RAM, wiederholt leave-one-repo-out und umfasst DE/EN-Prosa, Code, Signatur, Consumer, Fehler, Impact und No-hit leak-free Matrizen sowie Betriebs-, Index-, Security-, Staleness- und Leak-Gates. Kommentare/Docstrings werden vor jeder Aktivierungs- oder Mandatsentscheidung in einer Ablation `stripped` vs. `comments-only` vs. `combined` geprüft. Bei fehlendem/veraltetem Modell oder Index gilt exakt der bestehende BGE-/Kontext-Fallback. | Retrieval / Code Knowledge | `BDW-P30-AC1`: Der neue Tranchentest weist H0/H1, getrennte Vektorräume, leak-freie Splits/Matrizen, vorab endliche Dev-only-Tuningparameter, versiegelten Einmal-Test, Full-Repo-/Zeit-/RAM-/leave-one-repo-out-Evidenz, Ablation und alle Betriebs-Gates getrennt aus; fehlendes/veraltetes Modell oder Index führt deterministisch exakt zum BGE-Fallback. | versiegelte Code-Queries, realistischer Full-Repo-Train/Dev/Test-Lauf und deterministische Negativtests | PASS baseline unverändert: bestehender lokaler 13-Fall-Lauf BGE-M3 Recall@1 0,80 / MRR 0,8643 in 27,266 s, CodeRankEmbed 0,60 / 0,7007 in 30,349 s; kein Kandidat aktiviert und keine neue Aktivierung behauptet; Bericht: `messungen/code_retrieval_benchmark_2026-08-26.md`. | Betreiberauftrag 2026-08-26; Modellkonsil, Knoten `1a714374`; L-305730; L-2be21f; L-6e0c0f |
| BDW-P31 | `retrieval-modality-matrices` | MUSS | DECIDED | Der eingefrorene Retrievalvergleich umfasst Python, TypeScript, Rust, Swift, Dart/Flutter, Java und Go vollständig; die erweiterte Auswertung ist **deferred next tranche** und nutzt realistischen Full-Repo-Train/Dev/Test-Aufbau mit leave-one-repo-out sowie Zeit-/RAM-Messung. Pro Sprache und Split gelten leak-freie DE/EN-Matrizen für Prosa→Code, Code, Signatur→Implementierung, Code→Consumer, Fehler, Impact und No-hit; dazu eine Prosa→Prosa-Kontrolle, zwei gleichsprachige Hard Negatives je positivem Fall und genau ein unveränderter versiegelter Testlauf. Queries dürfen weder Zielsymbol, Pfad/Datei noch Split-/Case-Varianten enthalten. SQL sowie Shell/YAML/HCL bleiben deklarative bzw. Build-/Abhängigkeits-Fixtures, keine gleichgestellte Embedding-Sprachwertung; C/C++, C#, PHP, Kotlin und Ruby bleiben projektbedingte Erweiterungen. Der Bericht nennt Kandidaten, R@1/R@5/R@10, MRR, Fehlalarme, Modell-/Prefixvertrag, separate Vektorräume, Prosa-BGE-Identität, finite vorab deklarierte Dev-only gewichtete-RRF-/Router-Tuningparameter, Betriebs-Latenz/RAM, Index-/Security-/Staleness-/Leak-Gates und die Kommentare-/Docstrings-Ablation `stripped`/`comments-only`/`combined`. Ein fehlender oder undichter Pflichtgoldset blockiert; BGE-only bleibt die aktuelle zulässige Entscheidung, bis H1 gegen beide Einzelkanäle auf dem versiegelten Test belegt ist. | Retrieval / Code Knowledge | `BDW-P31-AC1`: Alle Pflichtmatrizen und Kontrollen sind pro Split/Repo leak-free und hashgebunden; Dev-Tuning ist endlich und vom Test getrennt, der Test wird genau einmal ausgewertet, leave-one-repo-out und Ablation sind enthalten, und jeder Gate-/Staleness-/Fallback-Befund bleibt sichtbar. | lokale versiegelte Goldsets und Full-Repo-Splits | PASS baseline unverändert: v2 28/28 + Prosa, 140 identifierfreie Fälle, vier Kandidaten je Sprache inkl. gleichdomänigem Code-Hard-Negative; SHA-256 `77d59017125d8cd62a1f041b257121dd141bce0d87885fc599b6f8e0b3424da0`. Router/RRF/CodeRank scheitern mindestens eine Pflicht-Nichtregression; BGE-only bleibt aktiv; Tranchentest deferred, keine Aktivierung behauptet; Bericht: `messungen/code_retrieval_benchmark_2026-08-26.md`. | Betreiberweisung; `dfe4df69`; `L-ff5d75`; `L-0f0b4b` |
| BDW-P32 | `request-local-boundary` | MUSS | DECIDED | Jeder Klient kann einen request-lokalen Modus `auto`, `knowledge`, `code` oder `mixed` und die Phase `plan`, `read`, `edit`, `build`, `test`, `commit` abfragen. Die Priorität ist fest: expliziter Modus > vom Server belegte Aktion (nur ein nichtleerer Staged-Tree) > benannte Operation > `unknown`. `cwd`, Repo, Manifest und Inventar allein ergeben nie `code`. `knowledge` darf weder Git-/Code-Scan, Code-Retrieval, Analyzer noch Commit-Gate auslösen; `mixed` führt Wissen und Code getrennt. Die Antwort bleibt maschinenlesbar und gedeckelt (`mode`, `evidence`, `must`, `may`, `must_not`, `coverage_gaps`, `allowed_next`). Sie speichert weder Prompt, Thinking noch ein dauerhaftes Nutzerprofil. | Client Boundary / Privacy | `BDW-P32-AC1`: Override, Wissen, Code, Mixed und Unknown sind deterministisch; ein vorhandenes Repo ohne Staged-Änderung bleibt `unknown`; Knowledge-only führt keinen Git-Aufruf aus; Antwort und Evidenz sind gedeckelt. | Quelltest gegen reinen Kern ohne DB, Netzwerk oder Modell | PASS: `python3 -m pytest -q tests/test_project_boundary.py` (5 passed) | Betreiberauftrag 2026-08-26; Knoten `ac76be2e`; BSI-Anwenderkatalog `TEST.1.3` @ `12abb438` (Leitlinie, keine Zertifizierung) |
| BDW-P33 | `staged-impact-ack` | MUSS | DECIDED | Ein Projekt aktiviert das Commit-Gate nur durch die explizite Werkzeugregistrierung `project-commit-gate`. Das Gate prüft ausschliesslich Basis-Commit, gestagten Diff und dessen SHA-256-Fingerabdruck; bei einem nichtleeren Staged-Tree blockiert es, bis eine bewusste lokale Bestätigung mit nichtleerem Grund vorliegt. Die append-only Bestätigung bindet Grund, Basis und Staged-Tree-Fingerabdruck; jede Änderung des Staged-Trees macht sie ungültig. Sie liefert genaue nächste Befehle, mutiert beim Prüfen/Ablehnen keine Dateien und behauptet weder eine ausgeführte Prüfung noch Laufzeit-, Build-, Timing- oder Datenflussabdeckung. Solche Evidenz stammt nur aus einem registrierten Test-, Trace- oder Vertragswerkzeug, andernfalls bleibt eine Coverage-Lücke sichtbar. Ein lokaler Hook kann umgangen werden und ist deshalb keine Sicherheitsgrenze; bestehende Hooks werden nicht ersetzt oder umgangen. | Code Knowledge / Change Control | `BDW-P33-AC1`: Opt-in- und Nicht-Opt-in-Projekt, Block, append-only Ack, unveränderte Ablehnung, Ack nach Staged-Änderung ungültig, Nicht-Python-Lücke und genaue Folgeanweisung sind testbar. | isoliertes temporäres Git-Repository, keine DB, kein Modell | PASS: `python3 -m pytest -q tests/test_project_boundary.py` (5 passed) | Betreiberauftrag 2026-08-26; Knoten `125d3731`, `2ae96eda`; BSI-Anwenderkatalog `TEST.1.3` @ `12abb438` (Leitlinie, keine Zertifizierung) |
| BDW-P34 | `impact-visual-projection` | SOLL | DECIDED | Eine optionale menschliche Visualisierung darf nur einen nach Revision gebundenen, typisierten Teilgraphen derselben Impact-Evidenz zeigen: Importdistanz, Basis-/Zielkanten-Diff, Test-/Belegstatus, Timing-/Sequenzverträge und Coverage-Lücken. Sie ist nie eigener Analyzer, Speicher oder Wahrheit; der KI-Klient erhält stattdessen den kleinen maschinenlesbaren Subgraph. README-Flowcharts bleiben stabile Erklärungen, keine Laufzeitansicht. Der vorhandene Metroviz-Quellvertrag wird vor einem Adapter explizit nachgewiesen; ohne zugängliches Schema bleibt nur ein geplanter Werkzeugverweis und eine Coverage-Lücke. | Visualization / Code Knowledge | `BDW-P34-AC1`: Ein späterer Adapter akzeptiert nur Quellrevision plus Receipt/Trace-Referenz und kann keine Kante, Route, Gate, Kollision oder Sackgasse ohne Quellenbeleg darstellen; die fünf Ansichten bleiben getrennt auswählbar. | source-backed Metroviz-Vertrag und vorhandener Receipt, kein zweiter Graph | NOT RUN: Metroviz-Quellschema am erwarteten Pfad nicht zugänglich; kein Adapter oder Renderer gebaut. | Betreiberauftrag 2026-08-26; Knoten `198ac704`, `ac76be2e` |
| BDW-P35 | `typed-impact-graph` | MUSS | DECIDED | Nach einem verifizierten Commit erzeugt `project_change` aus derselben statischen Impact-Evidenz einen tokenarmen, typisierten JSON-Graphen mit Schema, Basis-/Quellrevision, stabilem Inhalts-Hash, geänderten Knoten, Verbraucher-Distanzen, Kantenbelegen, aufrufergelieferten Prüfungen, registrierten Evidenzwerkzeugen und Coverage-Lücken. Eine deterministische Mermaid-Projektion liest ausschliesslich diesen Graphen; sie speichert keinen zweiten Zustand und enthält keinen Rohcode. `metroviz` bleibt bis zu einem nachgewiesenen Adaptervertrag `planned`. | Code Knowledge / Visualization | `BDW-P35-AC1`: Zwei Läufe über dieselben Revisionen erzeugen denselben Hash und dieselbe Mermaid-Ausgabe; Receipt und Visualisierungsreferenz nennen Schema, Revision und Hash; eine Importkette zeigt Verbraucher und Kante. | temporäres Git-Repository, statischer Python-Analyzer | PASS: `python3 -m pytest -q tests/test_project_boundary.py tests/test_project_context.py` (22 passed) | Betreiberauftrag 2026-08-26; Knoten `198ac704`, `125d3731`; `kern/project_context.py` |
| BDW-P36 | `oss-analysis-research-gate` | MUSS-NICHT | DECIDED | Vor einer neuen Analyseabhängigkeit wird anhand offizieller Primärquellen Lizenz, Aktivität, lokale/offline Ausführung, Sprachabdeckung, Revision/Inkrement, Kantenprovenienz, Laufzeitbeleg, Austauschformat, Datenschutz und Installationsfußabdruck entschieden. Ohne gemessenen Bedarf bleibt der Kern dependency-frei. Heute: Mermaid als reine Projektion wiederverwendbar; Semgrep nur optionaler lokaler Verifier; tree-sitter/SCIP/Kythe als spätere Adapter; OpenTelemetry nur für zusammengefasste Laufzeitbelege; Glean, Joern/CPG, Cytoscape und CodeQL-CLI nicht als Standard. | Architecture / Supply Chain | `BDW-P36-AC1`: Die ADR nennt offizielle URLs, Lizenzen und die Entscheidung `reuse now|adapter later|reject`; keine neue Runtime-Dependency oder Modellartefakt wird dadurch eingeführt. | Primärquellen und `pyproject.toml` | PASS: `docs/AI_DECISIONS.md` + `python3 -m pytest -q tests/test_paketbau.py` (1 skipped: Paketbau optional) | Betreiberauftrag 2026-08-26; offizielle Upstreams SCIP, Kythe, Glean, Joern, Semgrep, tree-sitter, OpenTelemetry, Mermaid, Graphviz, Cytoscape, CodeQL |
| BDW-P37 | `agent-reuse-recommendation` | MUSS | DECIDED | Brainlehr ergänzt seinen temporären technischen Checkpoint um Rolle/Fähigkeit, Quellrevision, verwendete Knoten-/Lehren-IDs, offene Gates und Terminalzustand und liefert eine reine Empfehlung `reuse_followup`, `refresh_delta` oder `fresh_agent`. Gleiches Projekt, gleicher Aufgabenfingerprint, kompatible Rolle und gleiche Revision erlauben Follow-up mit direkten Nachbarn; neue Revision verlangt Diff plus direkte Nachbarn. Themen-/Projektwechsel, inkompatible Rolle, Kontext-Sättigung, Terminalzustand oder unabhängige Prüfung verlangen einen frischen Agenten. Der Host entscheidet, startet oder reused Agenten; Brainlehr speichert/liest weder Prompt, Antwort, Transcript noch Hidden Thinking und startet keine Agenten. | Orchestration / Token Economy | `BDW-P37-AC1`: Gleicher Kontext ergibt `reuse_followup`, Revisionswechsel `refresh_delta`, Unabhängigkeit/Sättigung/Rollenwechsel `fresh_agent`; Ergebnis enthält nur technische Felder und kein Auto-Spawn. | deterministische Checkpoint-Funktion ohne Host-API | PASS: `python3 -m pytest -q tests/test_session_checkpoint.py` (7 passed) | Betreiberauftrag 2026-08-26; Knoten `cb24f119`, Lehre `L-2d6598`, `ac76be2e` |
| BDW-P38 | `ephemeral-analysis-overlay` | MUSS | DECIDED | Code-/Mixed-Arbeit verwendet einen unveränderlichen Basisgraphen am Commit und ein ausdrücklich flüchtiges Arbeitsbaum-Overlay `(repo, base_commit, tree_hash, analyzer_version)`. Bei Task-Start wird nur Modus/Kapsel geladen; vor der ersten Code-Aktion Kontext; nach abgeschlossenen Edit-Tool-Batches latest-wins/debounced ausschließlich im Speicher; vor Build/Test nur bei geändertem Hash; vor Commit nur der Staged-Tree plus Gate; nach Commit genau ein append-only `project_change`-Receipt. Trace-/Timing-Beleg gilt nur für verifizierten Test/Lauf mit Commit- und Tree-Bindung. Origin/Korrelations-ID/Idempotenz, Abbruch veralteter Läufe, begrenzte Wiederholungen und sichtbare Circuit-Breaker-Lücke verhindern Schleifen. Generierte Brainlehr-Artefakte triggern keine Quellanalyse, der Analyzer schreibt nie Code, und Knowledge-only umgeht die Schleife vollständig. | Code Knowledge / Change Control | `BDW-P38-AC1`: Schnelle Edit-Serie startet exakt eine Analyse und null dauerhafte Writes; gleicher Hash ist `current`; veraltetes Ergebnis wird verworfen; generiertes Artefakt rekursiert nicht; Staged/Unstaged sind getrennt; Post-Commit fordert genau einen Receipt; Timing ohne Revision/Hash bleibt Lücke. | reiner In-Memory-Kern im temporären Git-Repository, kein Daemon/DB | PASS: `python3 -m pytest -q tests/test_project_boundary.py` (8 passed) | Betreiberauftrag 2026-08-26; Knoten `cb24f119`, `125d3731`; Lehre `L-305730` |
| BDW-P39 | `client-neutral-lazy-bootstrap` | MUSS | DECIDED | Ein kleines, immer geladenes T0-Bootstrap kommt aus genau einem getrackten, versionierten Policy-Bundle und ist für Claude, Hermes und ChatGPT/Codex nur durch dünne Lifecycle-Adapter verschieden. Der gemeinsame MCP-Vertrag enthält `mode`, Phase `plan|read|edit|build|test|commit`, Evidenz, `must/may/must_not`, Coverage-Lücken, Folgeaktionen sowie Policy-Schema/-Hash/-Quellrevision. T1 lädt Modus/Kapsel/Zusammenfassungen, T2 ausgewählte Typrelationen/Impact, T3 ausgewählten Volltext/Trace, T4 Staged-Gate. Nur das Policy-Bundle darf maschinenlesbares `must` bestimmen; Recall, Code, Kommentare und Tool-Evidenz sind untrusted DATA. Kein Bootstrap speichert Chat, Prompt, Antwort, Thinking, PII oder Geheimnisse. Hosted ChatGPT-Systemprompts sind nicht ersetzbar; Hooks/AGENTS/Skills/MCP und Hermes-Provider haben unterschiedliche Durchsetzungsgrenzen. | Client Integration / Prompt Security | `BDW-P39-AC1`: Generator erzeugt drei Hash-identische Contract-Semantiken mit gemessenen Zeichen-/Schätztoken-Caps; ein manipuliertes Artefakt ist stale; Injection-Text kann keine Operation oder Policy-Felder erweitern; Knowledge-only lädt kein Code-Bundle. | getracktes JSON-Policy-Bundle und deterministischer Generator | PASS: `python3 -m pytest -q tests/test_public_prompt_templates.py tests/test_project_boundary.py` (11 passed) | Betreiberauftrag 2026-08-26; Knoten `ee154d10`, `ac76be2e`, `125d3731` |
| BDW-P40 | `evidence-graph-v2` | MUSS | DECIDED | Brainlehr ist eine revisionsgebundene Code-Evidenzplattform: jeder unveränderliche Snapshot trägt Repo, Revision, Schema, Inhalts-Hash, Knoten, Kanten und ein typisiertes Evidence-Envelope mit Quelle, Stärke, Zeitpunkt, Analyzer-Version und Status. Konflikte bleiben explizit; eine neue Evidenz überschreibt nie still eine alte. Unvollständige, veraltete oder widersprüchliche Daten werden als Gap/Conflict statt als Fakt geliefert. | Evidence Graph | `BDW-P40-AC1`: Schema-Upgrade, Hash, Revision, Evidence-Provenienz und Konfliktresultat sind deterministisch testbar. | fixture graph, kein Netzwerk/DB | NOT RUN: P40–P49 Tranche | Betreiberweisung 2026-08-26; `609c5314` |
| BDW-P41 | `adapter-abi-registry` | MUSS | DECIDED | Alle Analyzer registrieren eine kleine ABI mit Input-Revision, Timeout, Evidence-Klasse, normalisiertem Graph-Fragment, Messwerten und sichtbaren Gaps. Sie schreiben weder Brainlehr noch Quellcode. Timeout, fehlendes Binärprogramm oder Parsefehler ergeben `coverage_gap`, nie einen stillen Fallback. | Analyzer Boundary | `BDW-P41-AC1`: Registry validiert Fragment/Provenienz und behandelt unavailable/timeout deterministisch. | isolierte Adapter-Fakes | NOT RUN: P40–P49 Tranche | Betreiberweisung 2026-08-26 |
| BDW-P42 | `real-static-symbol-channels` | MUSS | DECIDED | tree-sitter, SCIP und Semgrep liefern getrennte Syntax-/Symbol-/Regel-Evidenz mit Werkzeugversion, Lizenz, Input-Revision und Abdeckungsgrenze. Tree-sitter prüft mindestens Python und eine nicht-Python-Fixture; SCIP importiert Def/Ref/Call aus einem real erzeugten Index; Semgrep bleibt Regel-/Taint-Evidenz, keine Abhängigkeitskante. | Static Analysis | `BDW-P42-AC1`: reale lokale Runner/Fixtures ergeben normalisierte Kanten und klare Unterschiede zur AST-Basis. | lokale Caches unter `/Volumes/daten` | NOT RUN: real runner/test/fixture are currently untracked; manifest keeps all three channels planned/non-callable until tracked source, command and regression exist. | Betreiberweisung `609c5314`; SCIP Apache-2.0, tree-sitter MIT, Semgrep LGPL-2.1 |
| BDW-P43 | `cpg-runtime-channels` | MUSS | DECIDED | Joern/CPG und OTLP liefern getrennte Control/Call/Dataflow- beziehungsweise Laufzeit-/Dauer-/Kausalitäts-Evidenz. CPG-Kanten werden nie als Runtime-Beweis umetikettiert; Trace-Evidenz braucht Test/Build/Lauf, Commit, Tree-Hash und datensparsame Referenz statt Rohpayload. | Dynamic Evidence | `BDW-P43-AC1`: CPG-/OTLP-Importer, Revision/Timing-Bindung und fehlende Tool-/Payload-Gap sind testbar. | lokale Fixtures/optionale Runner | PASS (bounded): offizielles Joern 4.0.612 parse/exportiert die C-Fixture lokal, `tool/joern_cpg.py` normalisiert revisionsgebundene CPG-Kanten ohne Rohcode, nutzt macOS `sandbox-exec` `deny network*` und einen kalibrierten 1-GiB-Prozessgruppen-RSS-Wächter; OTLP/Metroviz prüfen Graph-Hash sowie per-span Revision/Tree/Timing und lehnen Payload ab. JVM-Dateisystemzugriff bleibt Gap. | Betreiberweisung `609c5314`; Joern Apache-2.0, OpenTelemetry Apache-2.0 |
| BDW-P44 | `reconciliation-impact-selection` | MUSS | DECIDED | Normalisierte Evidenz wird pro Revision vereinigt, verglichen und nur durch eine verifizierte Aktion befördert. Direkte und indirekte Verbraucher, Tests, Verträge und registrierte Build-/Schema-Kanten ergeben eine konservative Auswahlliste; jeder unbekannte Kanal verbreitert Prüfung oder bleibt als Gap sichtbar. Einbettungen sind nur Retrieval-Kandidaten, nie Kanten. | Reconciliation | `BDW-P44-AC1`: statisch/runtime abweichende Evidenz bleibt Conflict; transitive Auswahl und unknown-coverage sind deterministisch. | Graph fixtures | NOT RUN: P40–P49 Tranche | Betreiberweisung; Pants/Nx-Prinzip |
| BDW-P45 | `immutable-overlay-concurrency` | MUSS | DECIDED | P38 wird auf Graph-v2 fortgeführt: Base-Snapshot, Working-Overlay und Staged-Snapshot bleiben getrennt; latest-wins, Korrelation, Idempotenz, Rerun-Limit und kein Rekursionspfad sichern gleichzeitige Änderungen. | Incremental Analysis | `BDW-P45-AC1`: stale/rapid/generated/staged cases bleiben ohne falschen Receipt grün. | In-memory test | NOT RUN: P40–P49 Tranche | Betreiberweisung; `cb24f119` |
| BDW-P46 | `client-mode-policy` | MUSS | DECIDED | P32/P39 bleiben clientneutral: Knowledge-only ruft keinen Codekanal; Code/Mixed lädt nur stufenweise denselben Graph; Policy-Hash, Injection-Schutz und Adapter-Parität sind messbar. | Client Boundary | `BDW-P46-AC1`: Claude/Hermes/Codex artefacts teilen Contract/Hash; Knowledge-only zählt null Analyzer-Aufrufe. | generated templates | NOT RUN: P40–P49 Tranche | Betreiberweisung; `ee154d10` |
| BDW-P47 | `human-graph-projections` | MUSS | DECIDED | Mermaid, Metroviz-Vertrag und eine lokale/offline Cytoscape-Ansicht projizieren ausschließlich denselben aktuellen typisierten Graphen mit Schema, Revision und Hash. Ansichten für Distanz, Kanten-Delta, Timing, Test-Evidenz und Gaps sind auswählbar; stale Artefakte werden abgelehnt. | Visualization | `BDW-P47-AC1`: deterministische Projektion/Current-Gate und menschlich geprüfte Wireframe-/HTML-Fixture. | local licensed asset | PASS: `python3 -m pytest -q tests/test_project_boundary.py`; Mermaid/Cytoscape/Metroviz lesen denselben Graph, CLI-Test prüft Revision, Hash und gebundene OTLP-Route. | Betreiberweisung `609c5314`; Cytoscape MIT |
| BDW-P48 | `reuse-checkpoint-graph` | MUSS | DECIDED | Ein Registry-Eintrag trägt stabile Agent-/Task-ID, Rolle/Fähigkeiten, Projekt-/Topic-Fingerprint, kompatible Revision/Tree-Hash, Checkpoint, Terminalzustand, offene Gates und Freshness. Kompatible Fortsetzung reaktiviert denselben Terra (hier `/root/project_context_consilium`), kein neuer Terra-Agent; nur unabhängige Prüfung wird fresh. | Orchestration | `BDW-P48-AC1`: kompatibel→same follow-up, Revision→Delta, unabhängig→fresh; kein Transcript/Auto-Spawn. | checkpoint tests | NOT RUN: P40–P49 Tranche | Betreiberweisung; `L-2d6598` |
| BDW-P49 | `evidence-evaluation-privacy` | MUSS | DECIDED | Jede Tranche misst Laufzeit/Timeout, Cache-/Freshness, Gaps, Konflikte, False-positive-/Override-Last und Token-Grenzen; sie klassifiziert generated/vendor und speichert keine Rohquellen, Chats, Thinking oder Tracepayloads. BSI-Kontrollen sind Anforderungen/Leitlinien, keine Software und keine Zertifizierung. | Evaluation / Privacy | `BDW-P49-AC1`: degraded-tool, vendor/generated, privacy and metric regression tests bestehen. | deterministic fixtures | NOT RUN: P40–P49 Tranche | Betreiberweisung; BSI `TEST.1.3` Leitlinie |
| BDW-P50 | `optional-codeql-sarif` | MUSS | DECIDED | CodeQL ist ein optionaler, nie still aktivierter SARIF-Kanal. Ein Lauf braucht expliziten Nutzerauftrag und einen Beleg: öffentliche OSI-Quelle oder deklarierte bezahlte GitHub-Code-Security-Berechtigung; Basis, Version, Terms-URL und Zeitpunkt werden als Evidenz protokolliert. Ohne Beleg ergibt ein privates Projekt eine Lizenz-Coverage-Lücke und wird nicht gescannt. Normalpfade laden nie Netzwerk/CLI; ein separater expliziter Helper darf nur offizielle GitHub-Release-Artefakte nach Annahme und Prüfsumme beziehen. | Optional Security Analysis | `BDW-P50-AC1`: SARIF-Normalisierung, private Ablehnung, erlaubte Berechtigungen und no-network default sind testbar. | synthetisches SARIF, kein CodeQL-Download | NOT RUN: P50 Tranche | Betreiberweisung `041d6170`; CodeQL CLI terms |
| BDW-P51 | `dependency-evidence-lifecycle` | MUSS | DECIDED | Dependencies werden als revisionsgebundene Paket-/Version-/Lizenz-/Advisory-Knoten mit direkten/transitiven Kanten, Scope, Registry-/SBOM-/Lockfile-Herkunft und Current→Candidate-Delta geführt. Ein geplanter Check lädt Metadaten ohne Auto-Install; Diff, Verbraucher, Tests/Verträge/Traces, Release-/API-/Security-Beleg und Policy priorisieren eine isolierte Änderung. Der Mensch entscheidet Zeitpunkt/Override; LLM aktualisiert nie automatisch. | Supply Chain | `BDW-P51-AC1`: lokale Fixtures beweisen Lock/SBOM/Delta/Scope, no-network default, Policy und Dependency-Delta-Projektion. | manifest/lock fixtures | NOT RUN: P51 Tranche | Betreiberweisung `609c5314`; BSI Supply-chain Leitlinie |
| BDW-P52 | `supply-chain-release-identity` | MUSS | DECIDED | Locks, SBOM, Hash/Signatur, Advisory, Tool-/Buildversion, Artefaktprovenienz und Rollback sind revisionsgebundene Evidenz; kein Auto-Update. | Supply chain | `BDW-P52-AC1`: lokale Fixture und no-network gate. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P53 | `compatibility-deployment-identity` | MUSS | DECIDED | Schema/API/Event/Deploy-Identität, Kompatibilitätsfenster, Canary und Rollback werden explizit belegt. | Release | `BDW-P53-AC1`: incompatible migration is a gap. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P54 | `coverage-provenance-gaps` | MUSS | DECIDED | Dynamic/plugin/reflection/vendor/generated/IaC/mobile sowie CI/local/flaky provenance werden belegt oder als Gap ausgegeben. | Coverage | `BDW-P54-AC1`: unmodelled input cannot be current. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P55 | `local-actor-project-boundary` | MUSS | DECIDED | Lokale Actor-/Project-Grenze gilt jetzt; Remote-Tenant/Auth bleibt harter Vorab-Gate. | Authorization | `BDW-P55-AC1`: cross-project denial. | local tests | NOT RUN | Audit `53572c72` |
| BDW-P56 | `ack-content-authenticity` | MUSS | DECIDED | Ack bindet Basis, vollständigen Staged-/relevanten Untracked-Contenthash, Actor/Grund und TOCTOU-Invalidierung; kein Caller-Boolean ist Vertrauen. | Change control | `BDW-P56-AC1`: forged/stale ack denied. | git fixture | NOT RUN | Audit `53572c72` |
| BDW-P57 | `analyzer-sandbox-redaction` | MUSS | DECIDED | Analyzer haben Allowlist, Ressourcen-/Netz-/Output-Grenzen, Cache-Herkunft und Redaction; Ausfall ist Gap. | Execution | `BDW-P57-AC1`: secret/raw output rejected. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P58 | `trace-sanitization-lifecycle` | MUSS | DECIDED | OTel nutzt Sampling, Clock-/Revisionbindung, Redaction, Retention und Erasure; keine Rohpayloads. | Runtime privacy | `BDW-P58-AC1`: unsafe span denied. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P59 | `injection-restart-resilience` | MUSS | DECIDED | Injection kann Policy/Tool nicht befördern; Correlation/Idempotenz überlebt Restart oder meldet Gap. | Orchestration | `BDW-P59-AC1`: injected/restarted work fails closed. | tests | NOT RUN | Audit `53572c72` |
| BDW-P60 | `graph-durability-erasure` | MUSS | DECIDED | Graph-Schema-Migration, GC, Backup/Restore, Korruption und Löschung sind explizite Vorgänge. | Evidence lifecycle | `BDW-P60-AC1`: restore/corrupt/delete behavior. | fixtures | NOT RUN | Audit `53572c72` |
| BDW-P61 | `safe-accessible-projections` | MUSS | DECIDED | Cytoscape/Mermaid/Metroviz haben CSP/Escaping/A11y/Größenfilter/Current-Gate. | Visualization | `BDW-P61-AC1`: unsafe/stale/huge input rejected. | HTML fixtures | NOT RUN | Audit `53572c72` |
| BDW-P62 | `graph-vector-separation` | MUSS | DECIDED | Maschinen-Graph/Analyzer-/Trace-Evidenz wird getrennt, revisionsgebunden und ohne normalen Prosa-Vektor persistiert; nur eine kurze menschliche Zusammenfassung darf BGE-M3 nutzen. Code-Vektoren tragen Projekt, Revision/Tree-Hash, Graph-/Symbol-Ref, Modalität, Sprache, Content-Hash, Modell/Version/Dimension, Zeit und Status; sie sind Retrieval-Hinweise, nie Kanten. Stale oder unvollständige Revisionen werden abgelehnt; Analyzer schreiben nie direkt DB. CodeRank bleibt nach dem leak-free P31-Rerun wegen Pflichtregression inaktiv; RRF und Vektorkonkatenation bleiben aus. | Evidence / Retrieval | `BDW-P62-AC1`: Roh-JSON/Trace fehlt aus `knowledge_embeddings`; Graph überlebt Vektorfehler; stale/unvollständiger Vektor und analyzer-direct-write werden abgelehnt. | DB fixtures | PASS: `python3 -m pytest -q tests/test_project_context.py tests/test_code_retrieval_router.py`; Trennung, vollständige Metadaten- und stale rejection gedeckt. | Betreiberweisung; `dfe4df69`; `L-0f0b4b`; `57b5fa07` |

## Fähigkeiten und Priorität

| ID | Auswahl | Normtyp | Status | Vollständige normative Entscheidung | Scope / Owner | Akzeptanzkriterium | Latte | Produktgate | Quelle |
|---|---|---|---|---|---|---|---|---|---|
| BDW-F01 | `must` | MUSS | DECIDED | Episodisches Gedächtnis gehört in die erste Version. | Memory Core / Product | `BDW-F01-AC1`: Episoden bestehen Schreib-, Zeit-, Quellen-, Recall-, Korrektur- und Löschtest. | n/a -- Episodenpruefungen, kein Erzeugnis | PASS: `python3 migrationen/migrate_gedaechtnisart.py --selftest` — Spalte `gedaechtnisart` auf `lessons_learned`, Vorgabe `episodisch`, CHECK-Trigger (bi/bu), unzulässiger Wert abgewiesen, idempotent. Live gemessen 2026-08-19: 1112 Zeilen `episodisch`. GRENZE: die 70-%-Messung stammt aus einer Stichprobe über `gattung='arbeitsbestand'`, nicht über den vollen Bestand | „MUSS erste Version“; RQ-003 |
| BDW-F02 | `must` | MUSS | DECIDED | Semantische Claims gehören in die erste Version. | Memory Core / Product | `BDW-F02-AC1`: Jeder verwendete Claim weist Quelle, Ableitung, Status und Korrekturpfad aus. | die Herkunftsangabe im Quelldokument, gegen die Anzeige im Abruf | PASS: `python3 migrationen/migrate_gedaechtnisart.py --selftest` — Spalte `gedaechtnisart` auf `knowledge_nodes`, Vorgabe `semantisch`, CHECK-Trigger (bi/bu), unzulässiger Wert abgewiesen. Live gemessen 2026-08-19: 5191 Zeilen `semantisch`. GRENZE: davon 84 % Fremdkorpora (germanquad, nasa-llis) ohne eigene Messung — der Vorgabewert gilt dort ungeprüft | „MUSS erste Version“; RQ-003/004 |
| BDW-F03 | `must` | MUSS | DECIDED | Prozedurales Gedächtnis gehört in die erste Version. | Memory Core / Product | `BDW-F03-AC1`: Prozeduren sind von Fakten/Episoden getrennt und besitzen eigenen Freigabe- und Widerrufstest. | n/a -- Trennung und Freigabe, kein Erzeugnis | PASS: `python3 -m pytest tests/test_lehre_prozedur_migration.py` - 5 gruen. `gedaechtnisart='prozedural'` fuer die 80 `pattern`-Lehren mit Schrittfolge; live gemessen: 80 prozedural, 1036 episodisch, und ALLE 80 sind vom Typ `pattern` (selbst nachgezaehlt: 0 fremde Art). Die Zuordnung folgt der ART, nicht den Schritten - 166 Nicht-pattern-Lehren tragen ebenfalls eine Schrittfolge und blieben unangetastet. **Eigener Widerruf belegt, beide nebeneinander in EINEM Testfall:** eine widerrufene Prozedur bleibt im Wortlaut lesbar und traegt ihre Grenze, ein zurueckgezogener Fakt wird geleert. **GRENZE:** die Planzahl war 85, gemessen wurden 80 - die 85 stammten aus einer ungesicherten Vorabmessung mit anderer Heuristik | „MUSS erste Version“; RQ-003 |
| BDW-F04 | `must` | MUSS | DECIDED | Zeit-, Konflikt-, Confidence- und Decay-Governance gehört in die erste Version. | Governance Core / Product | `BDW-F04-AC1`: Ersetzen, Ablauf, Widerruf und Gleichrangkonflikt erscheinen ohne stilles Überschreiben. | der Wortlaut der Abloesung in `ADR-030`, gegen die Anzeige des abgeloesten Treffers | PASS: `pytest tests/test_widerruf_archiv.py` grün | „MUSS erste Version“; RQ-004/005 |
| BDW-F05 | `must` | MUSS | DECIDED | Retrieval-to-Action-Evaluation gehört in die erste Version. | Evaluation / Quality | `BDW-F05-AC1`: Ein Benchmark belegt korrekte Toolwahl/-parameter und bessere Wirkung gegenüber einer No-Memory-Baseline. | derselbe Lauf OHNE Speicher (No-Memory-Baseline) -- die Latte steht bereits im AC | TEILWEISE: `python3 messungen/wirkung_ohne_gedaechtnis.py` — n=35, mit Speicher 8, ohne Speicher 0. Positiv- und Negativkontrolle bestanden. **Gemessen ist die Zufuhr, nicht die Wirkung**: ohne `anthropic`-Paket und Schlüssel lief kein Modell, also ist belegt, was das Modell gesehen hätte — nicht, was es daraus gemacht hätte. | „MUSS erste Version“; RQ-005 |
| BDW-F06 | `must` | MUSS | DECIDED | Manipulationsgeschützte Auditkette gehört in die erste Version. | Audit Core / Security | `BDW-F06-AC1`: Nachträgliche Logänderung wird erkannt, legitime Korrektur bleibt append-only. | n/a -- Append-only-Pruefung, Byte-Vergleich | PASS: `pytest tests/test_auditkette.py` grün | „MUSS erste Version“; RQ-006 |
| BDW-F07 | `must` | MUSS | DECIDED | Portable Wissenspakete gehören in die erste Version. | Portability / Product | `BDW-F07-AC1`: Export/Import erhält IDs, Zeit, Provenienz, Rechte und Konflikte atomar. | n/a -- Export/Import erhaelt Felder, Byte-Vergleich | PASS: `pytest tests/test_interface_kompat_katalog.py` grün (17/17 INT-Gates) | „MUSS erste Version“; RQ-006/012 |
| BDW-F08 | `must` | MUSS | DECIDED | Dokument- und Quellenconnectoren gehören in die erste Version, ohne Brainlehr zum DMS zu machen. | Connectors / Product | `BDW-F08-AC1`: Connector speichert Referenz, Prüfsumme, Provenienz und Claims statt unkontrollierter Dokumentkopie. | n/a -- Connector speichert Referenz statt Dokument, kein Erzeugnis | PASS: `python3 -m pytest tests/test_connector_register.py` — 10 grün. `kern/connector_register.py` legt je Aufnahme Referenz, Prüfsumme (sha256), Provenienz (Quelle/Zeit/wer) und Claims ab; der Inhalt selbst wird NICHT kopiert (eigener Testfall, von mir nachgeprüft: Klartext in keiner Ablage). Gegenprobe: dieselbe Referenz mit anderem Inhalt wirft `AbweichendePruefsumme`, der alte Eintrag bleibt unverändert | „MUSS erste Version“; RQ-012 |
| BDW-F09 | `must` | MUSS | DECIDED | Privacy-Projektion für externe Modelle gehört in die erste Version. | Projection / Security | `BDW-F09-AC1`: Nur zweckgebundene freigegebene Felder verlassen die lokale Grenze; Negativfälle leaken nichts. | n/a -- Feldfreigabe, Ergebnis ist verlaesst/verlaesst nicht | PASS: `pytest tests/test_enigma_zweckprojektion_mehrere_paare.py` grün | „MUSS erste Version“; RQ-011/012 |
| BDW-F10 | `must` | MUSS | DEFERRED | Mehrbenutzer-Administration gehört in die erste Version. | Enterprise Admin / IAM | `BDW-F10-AC1`: Subjekte, Gruppen, Rechte, Deprovisioning und Audit bestehen einen geschlossenen E2E-Pfad. | — vertagt (`BDW-C03`) | DEFERRED: Unternehmensanmeldung (IdP/SSO/SCIM) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „MUSS erste Version“; RQ-007/008/013 |
| BDW-F11 | `should` | SOLL | DEFERRED | Föderation und Multi-Agent-Synchronisation folgen später. | Federation / Research | `BDW-F11-AC1`: Vor Produktisierung bestehen deterministische Konvergenz-, Widerrufs-, Rechte- und Offline-Replikationstests. | — vertagt (`BDW-C03`) | DEFERRED: Föderation — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „SOLL später“; RQ-006/014/015 |

## Benutzer- und Admin-Konfiguration

| ID | Auswahl | Normtyp | Status | Vollständige normative Entscheidung | Scope / Owner | Akzeptanzkriterium | Latte | Produktgate | Quelle |
|---|---|---|---|---|---|---|---|---|---|
| BDW-U01 | `org-ceiling` | MUSS | DECIDED | Die Organisation setzt die maximale Vertrauensreglerstufe; Nutzer dürfen sie absenken. | Trust / Org Admin | `BDW-U01-AC1`: Nutzer kann keine höhere Stufe als die Org-Grenze setzen; Absenkung wirkt sofort. | — vertagt (`BDW-C03`) | DEFERRED: aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Gemessen 2026-08-19: es gibt keine Org-Ebene — keine Tabelle mit org/mandant/tenant, keine Rollen-/Ausweistabelle im Bestand. Eine Obergrenze, die niemand setzen kann, ist nicht prüfbar, sondern erfindbar. | „Org setzt Maximalstufe, Nutzer darf absenken“; RQ-016 |
| BDW-U02 | `receipt` | MUSS | DEFERRED | Jede Antwort zeigt einen kompakten Quellen-/Policy-Beleg der Recall-Aktivität. | UX / Product | `BDW-U02-AC1`: Beleg nennt verwendete/abgelehnte Quellen und Policywirkung ohne geschützten Inhalt. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Kompakter Quellen-/Policy-Beleg je Antwort“; RQ-005/016 |
| BDW-U03 | `separate` | MUSS | DEFERRED | Persönliche und Firmen-Sicht bleiben getrennt; Übergang verlangt explizite Freigabe. | Views / Security | `BDW-U03-AC1`: Privater Testinhalt erscheint ohne Freigabe nie in Firmenabruf oder -export. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Getrennte Sichten mit expliziter Freigabe“; RQ-008/016 |
| BDW-U04 | `allowlist` | MUSS | DECIDED | Die Organisation führt eine Connector-Allowlist; Nutzer wählen nur daraus. | Connectors / Org Admin | `BDW-U04-AC1`: Nicht gelisteter Connector kann weder aktiviert noch durch Direktaufruf genutzt werden. | n/a -- nicht gelisteter Connector startet nicht | PASS: `python3 -m pytest tests/test_connector_register.py` — nicht gelisteter Connector scheitert AUCH beim Direktaufruf (`ConnectorNichtErlaubt`), nicht nur beim Aktivieren; beide Wege eigener Testfall, von mir nachgefahren. Gegenprobe: gelisteter Connector läuft unverändert durch. **GRENZE:** die Allowlist lebt heute im Objekt (`_erlaubt: set[str]`) und überlebt keinen Neustart — für eine Org-weite, gepflegte Liste fehlt die Ablage | „Org-Allowlist, Nutzer wählt daraus“; RQ-011/016 |
| BDW-U05 | `policy` | MUSS | DEFERRED | Freigabe und Export folgen Policy plus risikobasierter Genehmigung. | Export / Security | `BDW-U05-AC1`: Zweck, Ziel, Klassifikation und gegebenenfalls Genehmigung werden vor Export geprüft und auditiert. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Policy plus risikobasierte Genehmigung“; RQ-010/011/016 |
| BDW-U06 | `risk` | Profil | DECIDED | Benachrichtigungen sind risikobasiert und nutzen konfigurierbare Nutzerkanäle innerhalb der Org-Policy. | Notifications / Operations | `BDW-U06-AC1`: Konflikt, Ablauf, Quellenlücke und Policy-Denial werden nach Risiko geroutet, ohne Inhaltsleck. | die Meldungstexte der bestehenden Oberflaeche (Regeln in `~/.claude/skills/design-waechter/ui_guard.py`) | PASS: `python3 -m pytest tests/test_risikoeinstufung.py` - 23 gruen. Vier Ereignisarten eingestuft, Routing nach Stufe belegt (konflikt -> eskalation, ablauf -> protokoll, von mir nachgefahren). **Ohne Inhaltsleck:** `melde()` nimmt nur eine Objektkennung entgegen, nie Inhalt - der Testtext STRENG-GEHEIM-... kommt in keiner Senke vor, selbst geprueft | „Risikobasiert mit Nutzerkanälen“; RQ-011/016 |
| BDW-U07 | `approved` | MUSS | DEFERRED | Die Organisation führt eine Modell-/Provider-Allowlist; Nutzer wählen nur daraus. | Models / Org Admin | `BDW-U07-AC1`: Nicht freigegebener Provider erhält auch per Direktaufruf keine Daten. | — vertagt (`BDW-C03`) | DEFERRED: Unternehmensanmeldung (IdP/SSO/SCIM) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Org-Allowlist, Nutzer wählt“; RQ-009/016 |
| BDW-U08 | `org-wins` | MUSS | DEFERRED | Bei Konflikt gewinnt die Organisationsgrenze sichtbar vor der Nutzerwahl. | Policy / Security | `BDW-U08-AC1`: Ein Konfliktfall wird verweigert, erklärt und auditiert; Nutzerwahl überschreibt die Org-Policy nie. | — vertagt (`BDW-C03`) | DEFERRED: Ausgangskontrolle (DLP/SIEM/Export) — aktiviert mit dem ersten realen Mehrbenutzer-Piloten (`BDW-C03`). Betreiberentscheidung 2026-08-18, Knoten `9d77ad16`. | „Org-Grenze gewinnt sichtbar“; RQ-008/016 |

## Betreiberentscheidungen 2026-08-21 (Gesamtbau-Auftrag)

Eingetragen 2026-08-21T00:40:00+0200, **vor** der Umsetzung und nicht danach.
Umsetzungsplan: `docs/PLAN_GESAMTBAU_2026-08-21.md`.

| Gegenstand | Entscheidung | Wirkt auf |
|---|---|---|
| Vorgabewert der Mandanten-Achse | `lokal` -- Betreiberwort: "mandant Vorgabe lokal" | `BDW-P09` |
| Bindende Reihenfolge | B1 zuerst, danach erst B2, B3, C. Begruendung ausdruecklich **nicht** die Datenmenge: 5 232 Alteintraegen laesst sich rueckwirkend keine Zuordnung geben, die sie nie hatten | `BDW-P09`, `BDW-E22`, `BDW-E23`, `BDW-P11` |
| Nicht gebaut: zweiter Faktor | unentschieden, sechs Wege liegen vor. Kein Bau ohne Entscheidung | `BDW-E24` |
| Nicht gebaut: an einen echten Piloten gebunden | ein gegen einen erfundenen IdP gebauter Anschluss prueft den Pruefstand, nicht die Sache | `BDW-E01`, `BDW-E04`, `BDW-E05` |
| Nicht gebaut: Datenregionen | ohne zweiten Standort gibt es nichts zu begrenzen | `BDW-E19` |
| Oeffentlicher Export wird nicht gepusht | GitHub-Konto wegen einer Abrechnungsfrage gesperrt -- kein Codefehler | -- |

**Vom Assistenten entschieden, weil der Betreiber Reihenfolge und Zuschnitt
freigegeben hat:** die Profilnamen `einzelplatz` / `unternehmen` (offene Frage
am Ende von `docs/PLAN_BETRIEBSPROFILE_2026-08-20.md`). Deutsch wie der uebrige
Bestand -- `corporate` waere das einzige englische Wort in einem Schema, dessen
Felder `freigabe`, `geltung` und `herkunft` heissen. Widerspruch jederzeit
moeglich, die Namen stehen an genau einer Stelle.

## Untergeordnete Teilkataloge

- `docs/REQUIREMENTS_PROMPT_INVARIANZ.md`
- `docs/REQUIREMENTS_SESSION_CHECKPOINT.md`
- `docs/REQUIREMENTS_INTERFACE_KOMPAT.md` (Interface- und Kompatibilitätsvertrag zu `BDW-F07`)

Ihre lokalen IDs bleiben für Umsetzung und Regression erhalten. Bei
Widerspruch gilt dieser Root; der Konflikt wird hier an der betroffenen
`BDW-*`-ID sichtbar gemacht, nicht im Teilkatalog still aufgelöst.
| BDW-P67 | `release-readiness-priority` | MUSS | DECIDED | Vor P2-Visualisierung ist der gesamte Brainlehr-Kern P1: alle deterministischen CPU-/DB-/Systemtests laufen isoliert; jede Live-, Hardware-, Corpus-, Fremdprojekt- und Performance-Ausnahme ist einzeln als bestanden, echter Fehler oder bewusste externe Grenze klassifiziert. Der 51er Befund absoluter lokaler Pfade wird an der Quelle bereinigt oder als blockierende Datenschutzschuld belegt, nie blind allowlistet. Nur ein rein agent-owned, geprüftes Diff wird commitbar; Push geht ausschließlich an den privaten Origin-Branch. | Release / Privacy | `BDW-P67-AC1`: isolierter Testledger, Live-Ausnahmen-Audit, reproduzierbarer Pfad-Hygiene-Befund, exakter Stage-Stat und privater Pushbeleg. | fresh temp DB; no GPU/model/perf during external load | TEILWEISE PASS: the 51 historical path fields were rewritten through the MCP update path; dry ledger `56803224…ed1f28b5`, post-scan 0, live gate 4 passed, external SQLite backup verified. Historical audit-chain break id 1227 predates this write; full release ledger/stage/push remain open. | Betreiberweisung 2026-08-26; `57b5fa07`; `3eb63f93`; `L-29d16a`; `L-82ca8d` |
| BDW-P68 | `audit-chain-utc-repair` | MUSS | DECIDED | Die 11.119 gegen eine benannte Vor-UTC-Sicherung nachgewiesenen `access_log`-Zeilen, deren einzige Änderung die UTC-Zeitmarke ist, erhalten über den bestehenden Erklärungsweg eine hashgebundene, append-only Erklärung. Der Lauf ist resumierbar, vor jedem Write re-read-idempotent und nebenläufig sicher; er schreibt weder `access_log` noch Kettenhashes um. Die 19 Zeit+Modell- und 31 nach der Sicherung entstandenen Brüche bleiben getrennte, offene Forensikfälle. | Audit / Release | `BDW-P68-AC1`: hash-only Kandidatenmanifest aus aktueller DB + Sicherung, Copy-DB-Apply, zweite Writer-Gegenprobe, Wiederanlauf ohne Duplikat und vollständiger Kettenlint nach Live-Apply. | named SQLite backup; no model/GPU | PASS (bounded live): 11,118/11,118 append-only explanations through the existing contract; current and copy candidates 0; preflight manifest `a270e899…`, final manifest `8415e9f9…`; integrity and SQLite-backup restore pass; node/lesson/vector/access/trigger/FK invariants unchanged. The independent scan leaves only the separately classified 19 timestamp+model and 31 post-backup rows. | Betreiberweisung 2026-08-26; `57b5fa07`; `L-ed58b8`; `L-82ca8d` |
| BDW-P69 | `typed-relation-endpoints` | MUSS | DECIDED | `knowledge_relations` speichert typisierte Endpunkte `node|lesson|file`, statt fachlich gültige Lehren- und Dateireferenzen gegen zwei Node-Pfad-FKs zu erzwingen. Node-Endpunkte müssen auf `knowledge_nodes.path`, Lesson-Endpunkte auf `lessons_learned.id` zeigen; Datei-Endpunkte sind ausschließlich relative, nicht aufsteigende Referenzen. Der Bestand wird copy-first, atomar und ID-/Inhalt-erhaltend migriert. Kein globales `foreign_keys=OFF`; unbekannte Endpunktformen werden abgewiesen. | Data integrity / Relation graph | `BDW-P69-AC1`: Kopie sichert/rekonstruiert alle 10.546 Kanten; 10.358 node→node, 145 node→lesson und 43 lesson→file werden strikt validiert; `foreign_key_check=0`; Trigger lehnen fehlende Node/Lesson, absolute/aufsteigende File-Referenzen und unbekannte Kinds ab; SQLite-Trigger-SQL ist installiert geprüft. | named SQLite backup; fresh schema fixture; no model/GPU | PASS (bounded live): copy and production preserve 10.546 IDs/content; classification 10.358/145/43; `foreign_key_check` 231→0, integrity and restore pass; installed endpoint triggers and idempotent rerun verified; focused writers/tests 14 passed. | Betreiberweisung 2026-08-26; `L-ed58b8`; `L-82ca8d` |
| BDW-P70 | `legacy-chain-cutover-anchor` | MUSS | DECIDED | Die 50 offen gebliebenen historischen Kettenlücken bleiben sichtbar und werden nie als erklärt umetikettiert. Ein neuer, lokaler kryptographischer Cutover-Anker bindet hash-only Manifest der Altlasten, Vor-UTC-Sicherung, aktuelle DB und den neuen Segment-Genesis; er nutzt das bestehende Ankerverfahren im trockenen/lokalen Modus und keine neue Abhängigkeit oder Netzoperation. Der Lint meldet getrennt `historical_unresolved=50` und prüft das Segment ab dem Anker zu 100 %. | Audit / Release | `BDW-P70-AC1`: Copy-first Manifest/Anchor, reproduzierbare Hashes, neuer Genesis und lückenloser neuer Segmentlint; keine Änderung an historischen access_log-/chain_explanations-Zeilen. | named backups; local dry anchor; no model/GPU/network | PASS (bounded live): manifest `f7e607b8…7654d2f` binds exactly 19 `model+timestamp` plus 31 `missing_pre`; copy append stays healthy and copy tamper fails. Live anchor `40f35ebd…` reports profile/tail/manifest/anchor match and healthy current segment; integrity/FK and SQLite-backup restore pass. The local digest detects accidental DB-row alteration, but is not an external TSA proof. | Betreiberweisung 2026-08-26; `L-ed58b8`; `L-82ca8d` |
| BDW-P74 | `client-lifecycle-lazy-contract` | MUSS | DECIDED | Der gleiche maschinenlesbare Lazy-Load- und `edit_batch_complete`-Vertrag gilt für Codex, Claude, Hermes und einen normalen IDE-/File-save-Emitter. Adapter übersetzen nur native Lebenszyklusereignisse und dürfen keine dauerhaften Nutzerprofile, Prompts, Chat- oder Thinking-Daten speichern. `knowledge` startet weder Git-Discovery, Overlay, Analyzer noch Commit-Gate; `code`/`mixed` erhalten ausschließlich die angeforderten Kontextstufen. Der reale Hermes-Agent bleibt neben Brainlehr aktiv: der **externe Brainlehr-Provider** kann im foreground begrenzten, zurechenbaren Recall und nur bei opt-in Mitschrift Capture liefern; Cron und Oneshot sind für diesen Provider write-denied; Background-Review und delegierte Subagents verwenden `skip_memory=True`. Diese Grenze beschreibt nicht Hermes' Built-in Memory, das unter eigener Hermes-Policy aktiv bleiben kann. Empty/timeout/error bleiben sichtbar; wiederholte Manager/Retry erzeugen keine duplizierten dauerhaften Brainlehr-Writes. **Abgrenzung:** `BDW-P32/P39` definieren Request-Mode und Bootstrap-Policy, jedoch kein clientneutrales Edit-/Save-Ereignis oder Paritätstest für alle vier Emitter. | Client boundary / Privacy | `BDW-P74-AC1`: dieselben Ereignis-Fixtures erzeugen für alle Adapter identisches Mode-/Envelope-/Next-Probe-Ergebnis; knowledge führt null Codeoperationen aus; ein IDE-Save ist debounced, nicht zeilenweise; Adapter enthalten keine Prompt-/Transcript-Senke. `BDW-P74-AC2`: ein isolierter Lauf gegen reale Hermes-`MemoryManager`-/Provider-Klassen mit Fake-Transport deckt foreground sowie die tatsächliche Cron-/Oneshot-/Background-/Subagent-Konstruktion ab, trennt Built-in von Brainlehr-Beigabe, macht empty/timeout/error sichtbar und beweist ohne Prompt-/Transcript-/Rohcode-Speicher null duplicate durable Brainlehr writes bei Manager-/Retry-Wiederholung. | generated policy fixtures + isolated real Hermes/adapter E2E | PASS (bounded, real E2E): `tests/test_hermes_real_boundary.py` gegen den Host-Hermes-`MemoryManager`/`BrainlehrProvider` mit Fake-Transport belegt primary foreground genau einen Recall (`1`), während `cron`, `subagent`, `oneshot`, `background_review`, `unknown` und leerer Kontext null Writes auslösen; Built-in und Brainlehr bleiben getrennt, `empty`/`timeout`/`error` sichtbar. `/Volumes/daten/Begod2026/hermes-brainlehr/tests/test_provider.py` lief auf Python 3.11.15, 3.12.13 und 3.13.3 jeweils `50 passed, 2 skipped`; keine Secrets, Prompts/Transkripte/Rohcode oder temporäre Produktiv-DB werden gespeichert. Hermes wurde nach Commit `ebe2fe8b` (21:53:11) neu gestartet; laufender Host-Dienst PID 92182 startete 21:56:57, MCP-Kindprozess PID 92469 21:56:59. | Betreiberweisung 2026-08-26; `5d0c5cd4`; `ac76be2e`; `7a8a38a8`; `L-f3f358` |
| BDW-P99 | `ai-comment-policy` | MUSS | DECIDED | AI-Kommentare sind standardmäßig `NONE`. Falls maschinelle Kommentare ausdrücklich zugelassen werden, sind ausschließlich streng validierte `brainlehr:link`-Verweise und eine enge Maschinen-/Rechts-Allowlist erlaubt; vorhandene menschliche Kommentare bleiben unverändert erhalten. Freier generierter Kommentartext ist kein zulässiger Beleg. | AI provenance / Code review | `BDW-P99-AC1`: Fixture trennt `NONE`, gültigen `brainlehr:link`, ungültigen/freien Kommentar und vorhandenen menschlichen Kommentar; Allowlist und Erhalt menschlicher Kommentare bleiben sichtbar. | validator- und preservation-fixture | PASS (bounded supported AI workflow): `ai_comment_policy` and `ai_edit_gate` fail closed; `tool/ai_project_boundary.py` validates manifest/registered revision-bound anchors and receipt before unchanged acknowledgement. The generated bootstrap requires that route; generic human commits remain unchanged. Focused policy/gate/wrapper tests and both archives verify the boundary. Raw host/editor writes cannot be distinguished from human edits and remain outside this opt-in seam. | Betreiberweisung 2026-08-26; `2e05ada4` |
| BDW-P100 | `registry-anchor-lineage-resolver` | MUSS | DECIDED | Registry- und Anchor-Validierung bindet stabile IDs an Revision, Vertrag und lokalen Digest. Ein Lineage-Resolver lädt Relationen/Volltext ausschließlich lazy, budgetiert und für ausgewählte stabile IDs; Budgetende, Stale-Anchor, fehlende Registry und unbekannte Kante bleiben sichtbare Gaps statt eager Vollgraph oder erfundener Verbindung. | Evidence registry / lineage | `BDW-P100-AC1`: gültiger und stale Anchor, unbekannte Registry-ID, Budgetüberschreitung und ausgewählte Lazy-Auflösung liefern deterministische bounded Ergebnisse ohne globale Graphmutation. | isolated registry/anchor fixture | PASS (bounded): stdlib `kern/anchor_registry.py` validates stable IDs/revisions/contracts and deterministic local SHA-256 bindings. Copy-on-write registration has no global graph mutation; selected-only resolution caps budget at 3 and exposes `unknown_registry_id`, `stale_anchor`, `unknown_edge` and `budget_exhausted`. `python3 -m pytest -q tests/test_anchor_registry.py` — 4 passed; py_compile and packaged detached snapshot verified. | Betreiberweisung 2026-08-26; `ac76be2e` |
| BDW-P101 | `immutable-merkle-lineage-joins` | MUSS | DECIDED | Daten- und Wirkungs-Lineage werden in unveränderlichen Git-like Merkle-DAGs mit stabilen IDs und lokalem Binding-Digest verbunden. Append-only Joins bewahren unabhängige Knoten/Kanten, erzeugen bei Änderungen keinen globalen Churn und lassen keine stillen Lücken oder Umschreibungen unbeteiligter Historie zu. | Data/effect lineage / integrity | `BDW-P101-AC1`: unabhängige lokale Änderungen erzeugen nur neue gebundene DAG-Knoten/Edges; stabile IDs, Digest, unveränderte Nachbarn sowie erkannte Gap/Conflict-Fälle bleiben prüfbar. | immutable DAG join fixture | PASS (bounded): stdlib immutable DAG records revision-bound canonical SHA-256 local bindings. Copy-on-write append and multi-parent join preserve independent nodes; same-ID/different-digest conflicts raise, missing parents remain gaps and no unrelated node is rehashed. `python3 -m pytest -q tests/test_lineage_dag.py tests/test_anchor_registry.py` — 8 passed; py_compile and package snapshot verified. | Betreiberweisung 2026-08-26; `ac76be2e` |
| BDW-P102 | `rationale-index-failure-lifecycle` | MUSS | DECIDED | Rationale, Retrieval-Index, Failure-Taxonomie und Responsibility-Matrix bleiben getrennte Artefakte mit expliziten Prerequisites und Revisionen. Bindings enthalten Symbol-/Code-Hash; stale und tombstoned Einträge bleiben sichtbar, werden nicht als aktuell ausgegeben und durchlaufen einen definierten Lifecycle statt stiller Löschung. | Governance / lifecycle | `BDW-P102-AC1`: fehlendes Prerequisite, Revision-/Symbol-/Code-Hash-Mismatch, stale und tombstone werden getrennt klassifiziert; Responsibility-Matrix und Rationale bleiben ohne Indexmutation nachvollziehbar. | lifecycle and responsibility fixture | PASS (bounded): immutable copy-on-write lifecycle index separates rationale/responsibility references, validates registered anchors and emits distinct prerequisite, revision, symbol, code-hash, stale, tombstone and unknown-anchor gaps without mutation. `python3 -m pytest -q tests/test_rationale_index_lifecycle.py tests/test_lineage_dag.py tests/test_anchor_registry.py` — 14 passed; package snapshot verified. | Betreiberweisung 2026-08-26; `ac76be2e` |
| BDW-P103 | `sealed-annotation-coderank-ablation` | MUSS | DECIDED | Die Code-Retrieval-Entscheidung verlangt eine versiegelte, leak-freie Ablation von BGE-Annotation, CodeRank-Roh-Rang und ausschließlich rank-only RRF. Alle Operator-Arme und Prerequisites (getrennte Vektorräume, Prosa-Baseline, endliche Dev-Parameter, Full-Repo-Train/Dev/Test, Zeit/RAM, leave-one-repo-out, DE/EN-Matrizen, Ablation und Betriebs-/Index-/Security-/Staleness-Gates) werden getrennt ausgewiesen; keine Aktivierung folgt aus Annotation oder RRF allein. | Retrieval / sealed evaluation | `BDW-P103-AC1`: ein unveränderter, identifierfreier, leak-freier Versuchsplan weist jeden Arm, jedes Prerequisite, Ablation und Negativgate aus; fehlender Arm/Leak/Stale/fehlende Baseline blockiert die Entscheidung. | sealed retrieval benchmark | FAIL (Seal v1): raw report `3e0a79fd…`, collector `9a442472…`; CodeRank/BGE 28-matrix scoring ran offline but lacks executable sealed train/dev/test-once, LORO, prose control, ablations and fallback evidence. `H0/H1` is UNDECIDED; BGE-only stays active. | Betreiberweisung 2026-08-26; `2e05ada4`; `L-b32c2a` |
| BDW-P104 | `forbid-freeform-ai-comments` | MUSS-NICHT | DECIDED | AI darf keine Freiform-Kommentare, erfundenen LLM-IDs, selbstbeweisenden Akzeptanzbehauptungen, Secrets, Roh-Prompts, Transkripte oder unverifizierte Quellen in normative Kataloge, Evidenz oder Exporte schreiben. Normative IDs und Produktstatus stammen ausschließlich aus validierten, revisiongebundenen Quellen; fehlende Evidenz bleibt Gap/NOT RUN. | Security / provenance | `BDW-P104-AC1`: Negativfixture weist Freiformtext, erfundene ID, Self-proof, Secret-, Prompt- und Transcript-Feld zurück; ein gültiger enger Link bleibt ohne Rohinhalt zulässig. | sealed negative policy fixture | PASS (bounded supported AI workflow): strict manifest fields reject freeform, invented/stale anchors, self-proof and secret/evidence fields; a registered narrow link yields only a receipt hash. Invalid wrapper validation never delegates to acknowledgement. Raw host/editor writes remain an explicit out-of-seam gap, not a false global enforcement claim. | Betreiberweisung 2026-08-26; `2e05ada4` |
