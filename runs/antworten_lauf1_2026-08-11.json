{"A|OHNE": "showDialog(\n  context: context,\n  builder: (BuildContext context) {\n    return AlertDialog(\n      title: Text('Fahrt beenden?'),\n      actions: [\n        TextButton(\n          onPressed: () => Navigator.of(context).pop(false),\n          child: Text('Nein'),\n        ),\n        TextButton(\n          onPressed: () => Navigator.of(context).pop(true),\n          child: Text('Ja'),\n        ),\n      ],\n    );\n  },\n).then((result) {\n  if (result == true) {\n    // Fahrt beenden\n  }\n});", "A|MIT": "showDialog(\n  context: context,\n  builder: (BuildContext context) => ActionScreen(\n    title: 'Fahrt beenden?',\n    expandPrimaryAction: true,\n    actions: [\n      ActionButton(\n        label: 'Nein',\n        onPressed: () => Navigator.of(context).pop(false),\n      ),\n      ActionButton(\n        label: 'Ja',\n        onPressed: () => Navigator.of(context).pop(true),\n        isPrimary: true,\n      ),\n    ],\n  ),\n).then((result) {\n  if (result == true) {\n    // Fahrt beenden\n  }\n});", "B|OHNE": "flutter test", "B|MIT": "flutter test --dart-define=DEBUG_STATE_API=true", "C|OHNE": "kubectl get pods -n default", "C|MIT": "kubectl get pods -n default"}