# STAND brainlehr — 2026-08-16T14:37:48+0200
**Erledigt (Linie Kanalgüte, dieses Fenster):** K1 Schritt 2 ist verdrahtet und gemessen (`fuse_semantic_led()` läuft im Produktivweg, 37/40 statt 34/40, Endplätze aus dem Bedeutungskanal 68/585 statt 4/585). Relevanzlage ebenfalls (`kern/relevanzlage.py`, drei Maße bei 76–80 %, deshalb Kennzeichnung statt Filter). Einbettungsvarianten **entschieden**: V2 Anfrageumschrift gewinnt mit top5 11/35 gegen 4/35 der Ausgangslage, V1 nur 6/35, V3 vorher ausgeschieden (Knoten `1076f161`, Plan `docs/PLAN_EINBETTUNGSVARIANTEN_2026-08-16.md`).
**Falle bei jeder Variantenzahl:** V1s Gegenprobe reproduziert die Ausgangslage NICHT (top50 13→16, Median 79→56, ohne dass eine Variante wirkt). Der ehrliche Maßstab für V1 ist deshalb die eigene Gegenprobe, nicht Stufe 0 — statt +2/+5 bleiben +2/+2. Ursache: gespeicherte Einbettungen sind schlechter als frisch gerechnete (`L-1fe22b`).
**Nächstes (Kanalgüte) — KORRIGIERT 14:45:** Die Empfehlung „erst Einbettungshygiene" ist **zurückgezogen, gemessen und widerlegt**: 128 Einträge neu gerechnet, 0 veraltete Prüfsummen, Stufe 0 danach unverändert bei 4/35 · 13 · 79. Drei Ursachen geprüft, drei ausgeschieden (Pfad im Text · Prüfsummen · fehlendes `resolution`); zusätzlich ausgeschieden: gespeicherte gegen frisch gerechnete Vektoren (Kosinus 1,0000) und Fanout-Zeilen. **Offen und reproduziert:** ein frisch aus dem Volltext gerechneter Zielvektor rankt im Median 57 statt 79 — Ursache unerklärt, und solange sie offen ist, trägt jede Variantenzahl einen nicht zuordenbaren Anteil, auch V2s 11/35. Nächster Schritt ist die Ursachensuche, nicht Einbau.
**Bestandsbefund (eigenständig):** `lesson_text()` in `kern/build_embeddings.py` setzt Zuordnung + `description` + `root_cause` + `prevention` zusammen — **`resolution` fehlt**. Bei 925 von 954 Lehren gefüllt, im Schnitt 362 Zeichen, im Bedeutungskanal nicht auffindbar. Hebt die Suchgüte messbar nicht, auffindbar sein sollte es trotzdem.
**Ablaufpflicht ausgerollt:** 28 Repos gemessen (nicht 14 — der alte Knoten war überholt, berichtigt als `a55d2338`), davon 21 bereits über den geteilten hub-Hook versorgt, 5 nachgezogen (`openlehr_legacy`, `setfunk`, `wpdrop`, `sigmaforge`, `openlehr_einzelunternehmer`). 27 grün, **rot ist nur `buckeberg`** mit einem echten Befund (`0c1df3cf`) — bewusst stehen gelassen, gehört der dortigen Sitzung.
**Zwei Fallen am Wächter, beide behoben, beide waren stille Freisprüche:** (1) `@{u}..HEAD` ohne Upstream ist die leere Menge → meldete Erfolg, ohne je einen Commit zu sehen; fällt jetzt auf HEAD. (2) Erfolgsmeldung ohne Zahl → „0 geprüft“ sah aus wie „alle sauber“; nennt jetzt die Größe der geprüften Menge. Eigener Stichtag je Repo über die Datei `.ablaufpflicht-seit` im geprüften Repo.
**Rang-1-Direktive `b5604a62` implementiert:** zweite Regel im PreToolUse-Haken auf `Agent` — meldet, wenn ein Auftrag eine Bestandsangabe setzt, ohne dem Agenten das Widerspruchsrecht zu geben. An 543 echten Aufträgen gemessen: 126 (23 %) hätten gemeldet, davon 116 wegen einer genannten Datei. Kein Fehlalarm, sondern der Befund — die Hausregel verlangt den Satz seit Wochen. Plan `docs/PLAN_ERINNERUNG_KEINE_QUELLE_2026-08-16.md`, Umsetzungsknoten `8eb50bf8`.
**Neu bindend Rang 2 (`049b1690`):** Dateilinks im Chat ABSOLUT — relative werden gegen das Arbeitsverzeichnis aufgelöst, ein Stop-Wächter blockt sie. Gilt nur für Chat-Links; Pfade in Repo-Dateien bleiben relativ.
**Offen (Linie Kanalgüte, anderes Fenster):** K1 Schritt 2 — `kern/embeddings.py::fuse_semantic_led()` liegt seit dem 2026-08-12 fertig und **unverdrahtet**; Verdrahtung ist der nächste Schritt und darf NUR über die Messstufe `echt` gemessen werden. Dazu die Relevanzschwelle an derselben Stelle. Weiter: `96` (`melder/schemastand.py` ohne Auslöser) · `H12` Blaupause · Prüfer am Haltepunkt gegen nackte Zahlen · vier Werkzeuge vom 2026-08-15 unverdrahtet (`abrufwirkung`, `agentendauer`, `planstatus`, `okkultation_richter`).
**Nächstes (Kanalgüte):** Der Sockel ist gemessen (`7e3933ae`, Knoten `5c6dd28d`): er sättigt in **116 von 117** Anfragen, der Bedeutungskanal steuert **4 von 585** Endplätzen bei, der Stichwortkanal liefert im Median **4740 von 4933** Knoten. Der zweite Suchkanal ist im Produktivweg faktisch abgeschaltet — die Falschmeldequote 40/40 hängt an derselben Ursache.
**Offen (Linie openlehr_einzelunternehmer, dieses Fenster):** B1–B4 und B6 fertig, **B5 blockiert** (fremd gehaltene Datei). Domänen-Repo `/Volumes/daten/Begod2026/openlehr_einzelunternehmer`: 847 Commits, geerbter Bestand läuft (**347 grün, 3 rot**), Register 1 belegt / 1 unbelegt. Protokoll mit allen Rückschlägen: `docs/FORTSCHRITT_OPENLEHR_EINZELUNTERNEHMER_2026-08-16.md`. Vertagt mit Grund: der Umbau der 50 Dateien mit `begod/`-Verweisen gehört an die einzelne Übernahme, nicht in einen Sammellauf.
**Achtung bei jeder Suchgüte-Zahl:** Alle Zahlen vor dem 2026-08-16 wurden über einen **sockellosen** Pfad erhoben, den das System nie ausführt. Echt gegen sockellos, derselbe Lauf: 34/40 gegen 39/40, einsprachig **0/35** gegen 4/35, Leitfall trifft **nicht** gegen trifft.
**Achtung beim Schnitt:** `openlehr_legacy` ist NICHT das openlehr-Repo, sondern das 3lehr-Monorepo (13 Apps, 1566 Commits, 318 MB `.git`); openlehr liegt darin als `apps/openlehr`. „Mit Historie" heißt Teilbaum-Schnitt per `git-filter-repo`, nicht Klon. Vorarbeit liegt fertig in `openlehr_legacy/docs/openlehr/schnittgrenze_2026-08-14.md` (`L-1c5e26`).
**Wartet auf dich:** Push (**5** Commits vor `origin/brainlehr/b4-ausweis`, gemessen `git log @{upstream}..HEAD` — die frühere Zahl 28 stammte aus einer anderen Zählweise) · Eintrittsweg markus-lehr.de · 22 GB Sicherungen löschen? · Domänen-Repo bei GitHub? · **B5 (`DienstAufsicht.swift`, fremd gehalten ohne Stopp-Ereignis)** · **nativer Zeichner für den Domänen-Bildschirm** · **Domänen-Repo nach GitHub? (Außenwirkung, nicht meine Entscheidung)** · G5 · Linie E (`20`/`23`/`29`/`31`) · Rang von `3c524455` von Hand setzen: der Wächter lässt Rang 1 nur mit menschlichem Entscheider zu, `knowledge_add` hat dafür keinen Eingang.
**Erledigt heute:** Name der Steuerdomäne entschieden (`openlehr_einzelunternehmer`, `806132da`) · Historienfrage entschieden (`3c524455`, wörtlich „Mit Historie.") — ADR-013 bleibt unverändert in Kraft, `73f8a1c0` ebenso, sie reden über verschiedene Gegenstände.
**Nicht vergessen:** `82`/`83`/`87` sind Phantomkennungen, keine Aufgaben (`L-58d434`). · Zwei Einstellungsdateien, wer eine liest misst falsch (`L-ca836f`). · 30 fremde uncommittete Dateien im Arbeitsbereich — vor dem Committen Register prüfen. · Pfadliste beim Commit IMMER hinter `-m`, neue Dateien vorher einzeln `git add` (`L-5e40a7`, 3×, eskaliert).
