# STAND brainlehr — 2026-08-10T12:30:00+0200
**Zuerst lesen:** `docs/adr/ADR-003-erstanlage-gleicht-dem-betrieb.md`, dann `docs/PLAN_WURZELORDNUNG_2026-08-10.md` §5.
**Alles lokal, NICHTS gepusht.** Privat (`brainlehr-privat`): `12f6097`, `b8d1f09`, `49bff30` + CLA. Oeffentlich (`_brainlehr_open` → `github.com/3lehr/brainlehr`): `455d45d`, `8c926ee`, `884afa3`. Beide Baeume sind wortgleich in `CONTRIBUTING.md`, `schema.sql`, `knowledge_mcp_server.py`, `kern/schema_nachzug.py`.
**CLA gilt** (Fassung 2026-08-10, Betreiberentscheidung): der Entwurfsvorbehalt machte ihn wirkungslos. Hinweis auf die fehlende anwaltliche Pruefung bleibt, aber als Information VOR der Zustimmung. Neu: Fassungsangabe in der Zustimmungszeile, spaetere Fassungen wirken nicht rueckwirkend.
**Lizenzsorge im alten STAND war ueber das falsche Repo:** `origin` des Arbeitsbaums ist `brainlehr-privat` (privat). Das oeffentliche `3lehr/brainlehr` traegt LICENSE, NOTICE, LICENSE_FAQ.md und CONTRIBUTING.md auf `main`. Niemand klont Quelltext ohne Lizenz.
**Erstanlage gleicht wieder dem Betrieb (ADR-003).** `knowledge_versions` samt zwei Triggern aus dem Betrieb entfernt (Zaehler ohne Leser, abgeloest durch `knowledge_fassungen`); `schema_migrations` und die vier `access_log.tokens_*` in `schema.sql` aufgenommen. Belegt am echten Klon: `rm knowledge.db && schnellstart.py` → Tabellenvergleich in beide Richtungen leer.
**`kern/schema_nachzug.py` ersetzt vierzehn Handfunktionen** (`_ensure_<spalte>_column`): Soll-Spalten kommen aus `schema.sql`, ergaenzt wird vor allen Trigger-Anlegern, mit WAL-Checkpoint und Sicherung davor. Grund: `freigabe` fehlte an `knowledge_nodes`, `gattung` ebenfalls — jeder frische Klon brach im Schreibpfad mit `no such column: NEW.freigabe` (`L-0de1a9`).
**Testsuite: 772 gruen / 7 rot / 0 Fehler** (Vorzustand heute frueh: 762 / 8 / 6). Offen und ungeprueft: `test_agent_compliance` (2), `test_caveman_integration` (2), `test_knowledge_recall_hook` (1), `test_pruefkorpus` (2).
**Falle, zweimal an einem Tag:** Bricht ein Test im AUFBAU statt in der Zusicherung, ist die erste Hypothese die Vorrichtung, nicht der Gegenstand (`L-c9d2aa`, jetzt 2×). Startmelder sind Befunde, keine Aufgaben — `NODE_INDEX.md`, `antwort_treffer.json`, `auszug/*.jsonl` nicht committen (`L-f1fcb9`).
**Naechster Schritt:** die 7 roten Tests einzeln ansehen — keiner davon ist bisher untersucht, nur gezaehlt.
