# STAND brainlehr — 2026-08-15T06:55:00+0200

**Offen:** H2, H3, **O1** und **F6a** sind erledigt (H2/H3 in openlehr, alles lokal, nichts gepusht). O1: `escHtml()` in `entscheidungen.html` (16/16, vorher 15/16 rot). F6a: `kern/satz.py` lief bis 2026-08-15T06:20:00+0200 nie, seine Maskierungsprobe war unerfüllbar formuliert — jetzt in der Selbsttestliste (90 → 91), der Satzweg selbst war korrekt. **I4 gemessen:** Ausweise kennen nur `gilt_bis`, keinen Widerruf — er ist zu bauen (`5124a160`). **G6 teilbelegt:** yswift läuft in der Sandbox, aber nur im `.app`-Bündel; gehärtete Laufzeit und Beglaubigung ungemessen (`fef0cb9d`). Vier Sicherheitsfunde aus `docs/SICHERHEITSFUNDE_2026-08-14.md` unangetastet. Suiten: brainlehr 1541 grün / 0 rot, openlehr 3316 grün / 25 rot (alle 25 vorbestehend, gegen `HEAD~2` gemessen).

**Nächstes:** `F6` zwei Geschwindigkeiten samt Drift-Wächter, dann `F5` — das Modell hat noch nie am Dokument gesessen („modell" steht in `kern/dokument.py` nur als Selbsttest-Wert). Laut Plan steht Linie A (`81`, `85`, `84`) davor; H4/H5 offen.

**Wartet auf dich:** G5 Befehl mit deinem Passwort (eigener Systembenutzer, Bestand und Ausweisdatei auf `0600`) · Name der Steuerdomäne · Domänen-Repo bei GitHub? · Urheberschaft der atelier-Ergebnisse vor der ersten Weitergabe · F29/F30/F31/F19 · dürfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg (unumkehrbar).

**Nicht vergessen:** **`kern/domaene.py` bleibt gesperrt** — Wirkung Null muss stehen, bevor dort das erste Mal geschrieben wird (heute 0 Treffer für INSERT/UPDATE/commit, Vorbild `kern/regelpaket.py`). Der Haltepunkt-Haken verlangt die Fähigkeit `/learn`, die es hier nicht gibt — Pflicht direkt über `lesson_record`/`knowledge_add` erfüllen (`L-48ca4d`). Ein Auftrag an einen Agenten nennt die Ratschen der Zielrepos, sonst fallen sie erst im vollen Lauf auf (`L-e06933`-Nachbarfall: ASCII-Ratsche in openlehr). Kein Bau an den beiden Dokumentausgaben, solange die Ablösung unbelegt ist.
