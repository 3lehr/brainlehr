# STAND brainlehr — 2026-08-14T10:45:00+0200

**Neustart erledigt, gemessen 2026-08-14T09:57:** alle 14 laufenden MCP-Prozesse sind juenger als `knowledge_mcp_server.py` (Start ab 09:42, Datei 08:42) — **veraltet: 0**. `build_embeddings.py` gelaufen (5 neu gerechnet, Pruefsumme unveraendert), 0 Knoten ohne Vektor. **Aufgabe 111 Schritt 3 gelaufen** (11:1x): 37873 von 38105 Werten umgerechnet, Ratsche von 37851 Abweichlern auf gruen, keine Zeile in der doppelten Stunde. Die zwei veralteten Prozesse waren **nicht** Claude, sondern `hermes-agent` (Watchdog PID 1323) — Kind neu gestartet. Sicherung `brainlehr.db.vor_utc_2026-08-14` liegt noch da, kann nach einem ruhigen Tag weg.

**UTC (111) Schritt 1+2 fertig:** eine Quelle (`kern/zeitmarke.jetzt`), zwei Ratschen, 46 Erzeuger umgestellt, Spalten-Vorgabewerte migriert; Schritt 3 fertig. **111 damit ganz zu.** Die Ratsche war Minuten nach dem gruenen Bestandslauf wieder rot und hat **acht uebersehene Erzeuger** in sieben Dateien gefunden (`.isoformat()` statt `zeitmarke.jetzt`, `kanten_herkunft_rueckwirkend` trug beide Formen) — genau der Mechanismus, der 2026-08-06 fehlte, damals zerfiel der Beschluss acht Tage unbemerkt. `haken/antwort_abruf.py` bewusst ausgelassen: Leser nicht nachweisbar, also kein Beleg fuer den Formwechsel. **Sechste Zeitform gefunden**, die der Plan nicht zaehlte: `'2026-08-07 20:19:11'` ohne Zone (eine Zeile, `knowledge_config`). Entschieden ueber die FORM statt die Uhr — SQLites `datetime('now')` ist UTC, und kein Erzeuger im Repo baut diese Form; das access_log trennt die beiden Lesarten nicht. Historischer Einzelfall, jede andere zonenlose Form wirft weiterhin. **S12 (108): Nicht-Ergebnis als Ertrag** — falscher Nenner, ueber die behandelten Ziele 34 von 205, Zelle n=14, **mit diesem Korpus nicht entscheidbar** (nicht „keine Wirkung"); naechster Schritt groesserer Korpus, Knoten `0e6adb6c`. **Haken (98):** `worktree_identitaet.py` jetzt an `WorktreeCreate`; neue Ratsche prueft Existenz, nicht Tauglichkeit (`L-b3eb79`).

**Grundarchitektur entschieden (eigene Sitzung, ADR-006 bis 009):** DB-Schema als Quelle der Form, Python Grundsprache · zwei Schichten, brainlehr kann nein sagen, **openlehr** nicht · die Werkbank heisst **atelier** (`de.brainlehr.atelier`, umbenannt, 151 Tests gruen, Steuerschnittstelle auf 4599 + `app/werkzeuge/ax_baum.swift`) · Dokumentbasis **LuaLaTeX mit `verapdf -f ua1` als Schranke**, `\DocumentMetadata` bedingt (tex4ht bricht sonst ab), Quelltext semantisch. Konsil-Ergebnis in `docs/KONSIL_ERGEBNIS_2026-08-13.md`.

**Fallen von heute:** `pdfstandard=UA-1` schaltet Tagging NICHT ein (`66a4e633`) · ein Pruefkanal, der vom Verhalten des Menschen abhaengt, ist keiner — dreimal falsch geschlossen (`L-db37c6`) · Bauform ist kein Wirksamkeitsbeleg (`L-dd35c1`). **Dokumentfenster entschieden (ADR-010, Knoten `99f23638`):** nativ, mehrbenutzerfaehig **Zeichen fuer Zeichen** → CRDT; `pycrdt` im Dienst, `yswift` im atelier; erst LAN; Umsetzung durch die KI ist **einstellbar**, Vorgabe „nur vorschlagen". `spikes/crdt_pyswift/probe.py` gruen in beide Richtungen. **Spike 1 erledigt, und die Auflage daraus ist bindend:** `yswift` kuerzt die Teilnehmerkennung auf 32 Bit, `pycrdt` wuerfelt bis 2^53 — darueber verdoppelt sich Text STILL (2^32-1 traegt, 2^32 nicht, `L-44dc9f`). Jeder Teilnehmer vergibt seine Kennung unter 2^32. `automerge` fuer Python ausgeschieden (kein save/load, `merge(Doc)` scheitert). **Baustein-Vertrag steht** (`kern/baustein.py`, ADR-010 Schritt 2): Kennung vergeben statt abgeleitet · Anker haelt/heilt/zeigt, verwaist sichtbar statt still umzuhaengen · `feld` gleichberechtigt neben `absatz` · Zustandswechsel gibt den erreichten Zustand. **Plan fuer den Dienst liegt** (`docs/PLAN_DOKUMENTDIENST_2026-08-14.md`): WebSocket ueber `websockets` (Standardbibliothek hat keinen), `pycrdt-websocket` bewusst noch nicht (es vergibt die Kennung selbst), Bindung auf loopback als Vorgabe — LAN nur per Schalter, **meine Entscheidung, umstossbar**. Schritt 1+2 gebaut: `kern/teilnehmer.py` wirft ueber 2^32-1 statt zu kappen · `kern/dokumentdienst.py` laeuft (ein Raum, Kennung kommt vom Dienst, zwei Sabotagen als Rot-Probe gefahren — die zweite legte einen Mangel im TEST offen: er hing statt zu fallen, jetzt Frist an allen sechs Empfangsstellen). **Naechstes:** Ablage (Stand ueberlebt Neustart), dann das Fenster.
**Erledigt:** 69·80·86·88/1·91·98·107·108·110·111 (1+2+3) · beide Eilmeldungsdefekte · ADR-006..010 · Neustart · Vektoren. **Rot: keins mehr offen.** `kandidatendiagnose` (2) jetzt strict-xfail mit Grund — **und der Grund ist ein Abrufbefund, keine Testschuld** (`3ad501f3`): der Knoten `8dc84938`, an dem die Sockel-Reparatur belegt wurde, steht auf Rang 21 statt in den ersten 5. Wert bewusst NICHT nachgezogen; tragfaehig waere ein eingefrorener Pruefkorpus statt des Lebendbestands. **`haken_verdrahtung` gruen, und der Grund ist ein Befund:** Die Verdrahtung von 27635d2 war um 09:43 wieder WEG — Sicherungsvergleich zeigt als einzigen Unterschied das Ereignis `WorktreeCreate`, alles andere byteweise gleich; das Ereignis selbst gibt es (10 Treffer in der Klient-Binaerdatei). Zurueckgeschrieben, `L-083b95`: eine Verdrahtung in einer Datei, die auch fremde Programme schreiben, braucht eine wiederkehrende Probe — ein Commit belegt nie, dass etwas noch gilt. **Erledigt davon:** `sicherung_s12` (gesichert, 2 neu) · `abrufguete` (veralteter xfail entfernt, nicht als non-strict versteckt). **Neu bewacht:** jedes Modul mit `--selftest` muss in `MODULE` stehen — die Ratsche fand 11 vorbestehend tote Selbsttests, darunter `kern/fundstelle.py` (`L-3f1fa9`); `kern/regelpaket.py` bleibt rot mit Grund (`norm_art`, Normachse). **Wartet auf dich:** Push · **die Aufgabenliste** (TaskList ist je Sitzung, in diesem Fenster leer — die Nummern 7/29/77/… sind hier nicht lesbar) · #105 · #29 · #101 · #20.
