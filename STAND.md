# STAND brainlehr — 2026-08-15T12:05:00+0200

**Offen:** Nur noch Linie C `73`/`79` (haengt an `knowledge_mcp_server.py`, dort schreibt eine ZWEITE SITZUNG — vor dem Anfassen `git stash list` und Agentenregister pruefen), dazu `D67`/`D42` und `B75` in Arbeit. **Sperre `kern/domaene.py` ist AUFGEHOBEN** (`4b5e8b1`, Wirkung Null steht), dadurch O3 (`447b7b5`) und H10 (`c0077fe`) erledigt. Sicherheitsfunde: O1 `ee4645a`, O2 `91f096a`, O3 sichtbar-gemacht-nicht-verhindert, O4 halb (`0600`/`0700`, wirkt nicht gegen den eigenen Benutzer), O5 scharf (Sandbox aktiv, am Buendel belegt). Aehnlichkeitshinweis je Gattung kalibriert (`0a4cea59`, fremde Sitzung): eigener Bestand 88,7 % → 21,3 %.
**Erledigt heute:** H2 H3 H4 H5 H6 H7 H10 · F5 F6 F6a F7, F8 gemessen · I1 I2 I3 I4 · J3 J5 · B76 B88 B89 · C78 · D68 D71 · G1–G3 geklaert, G6 teilbelegt · ADR-016 fortgeschrieben, ADR-019 (fuenf Entscheidungen vor dem ersten Dokument) · LaTeX-Token-Erzeuger `4178a0b7` · Wache gegen `git stash` `be1946ed` · Vektorstand verdrahtet `591149e`. **Zwoelf Planzeilen waren laengst gebaut** — Melder dagegen: `melder/plan_bestandsabgleich.py`.
**Messungen, die jetzt tragen:** Pruefstand wiederholbar (`c37e8161`); Doppellauf byteweise identisch, nur Commit und Bestandsumfang wechseln. Zustaende wirken wieder (`3bf54fe`): A 3/35, B 27/35 bei `falsches_sprechen` 10/10, C 3/35 bei 0/10 — die alten dreimal 33/35 waren ein Artefakt. G3-Stundenlauf: 5535 Updates, 0 Fehler, 19 KB/min je Teilnehmer bei 5–40 ms Verzoegerung — **Bandbreite ist kein Hindernis fuer einen Dienst im Netz, die Reihenfolge ist es.** Byte je Zeichen waechst mit dem Dokument: 2,8 nach 150 s, 15,1 nach 3600 s (`L-55b830`).
**Wartet auf dich:** **G5 ist NICHT nur dein Passwort** — die Vorbereitung (`259672ce`, `docs/G5_SYSTEMBENUTZER.md`) hat gemessen, dass die MCP-Server nicht umziehen koennen (stdio, je Klient ein Prozess unter deiner Kennung). Die Gruppenloesung waere fast wirkungslos. Entwurfsfrage in ADR-020 (in Arbeit): MCP-Server als Klienten des Dienstes, wie die App seit `648432e` — derselbe Umbau ermoeglicht auch den Dienst auf einem anderen Rechner. · Name der Steuerdomaene · Domaenen-Repo bei GitHub? · Urheberschaft der atelier-Ergebnisse · F29/F30/F31/F19 · duerfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg (unumkehrbar) · nichts ist gepusht.
**Neu gemessen (J2, `40fd47eb`, `melder/wirkkette.py`):** **6 von 28 verdrahteten Mechanismen haengen AUSSCHLIESSLICH an `UserPromptSubmit`** — `haken/mcp_veraltet.py`, `knowledge_recall_hook.py`, `auftrag_recall_hook.py`, `regelwechsel.py`, `suchpfad_abruf.py`, `berichte/erstverwendung.py`. Dieses Ereignis feuert nur, wenn ein Mensch etwas eintippt: im Selbstlauf, im Cron und im Subagenten sind sie blind — also genau dann, wenn niemand zusieht. Entscheidung, an welches Ereignis sie stattdessen gehoeren, steht aus.
**Nicht vergessen:** Bau liegt als `~/Desktop/atelier-2026-08-15-aktuell.app` (Bau 832, 215 Faelle gruen, **erstmals mit aktiver Sandbox** — wenn etwas fehlt, ist das der erste Verdacht). Rot-Probe gegen einen alten Stand ist ungueltig, solange sie nicht das GELADENE Modul beweist: ein verlinktes `conftest.py` zog ueber `resolve()` das echte Repo zurueck in `sys.path`, drei Tests meldeten faelschlich gruen (`L-0e0ab6`, 5×, eskaliert). `git stash` war in der globalen Freigabeliste — sechs Verstoesse an einem Tag, Wache liegt jetzt in der repo-eigenen `.claude/settings.json` und wirkt ab der naechsten Sitzung.
