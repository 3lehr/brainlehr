# STAND brainlehr — 2026-08-08T18:27:16+0200
**Fertig heute:** eigenes Repo mit voller Historie · `brainlehr.py` init/raus/rein/haken · Automatik in `haken/` samt `haken/ort.py` · `doctor.py` (5 Proben) · Aufsatz `aufsaetze/agenten.py` · `README.md`/`START.md` · **neun Stellen vom Uebergangsverweis geloest** (hub `a736194b1`, hier `e5ebf7a`) — gefunden durch Abklemmen, nicht durch Suchen (L-5af2e2).
**Offen — EINE Bedingung, Reihenfolge zwingend:** `claude/weiter-1b6c1e` zusammenfuehren → `rm hub/shared-knowledge` → echten Prompt absetzen und pruefen, ob `build_node_index --print` noch Zeichen liefert. Der `SessionStart`-Hook laeuft aus der Hauptauscheckung, die den Fix noch nicht traegt: ohne Verweis 0 statt 5740 Zeichen, trotzdem `rc=0`. Die vier `UserPromptSubmit`-Melder ueberleben (kein Pfad, nur Kommentare).
**Stiller Nebenbefund vom Umzug:** `konfidenz.BEGOD_ROOT` zeigte auf `/Volumes/daten` statt `/Volumes/daten/Begod2026` — jeder relative `source` galt als unbeobachtbar, Konfidenz fiel lautlos in Regime 3. Wurzel jetzt als `VERBUND` in `haken/ort.py`, Beleg `tests/test_pfade_nach_umzug.py` (vorher rot).
**Wartet auf Betreiber:** Push (hub 34, brainlehr 11 Commits) · fuenf Knoten Rang 4/6 ohne Entscheidung · eine abgelaufene Norm im Bestand · `_VERWAIST_shared-knowledge-2026-08-08` loeschen oder behalten.
**Fremder Stand, nicht angefasst:** `hub/.claude/worktrees/stoic-dubinsky-dd9d76/.mcp.json` zeigt noch auf den alten MCP-Pfad — wer dort startet, hat nach dem Entfernen keinen Wissensspeicher.
**Nicht vergessen:** `knowledge.db` ist NICHT versioniert — nach groesseren Aenderungen `python3 brainlehr.py raus auszug/bestand_<datum>.jsonl` und committen.
`compliance` ist definiert und lief NIE, obwohl hub/CLAUDE.md ihn als erzwungen fuehrt (Audit 2026-07-25 mass dasselbe). Eine Regel im Klartext aendert kein Verhalten.
Agentenregister: `hub/laufzeit/agent-register.jsonl` (ueberlebt Neustart), Pfad in `hub/scripts/agent_register_ort.py`.
Testlauf im Arbeitsbaum: 626 gruen, 11 rot — alle elf mit gestashtem Stand identisch rot, Vorbestand (fehlende `knowledge.db` im Arbeitsbaum, Umlautfaltung, instructions).
