# STAND brainlehr — 2026-08-15T11:33:44+0200

**Offen:** Aehnlichkeitshinweis nachkalibriert (`0a4cea59`): `_KNOWLEDGE_HINT_FLOOR` ist jetzt je Gattung (`arbeitsbestand` 0,95 / `nachschlagewerk` 1,75), verglichen wird nur innerhalb einer Gattung — Hinweisquote eigener Bestand 88,7 % → 21,3 %, Fremdkorpus 0 % → 5,3 % (gemessen, 2204 Knoten). Davor unveraendert: I4 (Ausweise ohne Widerruf, `5124a160`), G6 teilbelegt (`fef0cb9d`), vier Sicherheitsfunde aus `docs/SICHERHEITSFUNDE_2026-08-14.md` unangetastet.
**Nächstes:** `F6`/`F5` wie gehabt (Linie A `81`,`85`,`84` davor, H4/H5 offen). Neu dazu: der belegte Dopplungsfall `dd367fd1/b6305304/6e0f0395` faellt unter die neue Schwelle und steht als `strict-xfail` — er ist der Massstab, an dem ein besseres Aehnlichkeitsmass sich messen lassen muss.
**Wartet auf dich:** G5 Befehl mit deinem Passwort · Name der Steuerdomäne · Domänen-Repo bei GitHub? · Urheberschaft der atelier-Ergebnisse vor der ersten Weitergabe · F29/F30/F31/F19 · dürfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg (unumkehrbar).
**Nicht vergessen:** **`kern/domaene.py` bleibt gesperrt.** `/learn` gibt es hier nicht — Pflicht direkt über `lesson_record`/`knowledge_add` (`L-48ca4d`). Rot-Probe gegen einen alten Stand ist ungueltig, solange sie nicht das geladene Modul beweist: ein verlinktes `conftest.py` zog ueber `resolve()` das echte Repo zurueck in `sys.path`, drei Tests meldeten faelschlich gruen (`L-0e0ab6`, 5×, eskaliert). Fuenf rote Tests liegen in fremdem Gebiet (`haken/knowledge_recall_hook.py` laeuft in 120 s Timeout, `melder/kantenstillstand.py`, `test_plan_bestandsabgleich` 2×, `test_sicherung_s12`) — nicht angefasst.
