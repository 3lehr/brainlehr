# STAND brainlehr — 2026-08-15T13:12:31+0200
**GEPUSHT bis `93944370`.** Danach 6 weitere Commits lokal: `150c46b4` `9507b082` `cc2984ab` `562a73c6` `90285514` `d6ab2505` `eb04c728` + Melder-Rubrik. Push jederzeit moeglich, Freigabe liegt vor.
**Urheberschaft entschieden, und sie war laengst gebaut** (Knoten `62ed1a2a`, `L-7033dc`). Betreiber: gibt der Mensch dem Vorschlag recht, hat das Modell die Urheberschaft an diesem Baustein -- sonst nicht. Beantwortet durch `Baustein.herkunft` (`93944370`), am selben Tag fuer eine ANDERE Frage gebaut. Kein Schalter am Dokument. **Nachgeschaerft `90285514`:** ein einzelner Wert reicht nicht, es braucht den VERLAUF -- eine Ruecknahme ("erst widersprochen, dann nachgegeben") loescht sonst den aussagekraeftigsten Vorgang. Bei Ruecknahme wird DERSELBE Eintrag mit `zurueckgenommen_am` ergaenzt; das Urteil wird bei jedem Aufruf neu gerechnet statt gespeichert.
**Der grosse Messbefund des Nachmittags** (`eb04c728`, Knoten `cbd79aaa`): Textregeln als Domaenenpaket im Sinne des Belegvertrags = **0 bei 13563 abgesuchten Dateien**. `voice_und_tone` und `zielgruppen` stehen als Daten in `aka-design-guide.json` -- **kein Code liest sie**, `designtokens_latex.py` ueberspringt genau diese beiden. Gegenbefund, meine Auftragsannahme war falsch: **Bilderzeugung existiert laengst**, `openlehr/begod/scripts/ai_image_forge.py`, 1322 Zeilen, mit `image_style_anchor`/`image_negative_prompt`. Trennlinie: bei Diagrammen ist ein Tokenwert buchstaeblich als `fundstelle` belegbar (Belegvertrag passt unveraendert), bei Foto/Illustration ist "konsistent" Bildsprache -- die beiden zu vermengen waere der Denkfehler.
**Sechs blinde Mechanismen entschieden** (`d6ab2505`): drei zusaetzlich an `SubagentStart` (fand nebenbei 21 veraltete MCP-Prozesse), einer bewusst NICHT (Wissensabruf kostet gemessen 6,0 s -- an jede Delegation gehaengt eine Bremse), zwei sind Bibliotheken. Zweiter Blindgaenger dabei: `regelwechsel.py` schrieb `UserPromptSubmit` FEST in seine Ausgabe. Melder zieht nach: Stufe 2 unbelegt = **0**, neue Rubrik "bewusst nur fuer Menschen" = 3, sichtbar statt geloescht. Ein Vermerk braucht 80 Zeichen Begruendung, sonst zaehlt er nicht -- kein Freibrief.
**Plan neu vermessen** (`562a73c6`): 35 von 58 erledigt, 10 teilweise, 13 offen. **STAND log frueher:** H2–H7 waren NICHT erledigt, belegbar sind nur H1 und H10. Drei Faelle "gebaut, nicht wirksam": `73` (0 statt >=125 Kanten am echten Bestand), `79` (`normiere_*` nie im Schreibpfad gerufen), `97` (Waechter ohne Ausloeser). Sechs Zeilen waren unbemerkt erledigt (`42` `68` `71` `76` `89`, `86` Schritt 2).
**In Arbeit:** misst, ob das Zuspiel aus dem Speicher tatsaechlich WIRKT -- bisher ist nur die Trefferquote belegt, nicht die Wirkung. Selbstauskunft im Chat: von rund 15 heute eingespielten Treffern ist EINER nachweislich in einen Auftrag eingegangen.
**Wartet auf dich:** Eintrittsweg auf markus-lehr.de (braucht Serverzugriff, einziger Punkt mit Uhr) · duerfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg (unumkehrbar) · Name der Steuerdomaene · Domaenen-Repo bei GitHub? · G5 Systembenutzer (Passwort tippst du selbst). **Homepage: nach hinten, Richtung Neubau statt WordPress-Anker** (Knoten `9a27a332`) -- die ~51 Zeilen `register_post_meta` fallen damit AUS der Reihenfolge, nicht nach hinten.
**Nicht vergessen:** Bau als `~/Desktop/atelier-2026-08-15-aktuell.app` (215 Faelle gruen, aktive Sandbox). O2 traegt nur gegen Browser (`_herkunft_ok()` prueft nur `Origin`; faellt die Loopback-Bindung, ist die Pruefung wirkungslos, nicht schwaecher). `git stash` bleibt verboten, Wache in der repo-eigenen `.claude/settings.json`.
