# STAND brainlehr — 2026-08-15T13:15:00+0200
**GEPUSHT.** brainlehr `796d0562..93944370` (`brainlehr/b4-ausweis`), openlehr `f8484d99..e40eb739` (`merge/daten-features`) -- erste Veroeffentlichung seit gestern.
**Urheberschaft entschieden, und sie war laengst gebaut** (Knoten `62ed1a2a`, `L-7033dc`). Betreiber: gibt der Mensch dem Vorschlag recht, hat das Modell die Urheberschaft an diesem Baustein -- sonst nicht. Das beantwortet `Baustein.herkunft` aus `93944370` (`eingegeben`/`abgeleitet`/`vorschlag_angenommen`/`importiert`), am selben Tag fuer eine ANDERE Frage entstanden. **Kein Schalter am Dokument** -- er waere eine Behauptung ueber etwas, das das Feld schon weiss, und in zu grober Koernung (ein Dokument ist gemischt). **Trainingsfrage gleich miterledigt:** `vorschlag_angenommen` ist wertlos (Training auf eigener Ausgabe); wertvoll ist der RUECKFALL auf `eingegeben` bei Handaenderung -- ein Praeferenzpaar als Nebenprodukt.
**Erledigt seit 12:05:** App-Regression nach O2 (`150c46b4`) -- `DomaeneImportDienst.swift`/`QuellenBereich.swift` sandten keinen Origin-Kopf, liefen seit `91f096a` (09:30) in 403, sechs Stunden unbemerkt. Zweiter Fund dabei: die Ausweispflicht aus `03cce992` zog ueber die Formel "7 von 9 POST-Pfaden" zwei LESENDE Endpunkte mit (`/api/fundstelle`, `/api/domaene-import` -- gemessen: kein INSERT/UPDATE, `speicher.lesen()`). Umfang zurueckgenommen, Schranke unangetastet. · Fuenf rote Tests `b854d9c5` · Baustein traegt Herkunft `93944370`.
**In Arbeit (3 Agenten):** F30 Behoerdenpost-Faecher in openlehr (1 von 6 Postsorten gebaut) · die sechs blinden Mechanismen · ADR-020 Schritt 2 steht noch aus.
**Offen:** Linie C `73`/`79` (`knowledge_mcp_server.py`, fremde Sitzung schreibt dort) · `D67`/`D42` · `B75`.
**Wartet auf dich:** Name der Steuerdomaene · Domaenen-Repo bei GitHub? · duerfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg (unumkehrbar) · G5 Systembenutzer (Passwort tippst du selbst).
**Sicherheitsfunde:** O1 `ee4645a` · O2 `91f096a` **traegt nur gegen Browser** (`_herkunft_ok()` prueft nur `Origin`; ein Programm setzt ihn frei -- faellt die Loopback-Bindung, ist die Pruefung wirkungslos, nicht schwaecher) · O3 sichtbar gemacht statt verhindert · O4 halb · O5 scharf.
**Nicht vergessen:** Bau als `~/Desktop/atelier-2026-08-15-aktuell.app` (215 Faelle gruen, aktive Sandbox -- fehlt etwas, erster Verdacht). Rot-Probe gegen einen alten Stand ist ungueltig, solange sie nicht das GELADENE Modul beweist (`L-0e0ab6`, 5x). `git stash` bleibt verboten, Wache in der repo-eigenen `.claude/settings.json`.
