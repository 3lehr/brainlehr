# STAND brainlehr — 2026-08-14T10:45:00+0200

**Neustart erledigt, gemessen 2026-08-14T09:57:** alle 14 laufenden MCP-Prozesse sind juenger als `knowledge_mcp_server.py` (Start ab 09:42, Datei 08:42) — **veraltet: 0**. `build_embeddings.py` gelaufen (5 neu gerechnet, Pruefsumme unveraendert), 0 Knoten ohne Vektor. **Aufgabe 111 Schritt 3 gelaufen** (11:1x): 37873 von 38105 Werten umgerechnet, Ratsche von 37851 Abweichlern auf gruen, keine Zeile in der doppelten Stunde. Die zwei veralteten Prozesse waren **nicht** Claude, sondern `hermes-agent` (Watchdog PID 1323) — Kind neu gestartet. Sicherung `brainlehr.db.vor_utc_2026-08-14` liegt noch da, kann nach einem ruhigen Tag weg.

**UTC (111) Schritt 1+2 fertig:** eine Quelle (`kern/zeitmarke.jetzt`), zwei Ratschen, 46 Erzeuger umgestellt, Spalten-Vorgabewerte migriert; Schritt 3 fertig. **111 damit ganz zu.** Die Ratsche war Minuten nach dem gruenen Bestandslauf wieder rot und hat **acht uebersehene Erzeuger** in sieben Dateien gefunden (`.isoformat()` statt `zeitmarke.jetzt`, `kanten_herkunft_rueckwirkend` trug beide Formen) — genau der Mechanismus, der 2026-08-06 fehlte, damals zerfiel der Beschluss acht Tage unbemerkt. `haken/antwort_abruf.py` bewusst ausgelassen: Leser nicht nachweisbar, also kein Beleg fuer den Formwechsel. **Sechste Zeitform gefunden**, die der Plan nicht zaehlte: `'2026-08-07 20:19:11'` ohne Zone (eine Zeile, `knowledge_config`). Entschieden ueber die FORM statt die Uhr — SQLites `datetime('now')` ist UTC, und kein Erzeuger im Repo baut diese Form; das access_log trennt die beiden Lesarten nicht. Historischer Einzelfall, jede andere zonenlose Form wirft weiterhin. **S12 (108): Nicht-Ergebnis als Ertrag** — falscher Nenner, ueber die behandelten Ziele 34 von 205, Zelle n=14, **mit diesem Korpus nicht entscheidbar** (nicht „keine Wirkung"); naechster Schritt groesserer Korpus, Knoten `0e6adb6c`. **Haken (98):** `worktree_identitaet.py` jetzt an `WorktreeCreate`; neue Ratsche prueft Existenz, nicht Tauglichkeit (`L-b3eb79`).

**Grundarchitektur entschieden (eigene Sitzung, ADR-006 bis 009):** DB-Schema als Quelle der Form, Python Grundsprache · zwei Schichten, brainlehr kann nein sagen, **openlehr** nicht · die Werkbank heisst **atelier** (`de.brainlehr.atelier`, umbenannt, 151 Tests gruen, Steuerschnittstelle auf 4599 + `app/werkzeuge/ax_baum.swift`) · Dokumentbasis **LuaLaTeX mit `verapdf -f ua1` als Schranke**, `\DocumentMetadata` bedingt (tex4ht bricht sonst ab), Quelltext semantisch. Konsil-Ergebnis in `docs/KONSIL_ERGEBNIS_2026-08-13.md`.

**Fallen von heute:** `pdfstandard=UA-1` schaltet Tagging NICHT ein (`66a4e633`) · ein Pruefkanal, der vom Verhalten des Menschen abhaengt, ist keiner — dreimal falsch geschlossen (`L-db37c6`) · Bauform ist kein Wirksamkeitsbeleg (`L-dd35c1`). **Dokumentfenster entschieden (ADR-010, Knoten `99f23638`):** nativ, mehrbenutzerfaehig **Zeichen fuer Zeichen** → CRDT; `pycrdt` im Dienst, `yswift` im atelier; erst LAN; Umsetzung durch die KI ist **einstellbar**, Vorgabe „nur vorschlagen". `spikes/crdt_pyswift/probe.py` gruen in beide Richtungen. **Spike 1 erledigt, und die Auflage daraus ist bindend:** `yswift` kuerzt die Teilnehmerkennung auf 32 Bit, `pycrdt` wuerfelt bis 2^53 — darueber verdoppelt sich Text STILL (2^32-1 traegt, 2^32 nicht, `L-44dc9f`). Jeder Teilnehmer vergibt seine Kennung unter 2^32. `automerge` fuer Python ausgeschieden (kein save/load, `merge(Doc)` scheitert). **Baustein-Vertrag steht** (`kern/baustein.py`, ADR-010 Schritt 2): Kennung vergeben statt abgeleitet · Anker haelt/heilt/zeigt, verwaist sichtbar statt still umzuhaengen · `feld` gleichberechtigt neben `absatz` · Zustandswechsel gibt den erreichten Zustand. **Plan fuer den Dienst liegt** (`docs/PLAN_DOKUMENTDIENST_2026-08-14.md`): WebSocket ueber `websockets` (Standardbibliothek hat keinen), `pycrdt-websocket` bewusst noch nicht (es vergibt die Kennung selbst), Bindung auf loopback als Vorgabe — LAN nur per Schalter, **meine Entscheidung, umstossbar**. Schritt 1+2 gebaut: `kern/teilnehmer.py` wirft ueber 2^32-1 statt zu kappen · `kern/dokumentdienst.py` laeuft (ein Raum, Kennung kommt vom Dienst, zwei Sabotagen als Rot-Probe gefahren — die zweite legte einen Mangel im TEST offen: er hing statt zu fallen, jetzt Frist an allen sechs Empfangsstellen). **Schritte 1–4 fertig:** `kern/teilnehmer.py` (wirft ueber 2^32-1 statt zu kappen) · `kern/dokumentdienst.py` (Raum, Kennung vom Dienst, Stand ueberlebt Neustart, Update-Fehler toetet die Verbindung nicht mehr) · `kern/dokument.py` (Anmerkungen liegen im SELBEN Dokument wie die Bausteine; Anker loest nur ueber die Kennung auf, verwaist sichtbar). **LAN ist offen** (Betreiberentscheid) — **aber mit Ausweispflicht**: eine Regel statt eines Schalters, alles ausser `127.0.0.1` verlangt `ausweis.loese_auf`. Laeuft auf **192.168.178.50:4610**, Anmeldung mit dem eigenen Ausweis belegt. **Sicherheitsplan** `docs/PLAN_SICHERHEIT_2026-08-14.md`: acht Kennzahlen ohne Schwellen, vier Sofortwarnungen ohne Normalfall, alles Mengenhafte erst nach Nullmessung mit dem Mini (S1–S3). Preis benannt: keine Transportverschluesselung, das Geheimnis wandert im Klartext. **Naechstes:** S1–S3, dann Auftrag 5 (Fenster im atelier).
**Erledigt:** 69·80·86·88/1·91·98·107·108·110·111 (1+2+3) · beide Eilmeldungsdefekte · ADR-006..010 · Neustart · Vektoren. **Rot: keins mehr offen.** `kandidatendiagnose` (2) jetzt strict-xfail mit Grund — **und der Grund ist ein Abrufbefund, keine Testschuld** (`3ad501f3`): der Knoten `8dc84938`, an dem die Sockel-Reparatur belegt wurde, steht auf Rang 21 statt in den ersten 5. Wert bewusst NICHT nachgezogen; tragfaehig waere ein eingefrorener Pruefkorpus statt des Lebendbestands. **`haken_verdrahtung` gruen, und der Grund ist ein Befund:** Die Verdrahtung von 27635d2 war um 09:43 wieder WEG — Sicherungsvergleich zeigt als einzigen Unterschied das Ereignis `WorktreeCreate`, alles andere byteweise gleich; das Ereignis selbst gibt es (10 Treffer in der Klient-Binaerdatei). Zurueckgeschrieben, `L-083b95`: eine Verdrahtung in einer Datei, die auch fremde Programme schreiben, braucht eine wiederkehrende Probe — ein Commit belegt nie, dass etwas noch gilt. **Erledigt davon:** `sicherung_s12` (gesichert, 2 neu) · `abrufguete` (veralteter xfail entfernt, nicht als non-strict versteckt). **Neu bewacht:** jedes Modul mit `--selftest` muss in `MODULE` stehen — die Ratsche fand 11 vorbestehend tote Selbsttests, darunter `kern/fundstelle.py` (`L-3f1fa9`); `kern/regelpaket.py` bleibt rot mit Grund (`norm_art`, Normachse). **Waechter repariert** (`47f7c84`, `L-528f0c`): `regelwechsel.py` las `actor` statt `norm_entschieden_von` und meldete 15 von 23 Rang-1-Normen als „ungeklaerte Herkunft" — woertliche Abschnitte der globalen CLAUDE.md, deren `source` genau diese Datei nennt. Ein blockierender Melder mit dieser Trefferquote wird weggeklickt und meldet dann auch den echten Fall nicht. Zweiter Fall derselben Klasse (`74a61f6`, `L-528f0c` 2x): `pruefer.py` mass `zurueckgezogen_grund/_von/_am` ueber dem Nenner `zurueckgezogen = 0` — also ueber den Zeilen, in denen sie leer sein MUESSEN. „100 % leer" war die einzig moegliche Antwort, nicht ein Befund; tatsaechlich 5 von 2200 zurueckgezogen und genau die tragen alle drei Felder. Vier weitere Nenner nachgezogen (`ccc65c4`): `gilt_bis` (bei `norm_unbefristet` PFLICHTMAESSIG leer, 78/78) · `status` und `auto_rule_generated` (unterscheiden sehr wohl, gegen die Eskalationsschwelle gegengerechnet) · `norm_entschieden_belegart` (Befund bleibt, aber Nenner 85 statt 2195). **Dauermeldungen 18 → 12; die zwoelf sind echte Luecken und sollen stehen.** **Griff dagegen: vor jeder Quote den Nenner hinschreiben — welche Bedingung erlaubt dieser Spalte ueberhaupt einen Wert?** **Suite: 1478 gruen, 0 rot.** Vierter Melder, in `hub` (`99f40913a`): `gegenprobe_faellig.py` nannte als Ausweg einen relativen Pfad, der vom Leseort (brainlehr) aus **nicht existiert** — nachgestellt —, und zaehlte 83 Dateinamen in jede Sitzung. Jetzt absoluter Pfad aus `__file__`, Deckel bei 5 und **benannt**, sortiert nach mtime statt alphabetisch. **Verfahren dazu: `L-1c081e`.** Auch `rasterblick` nachgezogen (`3a94f4b`), gleiche zwei Punkte. **Bei `planbindung` bewusst AUFGEHOERT:** 100 von 123 ist dort kein Baufehler, „fehlende Kennung" ist laut Modulkopf der Normalfall vor der Bindung — seine Menge ist eine Entwurfsfrage, keine Reparatur. **Vier Betreiberentscheidungen getroffen und umgesetzt** (`64c7ae2` Plan, `1aedf27`): Gegenprobe-Altlast 83 → **6** (nur noch die von heute, also die fahrbaren; jeder Vermerk sagt ausdruecklich, dass keine Gegenprobe behauptet wird) · Token-Spalten bleiben, Befund in `e504b10c` nachgetragen — **Hermes misst wirklich** (`session_model_usage`, 30 Zeilen, 41 022 641 Eingabe-Token, mit cache_read/write), aber je Sitzung×Modell und damit **groeber** als die 2026-08-08 schon verworfene Koernung; loest es also nicht · **A3 fertig** (`c9482c6`): drei Gesetzesnormen tragen `norm_entschieden_von='gesetzgeber'`, Achse 3 **beantwortet**. Mitgebaute Sperre, ohne die es geschadet haette: `_urheber` haette sie ab sofort als „URHEBER OFFEN" gemeldet — derselbe Fehlalarm wie heute Vormittag, nur von der anderen Seite. **Offener Befund mit neuem Gewicht:** `FREMDE_QUELLE` trifft auch, wenn „Gesetz" nur in der Prosa steht — und dieses Muster treibt jetzt einen SCHREIBVORGANG. Abgesichert nur durch den Probelauf mit Kandidatenliste, also durch einen Menschen, nicht durch Code. **Normachse 2 war gar keine offene Frage:** `_is_spannung` nennt Sein/Sollen/Duerfen woertlich, `schema.sql` erzwingt sie per Trigger seit Auftrag 95 — es fehlt die Befuellung, nicht die Taxonomie; und die kommt erst NACH der Schranke am Schreibweg (`PLAN_RECHTSRAUM` Schritt 1). **Alte Fassung:** (1) ab wann eine Gegenprobe verfaellt (78 Altfaelle) · (2) Normachse 2 — welche `norm_art` es gibt (85 von 85 leer, `_is_spannung` unterscheidet dadurch nichts) · (3) Normachse 3 ist **echt faellig**, kein Fehlalarm: drei Normen stammen aus Gesetz (WEG-Jahresabrechnung, GEG/GMoDG, erkannt an `gesetz`/`BGBl`) und sind von dir gar nicht widerrufbar · (4) `access_log.tokens_*` sind seit 12722 Zeilen leer und **strukturell unfuellbar** — die Zahlen kennt der Klient, nicht der Dienst (`kern/tokenkosten.py` haelt das schon fest). **WorktreeCreate war blockiert** (`64dea18`, `f7333fe`, `L-b034c4` 2x): Klient verlangt, dass der gedruckte Pfad EXISTIERT — der Haken hielt sich fuer einen Nachbereiter. Gemessen: `git worktree add` nimmt ein **leeres vorhandenes** Verzeichnis, scheitert am befuellten; also anlegen, nicht befuellen. **Der Test hatte den Fehler festgeschrieben** (`assert not erwartet.exists()`) und war gruen, waehrend nichts mehr startete. Jetzt zwei Zusicherungen: existiert UND leer. **Luecke daraus geschlossen** (`b7d1172`): `nachziehen_hier()` seedet den Baum, in dem man STEHT, verdrahtet an SessionStart (Sicherung `settings.json.bak-2026-08-14T1242`, additiv). Belegt am echten Arbeitsbaum dieser Sitzung: vorher kein `CLAUDE.md`, danach Symlink. **Melder aufgeraeumt:** Gegenprobe 6, Rasterblick 6 offen, beide nur von heute (vorher 83/82, davon 47 Fehlalarm, `L-7faf6e`). **`FREMDE_QUELLE` NICHT geschaerft** — am echten Bestand gemessen: 10 Treffer in 2195 Knoten, **null Fehlalarm**; der einzige Falschtreffer war mein konstruierter Testfall. **`PLAN_RECHTSRAUM` Schritt 1 war schon fertig** (Pflichttrigger installiert, ohne den `%EN %`-Fehlalarm). **Suite 1485 gruen, 0 rot.** **Dauermeldungen 18 → 10** (`a9bc378`): `access_count` und `knowledge_nodes.bedient_von` haben Schreiber und sind nur selten befuellt; `lessons_learned.bedient_von` und `access_log.bedient_von` haben **keinen** und bleiben gemeldet — dieselbe Spalte, verschiedene Antworten (`L-1c081e` 2x). **Falle, unaufgeklaert:** ein Suitenlauf meldete 12 failed / 460 errors, der Wiederholungslauf 1485 gruen. Ursache **nicht belegt** — ich hatte den Lauf durch `| tail -3` geschickt und die Diagnose selbst vernichtet (`L-2ed46b`). Verdacht Parallelsitzung (`L-a1b276`), unbewiesen. **Wartet auf dich:** Push (brainlehr + **hub**) · **die Aufgabenliste** (TaskList ist je Sitzung, in diesem Fenster leer — die Nummern 7/29/77/… sind hier nicht lesbar) · #105 · #29 · #101 · #20.
