# STAND brainlehr — 2026-08-08T19:53:05+0200
**Repo liegt jetzt aussen:** `github.com/3lehr/brainlehr`, `visibility: PRIVATE`, 145 Commits. Entscheidung `d6f0dd0f` (Rang 2): brainlehr wird NIE oeffentlich geschaltet — `knowledge.db` liegt in 31 Commits der Historie. Eine Veroeffentlichung entsteht als NEUES Repo mit frischer Historie, nie durch Umschalten. Dafuer gilt hier kein Veroeffentlichungsvorbehalt beim Schreiben.
**Uebergangsverweis `hub/shared-knowledge` ist entfernt.** Neun Nutzer waren es, gefunden durch Abklemmen statt Suchen (L-5af2e2); dabei die stille `BEGOD_ROOT`-Regression gefunden (zeigte auf `/Volumes/daten`, jeder relative `source` galt als unbeobachtbar).
**Neu heute:** Annahmen-Tabelle mit `belegrang`/`kosten_wenn_falsch` als Pflicht und drei Werkzeugen · `bereinigung.py` (meldet, schwaerzt nicht) im Recall verdrahtet · `gegenprobe.py` (zweite Instanz rechnet nach) · Auszug zieht sich am `Stop`-Haken selbst nach · Modell-Schreibweisen vereinheitlicht (357 Zeilen).
**BSI: der Sync hatte das Regelwerk zerstoert** — Gegenseite baute ihre Ablage um, Parser fand 0 Controls, das leere Profil ueberschrieb die 947, gemeldet wurde `synced: true`. Wiederhergestellt, Einbruchssperre eingebaut (`hub c7db4f4b8`), Katalog auf 47de2824, 951 Controls, Review durchgefuehrt, alle drei ABSOLUT-Hard-Stops unveraendert.
**Gegenproben:** alle zwoelf vermerkt, **drei widersprechen sich selbst** — `ab_vergleich_abruf` (Bestand wuchs 1971→1974 waehrend des A/B-Laufs), `pruefkorpus_v3_erweitert` und `_lauf_B` (beide abgebrochen). Nicht als Beleg verwenden.
**Wartet auf Betreiber:** **sechs** Knoten Rang 4/6 ohne Entscheidung (alle buckeberg/Verwalterwahl, nicht fuenf wie bisher notiert) · abgelaufene Norm `/ops/buckeberg-anbieterabend-2026-08-05` (`gilt_bis 2026-08-06`) — die zweite abgelaufene ist Testmaterial und soll so bleiben · `_VERWAIST_shared-knowledge-2026-08-08` · fremde `.mcp.json` in `hub/.claude/worktrees/stoic-dubinsky-dd9d76` zeigt auf den entfernten Pfad.
**In einer Woche faellig:** `python3 bereinigung.py --bericht` — erste Messung war null Funde bei 11k ausgelieferten Zeichen, ein Fehlalarm sichtbar (Telefonmuster in IBAN-Ziffern).
**Offene Annahme A-d93330:** vier Spalten fuehren eine Identitaet als freien Text (`actor`, `norm_entschieden_von`, `zurueckgezogen_von`, `annahmen.geprueft_von`). Traegt, solange nur Agenten darin stehen; teurer ab dem ersten menschlichen Namen.
**Nicht vergessen:** die drei `annahme_*`-Werkzeuge sind erst nach einem Sitzungsneustart aufrufbar (MCP-Prozess laeuft mit altem Code).
Testlauf im Arbeitsbaum: 646 gruen, 11 rot — dieselben elf wie den ganzen Tag (fehlende `knowledge.db` im Arbeitsbaum, Umlautfaltung, instructions).
