# STAND brainlehr — 2026-08-08T18:55:43+0200
**Uebergangsverweis ist weg.** `hub/shared-knowledge` entfernt, Zweig zusammengefuehrt, alle sieben Nutzer nachgemessen: `build_node_index` 5742 Zeichen, `wiedereinstieg` 3147, knowledge-viz und methodik_export gruen. Neun Fundstellen waren es, gefunden durch Abklemmen statt Suchen (L-5af2e2).
**Stiller Nebenbefund vom Umzug:** `konfidenz.BEGOD_ROOT` zeigte auf `/Volumes/daten` statt `/Volumes/daten/Begod2026` — relative `source` galten als unbeobachtbar, Konfidenz fiel lautlos in Regime 3. Wurzel jetzt als `VERBUND` in `haken/ort.py`, Beleg `tests/test_pfade_nach_umzug.py`.
**Stiftshuette:** meldet sich nicht mehr (drei Zeilen aus `metroviz_autostart.sh`, LaunchAgent ausgehaengt, plist nach `_abgeschaltet/`). Uebernahme nach Plan: Punkt 1 und 2 gebaut, Punkt 3 verworfen — `scan_codebase.py` liest Dart, nicht Python, und erzeugt gegen brainlehr eine leere Karte.
**Neu — Annahmen** (`1b4bc9a`): Tabelle mit `belegrang` und `kosten_wenn_falsch` als Pflicht, vier Regeln an der Tabelle, drei Werkzeuge (`annahme_erfassen`/`_entscheiden`/`_liste`). Live-DB migriert, Sicherung `knowledge.db.bak-2026-08-08-annahmen`.
**Neu — Bereinigung** (`e368f0f`): meldet, schwaerzt nicht (Entscheidung des Betreibers). Verdrahtet im Recall, mit Koeder belegt. Erste Messung: drei echte Abrufe, 11k Zeichen, null Funde; ein Fehlalarm sichtbar (Telefonmuster in IBAN-Ziffern).
**Naechster Schritt dort:** eine Woche Protokoll sammeln, dann `python3 bereinigung.py --bericht` und ueber Schwelle und Schwaerzung an gemessenen Daten entscheiden.
**Wartet auf Betreiber:** Push (hub 37, brainlehr 15 Commits) · fuenf Knoten Rang 4/6 ohne Entscheidung · eine abgelaufene Norm · `_VERWAIST_shared-knowledge-2026-08-08` loeschen oder behalten · 12 Laeufe ohne Gegenprobe-Vermerk.
**Fremder Stand, nicht angefasst:** `hub/.claude/worktrees/stoic-dubinsky-dd9d76/.mcp.json` zeigt auf den entfernten Pfad — wer dort startet, hat keinen Wissensspeicher.
**Nicht vergessen:** `knowledge.db` ist NICHT versioniert — nach groesseren Aenderungen `python3 brainlehr.py raus auszug/bestand_<datum>.jsonl` und committen.
Testlauf im Arbeitsbaum: 638 gruen, 11 rot — dieselben elf wie vorher (fehlende `knowledge.db` im Arbeitsbaum, Umlautfaltung, instructions).
