# STAND brainlehr — 2026-08-08T18:27:16+0200
**Fertig heute:** eigenes Repo mit voller Historie · `brainlehr.py` init/raus/rein/haken · Automatik in `haken/` samt `haken/ort.py` · `doctor.py` (5 Proben) · erster Aufsatz `aufsaetze/agenten.py` · `README.md` und `START.md` (letzterer gegen Verrottung getestet) · **neun Stellen vom Uebergangsverweis geloest** (7 im hub `a736194b1`, 2 hier `e5ebf7a`), gefunden durch Abklemmen statt Suchen.
**Offen — EINE Bedingung, Reihenfolge zwingend:** `claude/weiter-1b6c1e` zusammenfuehren, DANN `rm hub/shared-knowledge`, DANN einen echten Prompt absetzen und nachsehen, ob `build_node_index --print` weiterhin Zeichen liefert. Grund: der `SessionStart`-Hook laeuft aus der Hauptauscheckung, und die traegt den Fix noch nicht — ohne Verweis liefert er 0 statt 5740 Zeichen und endet trotzdem auf `rc=0`. Die vier `UserPromptSubmit`-Melder sind geprueft und ueberleben das Entfernen (kein Pfad, nur Kommentare).
**Nebenbefund vom Umzug, still:** `konfidenz.BEGOD_ROOT` zeigte seit dem 08.08. auf `/Volumes/daten` statt `/Volumes/daten/Begod2026` — jeder relative `source` galt als unbeobachtbar, Konfidenz fiel lautlos in Regime 3. Wurzel liegt jetzt als `VERBUND` in `haken/ort.py`, Beleg `tests/test_pfade_nach_umzug.py`.
**Fremder Stand, nicht angefasst:** `hub/.claude/worktrees/stoic-dubinsky-dd9d76/.mcp.json` zeigt noch auf den alten MCP-Pfad — wer dort startet, hat nach dem Entfernen keinen Wissensspeicher.
**Wartet auf Betreiber:** Push (hub 32, brainlehr 9 Commits ueber der uebernommenen Historie) · fuenf Knoten mit Rang 4/6 ohne Entscheidung · eine abgelaufene Norm im Bestand · `_VERWAIST_shared-knowledge-2026-08-08` loeschen oder behalten.
**Nicht vergessen:** `knowledge.db` ist NICHT versioniert — nach groesseren Aenderungen `python3 brainlehr.py raus auszug/bestand_<datum>.jsonl` und committen.
`compliance` ist definiert und lief NIE, obwohl hub/CLAUDE.md ihn als erzwungen fuehrt (Audit 2026-07-25 mass dasselbe). Eine Regel im Klartext aendert kein Verhalten.
Agentenregister liegt jetzt unter `hub/laufzeit/agent-register.jsonl` (ueberlebt Neustart), Pfad in `hub/scripts/agent_register_ort.py`.
Zugriffsprotokoll: die 1310 Zeilen ohne `actor` sind alle vom 2026-08-06 oder aelter — seit dem 07.08. ist die Zuordnung lueckenlos. Kein Handlungsbedarf, nicht rueckwirkend reparierbar.
Testlauf: 675 gruen, 7 rot — alle sieben vorbestehend (instructions, caveman-policy, Umlautfaltung).
