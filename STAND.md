# STAND brainlehr — 2026-08-15T14:00:22+0200
**Betreiber abwesend, autonomer Lauf.** Auftrag: Gesamtplan vollstaendig abarbeiten, vorher fortschreiben. Fortschreibung erledigt (`f05af23b`, `abd06928`): **65 Kennungen -- 36 erledigt, 9 teilweise, 13 offen, 7 nicht nachgemessen**, dazu vier neue `I5`-`I8`. Neue Kategorie im Plan: **"gebaut, aber wirkungslos"** -- die beherrschende Fehlerklasse dieses Tages, zwoelfmal aufgetreten.
**Gerade erledigt:** `73`/`79` sind wirksam (`0d8ba87a`) -- Herkunftskette **257 Kanten statt 0** (Plan verlangte >=125, zweiter Lauf 0 neu, also idempotent), Normierung sitzt im Schreibpfad, 29 Bestandszeilen nachgezogen. Befund dabei: `ausweis.loese_auf()` kannte nur den woertlichen Sentinel `unbekannt` -- `Unbekannt` und Varianten mit Leerzeichen fielen als behauptete Namen durch. · `97` war NIE wirkungslos, nur nie belegt (`d786be7a`, 23,3 ms je Aufruf, 0 Fehlalarme bei 83 echten Auftraegen).
**Der teuerste Messfehler des Tages, und er betrifft den Plan selbst:** Die Planmessung las nur die repo-eigene `.claude/settings.json` und uebersah `~/.claude/settings.json`. **Es gibt ZWEI, und wer eine liest, misst falsch** (`L-ca836f`, heute in beide Richtungen aufgetreten). Alle uebrigen Verdrahtungsaussagen des Plans (`98`, `92`, `96`) sind deshalb nachzumessen -- laeuft.
**Zwei Messbefunde zur eigenen Arbeitsweise:** Der Abruf wirkt in **11,1 %** (37 von 334, `f4ebc128`) -- erstmals die Wirkung statt der Trefferquote; sechs Scheintreffer hatten die Kausalitaet verkehrt herum. · **Keine Formulierungseigenschaft** der Hausregeln erklaert die Verstoesse, ein verdrahteter Mechanismus ebensowenig (`c1d18f79`) -- und **490 von 572 Lehren (86 %) lassen sich ueberhaupt keinem Abschnitt zuordnen.** Meine eigenen Zahlenangaben waren heute dreimal falsch (11 statt 79 Laeufe, 15 statt 334 Kennungen, 91 statt 96 Module) -- jedes Mal zaehlbar gewesen.
**Sechs Agenten laufen:** `H2`/`H3` (openlehr, Belegvertrag am Klassifikator) · `I2` Designvorrat (beide Sperren sind seit heute ueberholt) · Nachmessung der sieben ungemessenen Zeilen plus Verdrahtung · Zeitmessung HTTP-Umweg (Vorbedingung fuer `I6`) · `I3` Auflage 3 (kann eine importierte Tabellendatei in `new Function` landen -- solange offen, ist der Import GESPERRT).
**Neue Werkzeuge, alle NICHT verdrahtet:** `melder/abrufwirkung.py` (`733c6d8b`, Verlauf mit Ruecknahme wie am Baustein) · `melder/agentendauer.py` (`41b5db73`). Schaetzung gegen Messung laeuft seit heute mit: drei Datenpunkte, Abweichung zwischen -34 % und +88 %, kein erkennbares Vorzeichen.
**Wartet auf dich:** Eintrittsweg auf markus-lehr.de (braucht Serverzugriff, einziger Punkt mit Uhr) · duerfen die 22 GB in `../brainlehr-archiv/db-sicherungen-2026-08-14/` weg · Name der Steuerdomaene · Domaenen-Repo bei GitHub? · G5 Systembenutzer · Linie E (`20`/`29`/`31`) · ob der Pruefer am Haltepunkt gegen nackte Zahlen gebaut wird.
**Nicht vergessen:** Gepusht bis `93944370`, danach 12 Commits lokal. `tests/test_alle_selftests.py` wird von einer FREMDEN Sitzung bearbeitet (Zaehler wechselte mehrfach) -- Finger weg, bis sie fertig ist. `git stash` bleibt verboten.
