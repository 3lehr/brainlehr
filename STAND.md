# STAND brainlehr — 2026-08-10T12:05:00+0200
**Zuerst lesen:** `docs/PLAN_WURZELORDNUNG_2026-08-10.md` §5, dann `docs/PLAN_B4_AUSWEIS_2026-08-09.md`.
**Zuletzt (`b8d1f09`, lokal, nicht gepusht):** §5 des Wurzelordnungs-Plans nachgetragen. Der Plan sah 19 Umzuege vor, gewandert sind **107** in drei Wellen (20 `migrationen/`, 19 Zweckordner, 68 `kern/melder/messungen/pflege`). Wurzel 88 → **3** `.py`, nicht 69 wie geplant. Die Begruendung stand vorher nur in Commit-Nachrichten.
**Der bezahlte Preis, jetzt schriftlich:** `kanten_aus_bedeutung.py`, `kanten_aus_lehren.py`, `hebb_kanten.py` sind trotz §0-Warnung nach `kern/` gewandert. `knowledge_relations.source` nennt weiterhin **5867 Mal den blossen Dateinamen ohne Ordner**, Herkunftsfelder sind per Trigger unveraenderlich. Aufloesbar nur, weil jeder Name im Baum genau einmal vorkommt — **wer einen zweiten `hebb_kanten.py` anlegt, macht 5867 Herkunftsangaben endgueltig mehrdeutig.**
**Behoben:** `tests/test_doctor.py` stand seit dem Umzug (10:06) rot — ueber sich selbst, nicht ueber `doctor`: er schrieb `WURZEL/doctor.py` fest, waehrend die Datei nach `kern/` gewandert war. Nutzt jetzt `Path(doctor.__file__)`. Der reale `doctor` meldet 40 Pfade, 0 tot.
**Testsuite:** 765 gruen / 8 rot / 6 Fehler — gleichauf mit dem Vorzustand vor allen drei Umzugswellen (762/8/6).
**Offen, unveraendert kritisch:** `origin/main` enthaelt **kein LICENSE, NOTICE, LICENSE_FAQ.md** — nur auf dem Arbeitszweig. Wer heute klont, bekommt Quelltext ohne Lizenz. CLA-Entwurf braucht anwaltliche Pruefung.
**Offen, gemessen:** Erstanlage aus `schema.sql` traegt zwei Tabellen weniger als der Betrieb (`knowledge_versions`, `schema_migrations`) — ein frischer Klon bekommt ein anderes Schema (`tests/test_brainlehr_umzug.py`).
**Startmelder sind Befunde, keine Aufgaben (`L-f1fcb9`):** `NODE_INDEX.md`, `antwort_treffer.json`, `auszug/*.jsonl` sind Erzeugnisse laufender Melder — nicht committen. „Pruefer: actor 94 % tautologisch" erledigt sich von selbst, seit Ausweise angemeldet werden. Die 17 Gegenprobe-Vermerke bleiben liegen, bis jemand die Messung braucht.
**Naechster Schritt:** frischen Klon `/Volumes/daten/Begod2026/_probe_brainlehr` nach README-Schnellstart aufsetzen und pruefen, ob eine Neuinstallation ohne unsere Datenbank durchlaeuft.
