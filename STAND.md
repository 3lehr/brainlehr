# STAND brainlehr — 2026-08-15T14:27:38+0200
**Autonomer Lauf, Betreiber abwesend.** Plan fortgeschrieben (`f05af23b`/`abd06928`): 65 Kennungen, neue Kategorie **"gebaut, aber wirkungslos"**. Linie H ist bis auf `H12` durch, Linie I zur Haelfte.
**Der beherrschende Befund des Tages, jetzt beziffert:** FUENFmal wurde ein Agent auf etwas angesetzt, das bereits gebaut war -- `97`, der Dienststart, `H4`, `H5`, `H7`. Jedes Mal, weil der PLAN es als offen fuehrte (`L-229bb2`). Alle fuenf Agenten haben es selbst entdeckt und gemeldet; einer loeschte seine fertige Doppelarbeit wieder. Der rettende Satz ist in jedem Auftrag: "Sieht der Code anders aus als hier beschrieben, halte dich an den Code und melde die Abweichung." **Vor jedem Bauauftrag jetzt Existenzprobe am Repo, gesucht nach der SACHE statt nach der Kennung.**
**Die Wache, die nie existierte:** `git stash` wurde heute von zwei Agenten ausgefuehrt. Ursache gemessen (`46500066`): Ein Arbeitsbaum unter `.claude/worktrees/` ist ein SCHNAPPSCHUSS -- er kopiert `.claude/` beim Anlegen und nie wieder. Dieser Baum entstand 05:44, die Wache kam 11:26. **Meine Pruefung heute frueh galt dem Hauptbaum; gearbeitet wird hier.** Widerlegt ist die schlimmere Vermutung: `PreToolUse` feuert bei Subagenten sehr wohl, es war Konfiguration. Behebung laeuft.
**Gebaut seit 14:00:** Dienst-Gegenstueck zum Start (`9e9cf83c`) -- LaunchAgent und Rueckfallweg existierten laengst seit `648432e`, gefehlt hat nur der Stopp, der nie einen fremden Prozess toetet. · openlehr: `H2`/`H3` (`3d1c4e88`), `H6`-Luecke (`21b00d8f`), `H4` verifiziert (`c79a1402`). · `I3` Auflage 3 (`4f9428c6`): **Import fremder Tabellendateien ENTSPERRT** -- 2 `new Function`-Stellen, 0 aus dem Dokumentmodell erreichbar, vier Angriffsversuche unter Netzsperre ohne Treffer. Gegenbefund: `WEBSERVICE` hat gar keinen Executor; und Univer erzwingt technisch eine VERBOTSliste, waehrend ADR-016 ausdruecklich eine Positivliste verlangt -- diese Auflage ist so nicht erfuellbar.
**Wo die Fehler wirklich sitzen** (`58569423`): 443 Lehren nach SCHRITT sortiert. 200 (45,1 %) haengen an einem Schritt und koennten einen Mechanismus bekommen, 243 an keinem. Groesster ungeschuetzter Schritt: **beim Auswerten einer Messung, 36 Lehren, kein Mechanismus** -- genau die Fehlerklasse dieses Tages. Dazu: **`push_guard.py` ist NIRGENDS verdrahtet**, weder in einer der beiden Einstellungsdateien noch als `pre-push`. Heute wurde gepusht, ohne dass etwas geprueft hat. Behebung laeuft.
**Zwei Messungen zur eigenen Arbeitsweise:** Abruf wirkt in 11,1 % (37 von 334, `f4ebc128`). Keine Formulierungseigenschaft der Hausregeln erklaert die Verstoesse, ein Mechanismus ebensowenig (`c1d18f79`). Meine eigenen Zahlenangaben waren heute VIERmal falsch -- alle vier waren zaehlbar.
**Wartet auf dich:** Eintrittsweg auf markus-lehr.de (Serverzugriff, einziger Punkt mit Uhr) · 22 GB Sicherungen loeschen? · Name der Steuerdomaene · Domaenen-Repo bei GitHub? · G5 · Linie E (`20`/`23`/`29`/`31`) · LaunchAgent laden (`dienst/LIESMICH.md`, eine Handbewegung) · Pruefer am Haltepunkt gegen nackte Zahlen?
**Nicht vergessen:** Gepusht bis `93944370`, danach 20 Commits lokal in brainlehr, 6 in openlehr. `tests/test_alle_selftests.py` gehoert einer FREMDEN Sitzung. `82`/`83`/`87` sind Phantomkennungen, keine Aufgaben (`L-58d434`).
