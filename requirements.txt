# Brainlehr dependency policy
#
# This file is intentionally not an install input.  The canonical declarations
# are `pyproject.toml`; their reproducible, hash-pinned resolution is `uv.lock`.
# Core MCP startup is stdlib-only.  Use `uv sync --all-groups --all-extras` for
# development/release evidence, or select one explicit optional extra.
#
# Keeping unconstrained convenience requirements here would create a second,
# stale resolver input and make advisory results non-reproducible.
